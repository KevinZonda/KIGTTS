# PROJECT

## 2026-07-15 Android 原生主题色

- 范围：仅 `android-app/`，Flutter 端未修改。
- 设置入口：`系统 -> 显示与主题 -> 主题色`。
- 颜色选择：名片主题色与应用主题色共用同一个颜色选择弹窗，包含预设色、HSL 滑杆、预览和 HEX 输入。
- 持久化：新增主题色 ARGB 与主题色色调修正开关；默认色为 `#038387`，修正默认关闭，旧用户没有新配置时保持原观感。
- 色调修正：使用 Material Color Utilities 的 HCT/TonalPalette。暗色主题生成较亮的主题色；亮色主题在选色过浅时生成同色系较深强调文字；填充控件内容色按对比度自动选择。
- 风险提示：修正关闭时，选择在亮色或暗色界面中对比不足的颜色会弹窗询问是否开启修正，用户仍可选择保持原色。
- 覆盖范围：主界面、悬浮窗、MD2 按钮/输入框/选择状态、名片与音色头像裁剪顶栏。

## 验证

- `:app:testDebugUnitTest`：4 项通过，0 失败。
- `:app:assembleDebug`：通过，包含 Kotlin、Compose、DEX、arm64 CMake/AEC3/eSpeak 构建。
- 验证命令：`./gradlew.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon`
- 构建环境没有连接 ADB 设备，尚未执行真机交互检查。

## 2026-07-15 Android 语音包元数据并发修复

- 范围：仅 Android 原生端，Flutter 端未修改。
- 根因：语音包列表刷新会反复重写 `voicepack.json`，多个 `ModelRepository` 实例共用固定的 `voicepack.json.tmp`；并发写入时一个任务会先移动或删除临时文件，另一个任务随后执行 `copyTo`，从而抛出 `NoSuchFileException`。
- 文件写入：新增按语音包目录共享的进程内锁，临时文件改为唯一名称，并通过同目录原子替换更新目标文件；合法的旧元数据会先保留为 `.bak`。
- 读取行为：已有且规范化后未变化的 `voicepack.json` 不再在每次列表扫描时重写；仅在缺失、损坏、从备份恢复或需要规范化时修复写入。
- 目录操作：同名语音包替换、元数据读改写、头像更新、导出和删除共用目录锁；列表扫描忽略尚未完成的 `.import-*` 解压目录。
- 刷新行为：语音包列表刷新取消过期任务，并通过 `Mutex` 保证磁盘扫描不会在同一 `MainViewModel` 内并发执行。

## 本次验证

- `:app:testDebugUnitTest`：6 项通过，0 失败，其中新增 2 项语音包文件并发测试。
- `:app:assembleDebug`：通过，包含 Kotlin、DEX、arm64 CMake、本地库合并与 APK 打包。
- 验证命令：`./gradlew.bat :app:testDebugUnitTest --no-daemon`、`./gradlew.bat :app:assembleDebug --no-daemon`。
- 尚未在真机上使用名为“纳西妲（1）”的实际语音包复测启动流程。

## 2026-07-15 Android LED 字幕模式

- 范围：仅 Android 原生端，Flutter 端未修改。
- 入口：便捷字幕大字幕操作区新增 LED 点阵图标，进入便捷字幕内部的独立沉浸式全屏路由。
- 显示逻辑：已提交字幕使用单行 LED 点阵显示，超出屏幕宽度时无缝循环滚动；换行会折叠为空格。粗体状态沿用便捷字幕设置。
- 输入预览：输入栏正在编辑时沿用便捷字幕大字幕预览；输入栏折叠后是否继续预览由“输入框内容保持预览”控制。提交字幕或快捷文本后，旧的未发送输入不会覆盖新字幕，继续编辑后才恢复预览。
- 全屏交互：左上返回、右上设置、右下键盘与快捷文本、左下锁定/解锁均为透明背景单图标按钮；3 秒无操作后降低透明度。
- 手势：左滑打开右侧快捷文本，上滑打开暗色输入底栏；点击空白区域折叠面板。进入页面时显示 3 秒操作提示和底部暗色渐变，也可点击立即关闭。
- 锁定：锁定后自动关闭输入法和全部面板，拦截页面返回与侧边抽屉手势，仅保留左下角解锁按钮可操作；字幕仍会继续更新和滚动。
- 快捷文本：右侧面板复用便捷字幕分组、当前选中分组、文本提交和 TTS 播报行为。
- LED 设置：支持 LED/背景颜色、圆点/方点、点阵密度、发光及强度、显示高度、滚动速度/方向/循环间距、短文本对齐、屏幕常亮和独立亮度；修改即时预览并持久化，低对比颜色只提示不自动修正。
- 渲染：中文先离屏栅格化为分块点阵位图，动画帧只平移缓存位图；点阵相关滑杆更新做 90ms 防抖，滚动速度即时生效。
- 文件结构：LED 路由、窗口副作用、主交互、覆盖层、输入/快捷文本面板、设置面板和渲染器分别独立，新增 UI 文件均不超过 500 行。

## LED 字幕验证

- `:app:testDebugUnitTest`：11 项通过，0 失败，其中 3 项覆盖 LED 设置，2 项覆盖自动滚动、拖动和惯性状态。
- `:app:assembleDebug`：通过，包含 Kotlin、DEX、arm64 CMake、本地库合并与 APK 打包。
- APK：`android-app/app/build/outputs/apk/debug/app-debug.apk`，104016431 字节。
- SHA-256：`33e8da659083b2db24cff596321746d121662de127a5c7c6d506a12bd5064fb8`。
- 当前环境没有 `adb`，尚未执行真机上的点阵视觉、手势、输入法、屏幕锁定和系统栏恢复检查。

## 2026-07-15 Android LED 字幕体验修正

- 渲染模式：LED 设置新增“使用正常字形”开关；开启后保留单行循环滚动，但改用抗锯齿正常字形。点阵/正常字形共用字幕颜色，默认值调整为白色。
- 主题一致性：返回、设置、锁定、键盘、快捷文本及面板内选中态均使用软件当前主题色，不再把字幕颜色当作 UI 控件主题色。
- 输入预览：输入栏激活后改为复用便捷字幕横屏模式的浮窗预览，浮窗自动避开输入底栏和软键盘；输入栏关闭后仍遵循“输入框内容保持预览”设置。
- 动效：已提交字幕切换时淡入淡出；快捷文本分组标题和内容使用轻量横向切换动画，右侧分组选中态平滑过渡并自动滚动到当前分组。
- 入口图标：便捷字幕操作区改用指定的 `LED` 字样矢量路径，不再使用通用点阵图标。
- 刘海适配：LED 内容背景延伸至显示刘海区域，外层不再重复叠加横屏安全边距；透明控制按钮仅由页面内部应用一次 `safeDrawing`，退出 LED 后恢复原窗口刘海模式和系统栏颜色。
- 滚动交互：点阵与正常字形共用页面级连续动画时钟，避免内容渲染或切换期间停止自动滚动；左右拖动可直接移动字幕，松手后按速度产生衰减惯性，之后自动恢复设置的滚动速度和方向。
- 快捷手势：仅达到距离与速度阈值的快速左滑才打开快捷文本，普通横向拖动只滚动字幕；LED 设置新增“快速左滑打开快捷文本”开关，关闭后横向手势全部用于字幕滚动，上滑输入保持不变。

## 2026-07-15 Android 单层启动屏

- 根因：应用同时使用系统 Splash 和 `settingsLoaded=false` 时的 Compose Logo 页面，设置加载稍慢时会连续显示两次 Logo，并可能因两套尺寸规则产生位置跳动。
- 启动流程：新增 AndroidX SplashScreen Starting Theme，由系统启动屏持续显示至 `MainViewModel` 完成设置加载，再直接进入引导页或主界面。
- Compose 衔接：移除第二个 `KigttsStartupLoadingScreen` Logo；设置尚未加载时仅保留与应用主题一致的纯背景兜底，不再出现第二次 Logo。
- 主题资源：亮色和暗色 Starting Theme 继续使用各自的 Logo 与背景色；正式应用主题的 `windowBackground` 改为纯色，退出 Splash 后不会再次透出旧启动图。
- 验证：合并后的 Debug Manifest 已确认 `MainActivity` 使用 `Theme.KGTTS.Starting`；Kotlin 编译、11 项单元测试和 Debug APK 打包均通过。

## 2026-07-16 Android 暗色抽屉与 LED 控件修正

- 抽屉遮罩：修复暗色主题误用浅色 `onSurface` 作为遮罩基色的问题；亮暗主题统一使用黑色遮罩，仅保留不同透明度。
- LED 透明按钮：返回、设置、快捷文本、键盘和锁定按钮改用随 LED 背景明暗自动选择的黑/白中性色，不再应用软件主题强调色。
- 正常字形默认值：继续保持“使用正常字形”默认关闭，即新用户默认启用点阵渲染；现有持久化设置不强制覆盖。
- LED 亮度默认值：新用户和“恢复默认设置”默认关闭“亮度跟随系统”，独立屏幕亮度设为 100%；已有持久化亮度设置保持不变。
- LED 入口图标：在原 24dp 画布内将指定 `LED` 矢量路径以中心为基准放大约 10%，按钮占位尺寸保持不变。

## 2026-07-16 Android 弹窗小高度滚动修复

- 新增独立的 `Md2ScrollableDialog` 共用组件，弹窗宽高由当前窗口的实际可用区域约束，不再使用可能超出横屏或分屏高度的固定内容高度。
- 滚动结构：标题和底部操作按钮固定，仅中间内容区在空间不足时滚动，避免内容把标题顶出弹窗或让操作按钮离开屏幕。
- 系统栏与键盘：弹窗显式应用 `safeDrawing` 和 IME Insets，避让通知栏、导航栏、显示刘海以及软键盘。
- 覆盖范围：应用/名片共用取色弹窗、语音识别资源包下载源弹窗和 Kokoro 下载源弹窗。
- 取色弹窗移除原有 `560dp` 内容区上限；下载源弹窗在横屏、小窗口和键盘弹出时可连续滚动编辑全部地址与下载源选项。

## 弹窗修复验证

- `:app:compileDebugKotlin`：通过。
- `:app:testDebugUnitTest`：11 项通过，0 失败，0 错误。
- `:app:assembleDebug`：通过，包含 Kotlin、DEX、arm64 CMake、本地库合并与 APK 打包。
- APK：`android-app/app/build/outputs/apk/debug/app-debug.apk`，104016431 字节。
- SHA-256：`30b68d913dc4bfa6d8106ac4ce8b9631f8793424cb38705af5eb9019fe28ce68`。
- 当前环境没有连接 ADB 设备，尚未进行真机横屏、分屏、刘海屏和软键盘交互检查。

## 2026-07-16 Android 应用字体管理

- 范围：仅 Android 原生端，Flutter 端未修改。
- 设置入口：`设置 -> 系统 -> 显示与主题 -> 字体`，二级页沿用语音包页面的卡片列表、返回导航和动态顶栏机制。
- 字体列表：保留不可移除的“系统字体”；自定义字体以整行大字号字体名作为渐进式预览，只有进入可见区域后才从磁盘加载字体文件。
- 导入与管理：顶栏提供 TTF/OTF 导入和远端下载；导入文件会先通过 OpenType 表解析与 Android `Typeface` 双重校验，再复制到应用私有目录。字体支持选择、删除和许可证查看。
- 可变字体：解析 OpenType `fvar/wght` 轴；可变字体显示字重设置按钮、当前字重和实时预览，字重选择随字体偏好持久化。
- 全局应用：新增字体 ID/字重偏好；字体在后台异步加载，加载完成后替换整套 Compose Typography，失败或文件缺失时自动回退系统字体，不阻塞启动。
- 下载安全：远端使用 `font_manifest.json` schema 1，限制清单大小和相对路径，字体与许可证均执行 SHA-256 校验，再通过临时目录原子替换安装。
- 下载源：默认优先魔搭，Hugging Face 保留备用源选择。魔搭公开仓库为 `https://modelscope.cn/models/LHTSTUDIO/KIGTTS_FONTS_Resource`，Hugging Face 备用仓库为 `https://huggingface.co/LHT02/KIGTTS_FONTS_Resource`。
- 资源仓库：本地发布目录为 `D:\KIGTTS_FONTS_Resource`，包含字体、许可证、来源链接、版本、文件大小和哈希清单。
- 首批资源：思源黑体 CN、思源宋体 CN、HarmonyOS Sans SC 可变字体、阿里巴巴普惠体 3.0、IBM Plex Sans SC、小赖字体和 GNU Unifont，共 7 项。
- 授权处理：OFL 字体保留原许可证；HarmonyOS Sans 保留专用字体授权并在 README 显著声明；阿里巴巴普惠体链接其官方法律声明，未误标为开源许可证。
- 发布状态：魔搭与 Hugging Face 均已上传完整的 16 个文件；Hugging Face 发布提交为 `4b3a4aaa9ac455f086e47ccadf70074697c91070`，软件默认备用源已切换到 `LHT02` 名下仓库。

## 字体功能验证

- `:app:compileDebugKotlin`：通过。
- `:app:testDebugUnitTest`：15 项通过，0 失败，0 错误，新增 OpenType 可变字重解析和字体偏好规范化测试。
- `:app:assembleDebug`：通过，包含 Kotlin、DEX、arm64 CMake、本地库合并与 APK 打包。
- `:app:lintDebug`：完整扫描仍有 18 个现有错误；字体模块发现的 API 33 `readNBytes` 调用已改为兼容 API 26 的有界流读取，后续复扫字体文件无新增 Lint 错误。
- 资源清单：7 个条目的字体/许可证文件、文件大小与 SHA-256 全部匹配。
- Hugging Face 镜像：反向下载全部 16 个文件并与本地发布目录逐文件核对 SHA-256，结果全部一致。
- APK：`android-app/app/build/outputs/apk/debug/app-debug.apk`，104016431 字节。
- SHA-256：`8e060f9f6f907ca63ac9951aa7236770081ad62f4638abdda83497e3bfc07f51`。
- ADB 可用但当前没有连接设备，尚未执行真机字体渲染、导入、切换、下载和字重交互检查。

## 2026-07-16 Android 字体字重、行距与资源修正

- 范围：仅 Android 原生端与独立字体资源仓库，Flutter 端未修改。
- 许可证修复：确认魔搭会把 HarmonyOS Sans、IBM Plex Sans SC、GNU Unifont 三份许可证的 CRLF 规范化为 LF；本地文件统一改为 UTF-8 无 BOM + LF，并更新清单 SHA-256，解决魔搭源许可证校验失败。
- 新增字体：加入 Noto Sans SC 2.004 与 Noto Serif SC 2.003 官方可变字体；思源黑体 CN 与思源宋体 CN 改用官方 `250-900` 可变字重文件。
- 可变字重：继续使用连续滑条，读取字体真实 `fvar/wght` 范围；清单可以把上游轴默认值调整为适合界面的 400，但不会突破字体真实范围。
- 静态多字重：清单 schema 保持 1，并新增向后兼容的 `weightFiles` 可选字段。IBM Plex Sans SC 安装 `100/200/300/400/450/500/600/700` 八个真实文件，不使用合成粗体。
- 分段滑条：静态多字重字体使用带吸附的分段滑条，滑块位置按真实文件档位映射，支持 IBM `Text 450` 这类非等差字重；列表预览与全局字体共用同一套归一化逻辑。
- 升级处理：已安装旧版思源或 IBM 时，下载弹窗显示“更新”并原子替换旧安装；当前正在使用该字体时通过进程内变更事件立即重新加载，无需重启。
- 元数据兼容：旧版单字体 `font.json` 可直接读取；新元数据同时保留主文件字段与多字重文件列表，旧客户端仍可按主文件安装新版清单。
- 行距处理：自定义字体统一关闭额外字体内边距，并为 Compose Typography 设置稳定行高与居中裁切，降低 IBM Plex Sans SC、思源黑体等字体自身纵向指标差异造成的列表和正文行距跳变；系统字体布局保持原样。
- 文件拆分：清单解析、元数据存储和字体变更通知拆为独立文件；字体相关 Kotlin 文件均未超过 500 行。
- 资源版本：`D:\KIGTTS_FONTS_Resource` 更新为 `2026.07.16.3`，共 9 个字体条目、24 个被清单引用的字体/许可证文件，引用资源总计 210253071 字节。
- 镜像发布：魔搭与 Hugging Face 均已发布并使用应用 User-Agent 逐文件反向下载校验，两个源均为 24/24 文件大小与 SHA-256 一致；Hugging Face 提交为 `b14b11c6c0e115b6d8d034e546051de22517dc2c`。

## 字体修正验证

- `:app:compileDebugKotlin`：通过。
- `:app:testDebugUnitTest`：17 项通过，0 失败，0 错误，0 跳过。
- `:app:assembleDebug`：通过，包含 Kotlin、Compose、DEX、arm64 CMake、本地库合并与 APK 打包。
- 构建使用仓库外 Gradle 输出与项目缓存目录，规避百度网盘上传临时文件和缓存锁干扰。
- APK：`D:\KGTTS_GradleOut\build\app\outputs\apk\debug\app-debug.apk`，101658042 字节。
- SHA-256：`f7a84153d1f3de37af02f37082120891e8d2ac7272a26540c12716a7583adc0b`。
- 当前没有连接 Android 设备，尚未执行真机上的字体下载、旧安装更新、分段吸附、字重视觉与行距检查。

## 2026-07-16 Android LED 锁定控件动效

- 锁定 LED 屏幕后，返回、设置、快捷文本和键盘按钮作为同一控件层淡出并在动画结束后移出组合树，锁定期间不可见且不可交互。
- 左下角锁定按钮不参与该控件层动画，锁定后继续显示为解锁按钮。
- 解锁后四个操作按钮同步淡入恢复；淡出期间立即禁用点击，避免透明动画过程中误触。
- `:app:testDebugUnitTest` 17 项通过；`:app:assembleDebug` 通过，APK 信息沿用上方本次最终构建结果。

## 2026-07-16 猫啃网字体资源扩充

- 范围：仅更新独立字体资源仓库与本项目记录，Android/Flutter 应用源码均未修改；现有 `font_manifest.json` schema 1、许可证校验和可变字重解析可直接兼容本批资源。
- 上游筛选：检查 `maoken-fonts` 组织的 14 个公开仓库，排除 `.github` 与 `issues-discussion` 两个非字体仓库，将其余 12 个字体项目中适合应用内下载的 17 个字体条目加入资源库。
- 新增条目：猫啃网扛重族黑/宋、荆南缘默体、荆南麦圆体、猫啃什锦黑、无界黑、龙珠体、荆南波波黑、演示佛系体、演示悠然小楷、演示春风楷、演示夏行楷、演示秋鸿楷、猫啃网风雅宋、猫啃硬笔楷书、械黑 GB、站酷快乐体。
- 版本固定：优先使用上游正式 Release；没有 Release 的 `slidefont` 与 `zcool-kuaile` 固定到明确提交，避免默认分支后续变化导致哈希失效。猫啃硬笔楷书使用稳定发布版 `0.10`，未采用主分支开发文件。
- 授权记录：本批字体均按各自上游的 SIL OFL 1.1 文件分发；`slidefont` 的 5 个演示字体共用上游授权并开源的许可证文件；站酷快乐体明确标记为上游字体 fork，不表述为猫啃网原创字体。
- 字重支持：械黑 GB 采用 `wght 100-700` 可变字体并提供连续字重设置；其余 16 个条目经 OpenType 表检查均为单一静态字重，无需伪造多字重档位。
- 资源版本：`D:\KIGTTS_FONTS_Resource` 更新为 `2026.07.16.4`，当前共 26 个字体条目、53 个被清单引用的字体/许可证文件，引用资源总计 395896736 字节。
- 本地校验：17 个新增字体均可由 FontTools 解析；全部 53 个引用文件的大小与 SHA-256 匹配，许可证统一为 UTF-8 无 BOM + LF，资源目录不存在未被清单引用的遗留文件。
- 镜像发布：魔搭与 Hugging Face 均已上传本批 29 个新增资源文件及更新后的清单/README；Hugging Face 提交为 `48719fa66d8593cd9afbe4944ffe895949dc4178`。
- 反向验证：使用应用实际 User-Agent 从两个镜像分别下载 29 个新增资源，魔搭与 Hugging Face 均为 29/29 文件大小及 SHA-256 一致；此前版本的 24 个资源已在上一轮逐文件验证。

## 2026-07-16 Android eSpeak-NG 原生库同步

- 将 `jniLibs/arm64-v8a/libespeak-ng.so` 同步为当前 CMake/NDK r26b 构建产物；该库由 `espeak_jni` 调用，为 Piper 音色包提供文本到音素转换。
- 源目录库为 ELF64 AArch64 动态库，大小 1471256 字节，SHA-256 为 `66e5de99842cf14104c093d100b6011a1d7fa4dc98205bca491361405ec3ab38`，与 CMake 输出和 Gradle 合并 JNI 产物逐字节一致。
- ELF 的全部可加载段按 `0x4000` 对齐，符合 Android 16KB 内存页兼容要求；运行时依赖仅为系统 `libm.so`、`libdl.so` 和 `libc.so`。
- `:app:assembleDebug` 通过；APK 内剥离调试符号后的 `libespeak-ng.so` 为 444392 字节，SHA-256 为 `4bf3d915060a4d4b1522daf3334d1bf71bbf52917ca50c0416767e039ade2a52`。

## 2026-07-16 Android 悬浮窗自定义字体

- 设置入口：`设置 -> 系统 -> 显示与主题 -> 悬浮窗使用系统字体`；默认关闭，旧用户没有该键时同样按关闭处理，因此悬浮窗默认跟随当前应用字体和字重。
- 独立控制：开关开启后仅悬浮窗回退 Android 系统字体，不改变主界面、便捷字幕或 LED 页面正在使用的应用字体。
- 字重适配：可变字体按真实 `wght` 轴生成普通/粗体字形；静态多字重字体沿用当前偏好的相对偏移并吸附到真实文件档位；单文件字体保留系统合成粗体回退。
- View 适配：悬浮窗使用传统 Android View，新增独立异步 Typeface 加载器和动态 View 层级字体应用；后续创建的快捷文本、音效板、名片和输入控件也会自动使用当前字体。
- 图标保护：Material Symbols TextView 使用专用排除标记，始终保留图标字体，避免应用字体替换后出现文字图标或方框。
- 实时刷新：切换字体、调整字重、切换系统字体开关或更新当前字体文件时，会重新加载字体并重建悬浮窗口，同时保留面板展开状态和悬浮按钮位置；加载失败时安全回退系统字体。
- 验证：`:app:compileDebugKotlin`、`:app:testDebugUnitTest` 和 `:app:assembleDebug` 均通过；单元测试共 21 项，0 失败、0 错误、0 跳过。
- APK：`D:\KGTTS_GradleOut\build\app\outputs\apk\debug\app-debug.apk`，100380232 字节，SHA-256 为 `44a83b53b3c81dccc1f30b8bb1b8b76b015d0ae2872e09be2c5aa3bcb02ee00a`。
- 当前没有连接 Android 设备，尚未执行真机上的自定义字体切换、悬浮窗热刷新、动态图标和输入控件检查。

## 2026-07-16 Android 悬浮窗 FAB、文本输入与状态面板

- 范围：仅修改 Android 原生端；主界面便捷字幕 FAB 的点击与 PTT 手势保持原样，只在缺少语音识别资源时把图标改为 `mic_off`。
- 悬浮文本输入：悬浮窗操作 FAB 可打开独立的全屏文本输入层，底部暗色输入栏提供光标左右移动、发送时播放开关、清空和发送按钮；输入期间在键盘上方显示大字号实时预览，并沿用当前快捷字幕字体、字重、对齐和草稿。
- 发送跳转：从启动器、迷你名片或音效板等非迷你快捷字幕界面发送文本后，自动切换到迷你快捷字幕并上屏；PTT 松手后的最终识别文本会继续同步到已打开的悬浮输入框，避免只保留半句预览。
- 资源缺失：悬浮窗 FAB 显示 `keyboard` 并点按打开文本输入；主界面和悬浮窗顶栏折叠按钮只保留音频图标，展开面板隐藏麦克风音量、音频输入设备和按住说话行。
- FAB 语音优先：关闭 PTT 时点按开关语音识别、长按打开键盘；开启 PTT 时保持按住说话，竖屏下滑或横屏右滑后松手转入悬浮文本输入。
- FAB 键盘优先：新增“悬浮窗 FAB 优先使用键盘输入”设置，默认关闭；开启后 FAB 使用 `keyboard` 图标，点按打开输入，非 PTT 长按开关语音识别并显示状态消息，PTT 长按进入按住说话。
- 首次引导：语音识别资源已安装且此前未提示时，首次进入悬浮面板会显示不可跳过的操作说明，并要求选择键盘输入优先或语音识别优先；选择和已提示状态持久化，后续不重复弹出。
- TTS 状态图标：“禁用TTS”改用 `graphic_eq_off`；禁用状态只改变音频图标，不再把麦克风图标改成 `mic_off`。
- LED：快捷文本按钮改用 `subtitles`；快速左滑除位移和末速度外新增 420ms 松手时限，长按拖动后再加速不会误开快捷文本。
- 代码拆分：悬浮文本输入和首次模式引导分别放在 `OverlayTextInputWindow.kt` 与 `OverlayFabModeGuideWindow.kt`，未继续把完整 View 构建逻辑塞入悬浮窗服务大文件。

## 悬浮窗 FAB 与 LED 验证

- `:app:compileDebugKotlin`、`:app:testDebugUnitTest` 与 `:app:assembleDebug` 均通过；24 项单元测试全部通过，0 失败、0 错误、0 跳过。
- 新增 LED 手势判定测试，覆盖阈值时间内快速松手和长按后高速滑动不触发两种情况；新增悬浮 FAB 默认语音优先及一次性引导默认状态测试。
- 构建使用独立 Gradle 项目缓存和仓库外输出目录，避免正在运行的 IDE 占用仓库内 `app/build` 与 `.gradle` 文件锁。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，99743544 字节。
- SHA-256：`74ba671ae72e41459e0a5436540720976fb21da21ee9bfda0dd84036584ab5d0`。
- 当前没有连接 Android 设备，尚未进行悬浮窗软键盘、PTT 方向手势、首次引导、系统栏避让和 LED 快速左滑的真机交互检查。

## 2026-07-16 Android 悬浮窗输入体验、图标与 LED 字体修正

- 范围：仅 Android 原生端，Flutter 端未修改；主界面便捷字幕 FAB 的原有点击与 PTT 操作逻辑继续保持不变。
- 输入栏：悬浮文本输入改为监听 IME 与系统栏 Insets，并保留可见窗口高度回退计算；输入栏会稳定停在软键盘上方，预览区域同步缩放，不再被键盘或状态栏遮挡。
- 清空交互：清空按钮移入输入框右侧，仅在输入框有内容时显示。点击发送后先清空输入控件与共享草稿，再执行上屏和退出动画；重新打开悬浮输入时不会恢复刚发送的文本。
- 预览卡片：悬浮输入直接复用迷你便捷字幕大字幕卡片的尺寸、背景、圆角、阴影、字号自适应、字体、字重和对齐逻辑，不再维护一套外观近似但行为不同的预览。
- 光标与动效：预览使用与主界面输入预览一致的主题色实线光标，并跟随真实选择位置；悬浮输入框保留原生光标、插入水滴手柄、左右选择手柄和长按复制/剪切/全选菜单，同时将光标与手柄改为当前主题色、选区改为半透明主题色，兼容 Android 26 及以上版本。输入文字更新采用即时内容刷新加 140ms 淡入，输入层打开/关闭时对遮罩、预览卡片和底栏分别执行淡入、位移与缩放动画。
- 悬浮字高：仅在悬浮窗实际使用自定义字体时统一关闭 TextView 额外字体内边距，并按应用 Typography 的字号档位计算稳定行高；大字幕与全屏输入预览分别沿用主界面的 `1.15` 和 `1.36` 行高倍率，自动字号测量使用同一规则，系统字体布局不变。
- 启动器菜单：快速启动器外部应用条目的长按快捷菜单标题改用当前悬浮窗字体 Span；音频输入/输出设备的原生下拉菜单也使用同一处理，保留系统 PopupMenu 的位置、主题和点击行为。
- PTT 提示：麦克风优先且按住说话时的提示更新为“松手悬浮窗上屏，下滑输入文本”；键盘优先模式继续显示原有简短提示。
- TTS 图标：更新为官方 Material Symbols Sharp 字体并加入 `graphic_eq_off` 连字，不再使用临时绘制图标；字体文件为 846000 字节，SHA-256 为 `37f736898135d45dcf5e76c5b790b4f9bbe85ce59db7edff0fa9fec2e5f78595`。
- LED 字体：正常字形和点阵掩模渲染都改为使用当前应用字体及对应字重；字体加载在 LED 路由层异步完成，渲染器只接收已解析的 `Typeface`，系统字体或加载失败时安全回退 Android 默认字体。
- 启动稳定性：真机安装后发现 `QuickSubtitleScreen` 的超大局部 Compose Lambda 会触发 ART `VerifyError`。已将旋转字幕、操作按钮和显示内容拆为顶层私有 Composable，保留原有回调及 FAB 行为，并消除启动崩溃。
- 文件拆分：悬浮输入样式、预览卡片数据和选择监听输入框移入 `OverlayTextInputTypes.kt`，`OverlayTextInputWindow.kt` 本体降至 500 行以内。

## 本轮最终验证

- `:app:compileDebugKotlin`、`:app:testDebugUnitTest` 与 `:app:assembleDebug` 均通过；28 项单元测试全部通过，0 失败、0 错误、0 跳过，其中 4 项覆盖悬浮窗稳定行高的字号档位、显示缩放和大字幕倍率。
- Debug APK 已覆盖安装到设备 `cfc8ef16` 并保留应用数据；冷启动进入 `MainActivity`，进程存活且 Logcat 未出现 `AndroidRuntime` 错误。
- 真机确认悬浮输入栏位于软键盘上方，输入后显示内置清空按钮、共享样式大字预览和实际光标；发送 `Test123` 后退出输入层并清空草稿，用户确认操作正常。
- 最终包安装后设备进入安全锁屏，未绕过锁屏继续触发原生长按菜单；最终代码已恢复原生文本交互并通过编译、测试和打包，光标/手柄/选区着色仍需解锁后做一次视觉确认。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，100418644 字节。
- SHA-256：`d301699e00a6d6f51fc03b536512ff1d668276c3d6485a5c4f71c614573a0f2b`。

## 2026-07-16 Android 实时通知与悬浮窗文本菜单修正

- 实时通知：声明 Android 16 的 `POST_PROMOTED_NOTIFICATIONS` 非运行时权限并继续请求 promoted ongoing；胶囊短文本按 Unicode 码点限制为 7 个字符，不会截断 emoji 或代理对。
- 真机通知：设备已授予 promoted ongoing 权限，通知记录包含 `PROMOTED_ONGOING` 标志；顶部实时胶囊恢复显示，用户确认当前正常。
- 悬浮窗文本菜单：新增独立的 `OverlayTextContextMenuController`，不再依赖 `TYPE_APPLICATION_OVERLAY` 下不稳定的系统 ActionMode；提供全选、剪切、复制、粘贴以及按词选择，外观和淡入/缩放动效与主界面菜单保持一致。
- 图标保护：悬浮菜单显式启用 Material Symbols 连字并标记为字体替换排除项，自定义正文字体不会再把菜单图标替换成连字文本或错误字形。
- 输入控件：悬浮字幕输入和“添加软件快捷方式”搜索框共用主题色光标、手柄、选区、焦点描边与文本菜单；两处均已在真机确认 Material Symbols 图标正确。
- 单元测试新增实时胶囊短文本的码点限制、emoji 完整性和空文本回退覆盖。

## 2026-07-16 Android 主界面文本输入与字体下载源管理

- 输入框审计：主应用全部 Compose 输入框和弹窗均位于 `MainActivity` 顶层 `LocalTextToolbar` 作用域，统一使用自定义全选/剪切/复制/粘贴菜单；独立 `ComposeView` 仅用于无编辑控件的列表条目，未发现遗漏的原生 `EditText`。
- 选区配色：在应用根层提供 `LocalTextSelectionColors`，所有主界面及弹窗输入框统一使用经过明暗色和色调修正后的主题色手柄与半透明选区。
- 控件补齐：内置文件选择器搜索框和第三方快捷方式弹窗搜索框改用统一 `Md2OutlinedField`，补齐主题色焦点边框、标签和光标；LED 输入、快捷字幕、名片编辑和取色输入继续保留各自已定义的场景配色。
- 字体下载弹窗：移除列表内直接切换源的单选项，改为显示当前源并提供“管理下载源”入口；安装进行中会禁用入口，避免下载任务中途变更资源地址。
- 下载源管理：新增二级滚动弹窗，可手动编辑魔搭与 Hugging Face 仓库根地址、分别恢复默认地址，并在同一弹窗选择首选下载源；URL 必须是无查询参数的 HTTP/HTTPS 地址，也兼容粘贴到 `font_manifest.json` 的完整路径。
- 持久化与下载：两个仓库地址和首选源写入 UserPrefs；字体清单、字体文件、各字重文件和许可证均从保存后的当前源解析。切换源时通过请求序号丢弃旧清单响应，避免慢请求覆盖新列表。
- 兼容性：新旧用户未保存字体源设置时继续默认优先魔搭，魔搭和 Hugging Face 默认仓库地址保持现有线上资源路径不变。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过；11 个测试套件共 33 项，0 失败、0 错误、0 跳过。Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`，应用进程正常且 Logcat 无 `AndroidRuntime` 错误；设备当前不接受 ADB 注入点击，字体下载二级弹窗仍需手动完成一次视觉交互确认。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，100430209 字节。
- SHA-256：`6816249f4227192c348f5bc9c1a0b38795af1544c63f7dda4d9a5ca400352723`。

## 2026-07-16 Android 启动屏等待自定义字体

- 根因：启动屏原先只等待 `settingsLoaded`；设置完成后 Compose 才在 IO 线程解析自定义字体，导致主界面先以系统字体绘制，再替换成用户字体。
- 退出门控：启动屏现在同时等待设置与首次字体请求完成。系统字体无需额外等待；自定义字体解析成功或失败都会释放启动屏，不会因损坏或缺失字体卡住。
- 请求一致性：字体加载状态携带字体文件集合与当前字重，仅当完成状态与当前请求完全一致时才允许首帧显示，避免启动阶段状态切换误用上一次结果。
- 运行时行为：门控只影响当前 Activity 的首次显示；进入主界面后切换字体或字重仍按原有渐进加载流程更新，不会重新触发启动屏。
- 自动验证：新增 2 项字体请求状态测试；`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，12 个测试套件共 35 项，0 失败、0 错误、0 跳过。
- 真机验证：Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`。冷启动录屏的启动屏退出首帧已经使用当前自定义字体，没有系统字体闪现；额外连续 3 次冷启动均成功，`TotalTime` 为 1475/1661/1610 ms，Logcat 无 `AndroidRuntime`、`VerifyError`。
- 录屏：`D:\KGTTS_GradleOut\kigtts-startup-font.mp4`；过渡关键帧：`D:\KGTTS_GradleOut\kigtts-startup-transition-contact.png`。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，100431613 字节。
- SHA-256：`9ba2f978eae303dd64c09e3519d67362f1703f454690899de4f0a8fb11ad4d69`。

## 2026-07-16 Android Release 构建与真机安装

- `:app:assembleRelease` 完整通过，包含 `lintVitalRelease`、Release Kotlin/DEX、arm64-v8a RelWithDebInfo 原生库和 APK 签名任务；未跳过 Vital Lint。
- Google Maven 直连下载首次缺失的 `lint-gradle:31.5.2` 时发生 TLS 握手中断，本次构建通过仓库外临时 Gradle 初始化脚本追加阿里云镜像完成依赖解析；项目自身仓库配置未修改。
- APK 通过 `apksigner verify --verbose --print-certs`：使用 APK Signature Scheme v2，签名者为 `CN=LHT STUDIO`，证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- Release APK 已使用 `adb install -r` 保留数据覆盖安装到设备 `cfc8ef16`；设备包版本为 `0.1.1 (2)`，`pkgFlags` 不包含 `DEBUGGABLE`。
- 真机冷启动状态为 `ok`，本次 `TotalTime` 530 ms；应用进程持续运行，进程 Logcat 无 `FATAL EXCEPTION`、`VerifyError`。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\release\app-release.apk`，95390864 字节。
- SHA-256：`675bb3da9b82a0e7b45f90288ba31a6be7dcee121287bb82ecfa1dcea92e02ac`。

## 2026-07-16 Android 局域网投屏与网页遥控

- 范围：仅修改 Android 原生端，Flutter 端未改动；在抽屉“悬浮窗与热键”下方新增“投屏与遥控”，投屏服务默认关闭且只在用户手动开启后运行。
- 设置迁移：原设置页的“蓝牙媒体标题字幕”和“实时通知”移入新页面，分别改为“蓝牙设备字幕显示”和“实时通知”，移除实验性描述并保留通知权限引导。
- 局域网服务：新增独立前台服务和 NanoHTTPD WebSocket 服务，提供 `/display`、`/remote`、静态资源、自定义字体、临时音频文件与健康检查路由；不使用 PIN 或令牌，停止服务后立即关闭连接并清理临时媒体映射。
- 多地址处理：自动发现 Wi-Fi、热点、有线和 USB 的私有 IPv4，过滤回环、链路本地与 `rmnet/ccmni/pdp/radio` 蜂窝接口；多个本机地址可点选，二维码与显示 URL 会同步切换。
- 二维码卡片：投屏显示和网页遥控器使用卡片内 MD2 Tab 切换，切换包含淡入淡出与水平位移动画；地址文本可点击复制。
- 投屏页面：支持自适应大字幕与 LED 单行滚动两种模式，保持便捷字幕文本、输入预览、字体/字重、对齐、LED 点阵样式和文本切换状态同步；自定义字体通过局域网按需加载，系统字体模式不发送无效字体请求。
- 网页遥控器：支持输入后朗读、仅上屏、重新朗读、清空、启停识别、快捷文本分组和打开主软件；静态 HTML/CSS/JS 不依赖 Node 运行时，并适配桌面及窄屏单列布局。
- 网页主题与交互：网页跟随软件明暗主题和主题色，高亮主题色会按亮度自动选择深色或白色前景；全部图标按钮、命令按钮、显示模式 Tab、分组 Tab 和快捷文本按钮加入 MUI/Material 按压点扩散波纹，鼠标、触摸及键盘 Enter/Space 均可触发。
- 音频路由：新增“仅本机 / 仅网页 / 本机与网页”三种持久化模式。TTS 以单声道 PCM 实时流送，音效板通过临时媒体路由流送；网页需先主动启用音频以满足浏览器自动播放限制，仅网页模式没有接收端时自动回退本机播放。
- 遥控桥接：网页命令由 `RealtimeHostService` 执行，沿用现有快捷字幕、TTS 队列和实时识别状态；投屏服务通知点击后会直接返回“投屏与遥控”页面。
- 依赖合规：新增 `org.nanohttpd:nanohttpd-websocket:2.3.1`，BSD 3-Clause 完整声明已写入 `THIRD_PARTY_LICENSES.md`。
- 构建验证：`node --check`、`:app:testDebugUnitTest` 和 `:app:assembleDebug` 通过；13 个测试套件共 38 项，0 失败、0 错误、0 跳过。为避开百度网盘锁定仓库内构建缓存，本轮临时将 Gradle build、项目缓存和 CMake staging 全部放到 `D:\KGTTS_GradleOut`，没有修改正式构建目录配置。
- 真机验证：Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`。新入口、设置迁移、服务启停和 Wi-Fi 地址 `192.168.60.171` 正常；最终地址列表不再出现蜂窝接口，HTTP 六个关键路由均返回 200，WebSocket 可收到字幕、主题、字体和快捷文本状态，当前进程 Logcat 无 `AndroidRuntime`、`FATAL EXCEPTION` 或 `VerifyError`。
- 网页验证：桌面 1280px 与窄屏 390px 均无横向溢出，投屏页无页面滚动，手机浏览器可打开遥控器；黄色主题下前景色自动切换为 `#151515`，波纹的鼠标/触摸与键盘触发均在浏览器中确认，控制台无 warning/error。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，99909010 字节。
- SHA-256：`254292c2119f314ff0a9ece73c02b03825c6d9f87f9214b20ddaac742f19fb5f`。

## 2026-07-16 网页 LED 复刻、电视遥控与独立样式设置

- 原生 LED 隔离：本轮未修改 `LedSubtitleScreen.kt`、`LedSubtitleSettingsPanel.kt` 或 `LedSubtitleSettings.kt`；网页显示样式使用独立的 `lan_cast_display_settings` 持久化键、UI 状态和 ViewModel 更新方法，不读取或覆盖原生 LED 设置。
- 网页投屏：投屏页复刻 LED 界面的单行滚动、正常字形/点阵渲染、输入预览、快捷文本、锁定、控制区自动淡出、180 度旋转、拖动惯性及快速左滑判定；保留网页全屏按钮，并增加长文本分块渲染和自适应大字幕模式。
- 电视遥控：支持方向键空间导航、确认、返回、菜单键、范围控件左右调节和锁定状态焦点约束；设置面板、快捷文本面板及确认弹窗会自动进入对应焦点范围。
- 网页遥控器：改为“字幕控制 / 设置”双页面抽屉导航，导航与显示模式 Tab 均带 Material Symbols 图标；控制页按便捷字幕布局提供大字幕预览、样式工具栏、输入、快捷文本和识别 FAB。
- 网页主题：遥控器默认跟随浏览器明暗模式，可切换跟随/亮色/暗色并通过 Cookie 保存；亮色卡片阴影强度减半。软件启用主题色色调修正时，原生端计算后的亮暗主题角色会同步给网页控件。
- 独立设置：Android“投屏与遥控”页新增“投屏显示设置”卡片和安全滚动弹窗，可独立配置字形、点阵色与形状、密度、发光、显示高度、速度、方向、间距、对齐、快速左滑、常亮和亮度；网页端设置面板修改同一套投屏配置，但不会影响原生 LED 页面。
- 网页音频与操作：投屏端和遥控器端均可启用网页音频并选择播放位置；按钮继续使用 Material 波纹，快捷文本分组切换、内容切换、面板开合和页面切换均带过渡动画。
- 自动验证：5 个 JavaScript 文件通过 `node --check`；`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，13 个测试套件共 39 项，0 失败、0 错误、0 跳过。APK 内 8 个 LAN Cast 资源与源码逐字节一致。
- 浏览器验证：桌面 1280x720 与手机 390x844 检查了遥控器双页面、亮暗主题、Cookie 持久化、50% 亮色阴影、投屏设置/快捷文本/输入面板、锁定按钮和电视方向键焦点；长文本输入预览会在底栏上方完成字号自适应。
- 真机验证：Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`；“投屏显示”卡片明确显示独立设置说明，服务启动后 `/health`、`/display`、`/remote` 和三个关键静态资源路由均返回 200。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，101919566 字节。
- SHA-256：`a409be6dab512ae7a60cefe4d2dcab3e21c4d744d5b1a6c1970399cc0e2c9556`。

## 2026-07-19 网页遥控器布局、稳定性与投屏端音频

- 移动端布局：遥控器消除页面横向溢出，竖屏大字幕占满快捷文本与底部输入栏之外的剩余高度；移除大字幕和快捷文本卡片标题、输入框浮动标签及麦克风 FAB，底部输入栏按 Android 便捷字幕样式重做。
- 快捷文本：普通模式使用横向条目和底部分组 Tab，紧凑模式使用独立分组切换器，横屏和窄屏改为纵向条目与侧边 Tab；条目使用分割线而非嵌套卡片，“添加当前字幕”只显示图标。
- 弹窗与桌面交互：长按或右键快捷文本可打开完整选择弹窗，宫格模式使用描边按钮与海拔，列表模式使用分割线；快捷文本和分组 Tab 支持鼠标拖动及滚轮，并按横竖布局自动选择滚动轴。
- 横屏操作：大字幕操作按钮移到卡片右侧，按钮过多时可触摸、滚轮或鼠标拖动上下滚动；侧边分组 Tab 保持图标居中且不显示组名，点击 Tab 和紧凑模式切组时通过独立气泡短暂显示分组图标与名称。
- 网页偏好：新增“使用紧凑快捷文本控件”开关。首次访问沿用应用当前值，之后写入一年期 `kigtts_remote_compact_quick_text` Cookie，刷新或重新打开浏览器后保持网页端选择。
- 连接稳定性：前端连接改为单实例、代际校验、单重试定时器、指数退避和 10 秒心跳；真机进一步定位到 NanoWSD 原 4 秒读取超时会主动切断空闲 WebSocket，服务端超时已调整为 60 秒。45 秒实测只有 1 次建连、0 次关闭、0 次错误，并连续发送 6 次心跳。
- 投屏端音频：界面统一改用“投屏端”文案，网页遥控器移除本地音频开关；服务端仅允许 `Display` 角色注册音频，并只向已启用音频的投屏连接发送音频控制消息和 PCM，旧版或伪装的 `Remote` 连接也无法接收。
- 浏览器验证：390x844、350x760 和 844x390 均无横向溢出；验证了横竖快捷文本布局、长按弹窗、分割线、Tab 居中与气泡、纵横滚动轴、右侧工具列滚动以及紧凑 Cookie 刷新保持，控制台无 JavaScript 语法错误。
- 音频真机验证：投屏页开启音频后实际收到 `audioStart/audioEnd`、16 个 PCM 帧（62464 字节）并启动 16 次浏览器音频缓冲播放；同时尝试以 `Remote + audioEnabled=true` 注册的连接收到 0 个音频帧和 0 个音频控制消息。
- 构建与设备：13 个测试套件共 39 项全部通过，0 失败、0 错误、0 跳过；Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`，最终投屏服务健康检查返回 200，应用进程日志无 `FATAL EXCEPTION`、`VerifyError` 或活动投屏连接读取超时。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，101920106 字节。
- SHA-256：`6563cd087a20977ea485647012c1e0dc9ed0fd44f7c01b200915fbdd667ece11`。

## 2026-07-19 Android 与网页 LED 自适应多行及速度上限

- 滚动速度：原生 LED、Android 投屏设置、网页投屏端和网页遥控端统一使用 `24..1600 dp/s`；默认值仍为 `72 dp/s`，已有用户配置不变。
- 自适应多行：原生与网页 LED 设置新增“自适应多行”，默认关闭以保持现有单行滚动行为；开启后按便捷字幕大字幕逻辑静态换行并自动寻找最大可用字号，不再播放跑马灯。
- 渲染一致性：自适应模式保留手动换行、正常字形/点阵字形、自定义字体与字重、字幕色、背景色、发光、点阵密度/形状、显示高度和文本对齐；输入框未发送内容的预览仍使用原有大字幕预览逻辑。
- 交互隔离：自适应模式隐藏速度、方向和循环间距，仅保留文本对齐；关闭后立即恢复原有拖动、惯性、方向、间距及快速左滑快捷文本行为。设置 JSON 升级至版本 4，旧配置缺少新字段时按关闭处理。
- 网页验证：投屏端与遥控端速度滑条均确认 `max=1600`；1280x720 桌面和 390x844 移动端无横向溢出，自适应点阵文本可静态多行完整显示，关闭后恢复滚动设置，相关 JavaScript 均通过 `node --check`。
- 构建验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，13 个测试套件共 41 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。
- 真机验证：Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`；确认自适应多行保留黄色点阵与发光效果，关闭后原生速度滑条末端显示 `1600 dp/s`。测试结束后设备速度恢复为原值 `217 dp/s`，Logcat 无 `FATAL EXCEPTION`、`AndroidRuntime` 或 `VerifyError`。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，101926104 字节。
- SHA-256：`9a62db858bdd1a0af68e66356d27071882abf438348461498e45ac4a18c34607`。

## 2026-07-19 Android 便捷字幕首次引导与快捷名片备注

- 首次入口：新用户第一次进入便捷字幕时先选择常规或紧凑布局；常规预览将分组 Tab 放在快捷文本底部并显示示例图标，紧凑预览完整显示右侧上下切组控件。旧用户沿用已完成的总引导状态，不会在升级后突然弹出；未完成流程的新用户返回页面时仍可继续选择，只有跳过或完成才持久化结束状态。
- 引导流程：使用 3 步非操作式遮罩，覆盖快捷文本与分组、大字幕与快捷操作、输入与语音识别；返回键只延后本次显示，不修改完成状态。快捷文本长按候选、紧凑模式横竖屏切组、顶栏与底栏按钮均纳入说明。
- 快捷文本步骤：在快捷文本区域内侧上方增加“长按打开候选列表”气泡，箭头朝下指向候选条目；引导窗原有点按、长按和分组管理说明保持不变。
- 大字幕步骤：高亮完整大字幕区域，在区域内部下方显示“大字幕（点按进入预览，长按复制文本）”气泡，同时保留步骤说明文案；补齐菜单、音频设置菜单、全屏、粗体、对齐、倒置、LED、清屏、历史和调整字体大小等气泡，并将步骤卡下移避开标签。
- 底栏步骤：左移、右移、朗读开关、折叠、重读使用同一行气泡，右移略向左避让；发送和麦克风使用侧向气泡，语音识别固定两行显示“语音识别（需要安装资源包）”，详细操作继续由资源安装后的专门引导负责。
- 气泡渲染：正文和三角箭头合并为同一个自定义 `Surface` 轮廓，由一次 elevation 着色和投影解决暗色主题下箭头与海拔表面色不一致的问题；气泡保持无描边、小圆角和明确箭头指向。
- 字节码稳定性：大字幕与字体大小锚点下沉至独立小组件，避免继续扩大历史遗留的超大 `QuickSubtitleScreen`；真机曾捕获到的 ART `VerifyError` 已消除，正式冷启动进程持续运行且日志无 `VerifyError` 或 `FATAL EXCEPTION`。
- 快捷名片：备注按二维码区域可用高度动态计算多行上限，短备注完整换行显示，仅在超过二维码区域高度后省略；无标题和极小高度场景均有单元测试保护。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，15 个测试套件共 46 项，0 失败、0 错误、0 跳过，`git diff --check` 通过。真机逐页确认模式预览、三步聚光、统一海拔气泡、底栏同排标签和两行语音识别标签；正式包保留数据覆盖安装后冷启动 `TotalTime=2056 ms`，旧用户未重复出现首次引导。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，101985266 字节。
- SHA-256：`5a775c959e6db03bacde18aa5774bbc71add9d81d54d5f6fdbebf1ed3402d5d2`。

## 2026-07-19 Android 单笔手势快捷文本、网页 Logo 与锁屏悬浮窗

- 范围：仅修改 Android 原生端，Flutter 端未改动；“悬浮窗与热键”新增“手势触发快捷文本”入口卡片、总开关和独立二级设置页。
- 手势模板：内置 M、W、Z、闪电、沙漏和折钩 6 个有意图的连续单笔模板；每项都可单独启用并编辑独立文本，条目左侧显示轨迹图标，编辑弹窗通过移动笔尖逐段演示完整画法。
- 现成识别能力：复用 Android SDK 自带 `GestureStore`，使用方向敏感和笔画顺序敏感模式，不引入新的第三方运行时依赖；外层增加最小笔画点数、时长、横纵跨度、路径长度和曲折度防护，降低点击、长按和普通横滑误触发概率。
- 大字幕交互：手势只在便捷字幕大字幕区域采样，命中后沿用快捷文本上屏与朗读流程；未形成有效手势时继续支持点按预览和长按复制。总开关开启后，便捷字幕主页面禁用右滑抽屉手势，顶栏菜单按钮仍可正常打开抽屉。
- 数据兼容：手势总开关、分项开关和文本以版本化 JSON 写入 UserPrefs；读取时按稳定模板 ID 合并默认项、忽略未知项并限制文本长度，新用户默认关闭且不会自动触发任何文本。
- 网页遥控器：抽屉顶部文字品牌替换为 KIGTTS 横版 SVG Logo；服务增加独立静态资源路由和 SVG MIME。Playwright 在 1280x720 亮色与 390x844 暗色下确认 Logo 加载、暗色反转、抽屉开合和零横向溢出，控制台无错误或资源失败。
- 锁屏悬浮窗：悬浮输入和 FAB 引导子窗口补齐锁屏窗口标志，服务在熄屏、亮屏和设置变化时统一重建窗口；由于 HyperOS 会把 `TYPE_APPLICATION_OVERLAY` 压在系统锁屏层下，启用锁屏显示时使用私有透明承载 Activity 提升窗口层级。承载页可聚焦以避免输入超时，悬浮窗保持完整交互，点空白区域退出承载页并恢复系统锁屏，解锁、息屏或关闭设置时自动清理。
- 真机手势：设备 `cfc8ef16` 上连续注入 M 形单笔轨迹后，大字幕由原文本切换为配置的“你好”；普通水平右滑未触发文本且未打开抽屉。设置列表、分项开关、动画编辑弹窗和 6 个轨迹预览均已逐项检查。
- 真机锁屏：HyperOS 设备点亮锁屏后 FAB 实际可见，点击可展开完整悬浮窗，点空白可先收起再返回原始锁屏；连续熄屏/亮屏并静置 8 秒后无 ANR、`FATAL EXCEPTION` 或承载页启动失败。
- 构建验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，17 个测试套件共 52 项，0 失败、0 错误、0 跳过；新增 6 项覆盖设置 JSON 往返、未知模板归一化、全部默认单笔轨迹、有效 M 手势和普通横滑拒绝。最终真机冷启动 `LaunchState=COLD`、`TotalTime=1662 ms`，进程正常驻留且日志无 `VerifyError`、ANR 或致命异常；`git diff --check` 通过。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，102028242 字节。
- SHA-256：`4b1f7e00f9d7a261f5e5266477e667b4464a223d8bd45b2578bff97d372f4afb`。

## 2026-07-22 Android 文本菜单、自定义字体测量与悬浮名片

- 范围：仅修改 Android 原生端，Flutter 端未改动。
- 文本上下文菜单：将自定义剪切、复制、粘贴和全选菜单从 `UiControls.kt` 拆为独立宿主；抑制文本框长按后同一轮布局产生的一次瞬时隐藏，解决快捷名片编辑页菜单不出现或一闪而过的问题。
- 弹窗菜单：滚动弹窗、内置取色/文件选择弹窗、快捷文本编辑弹窗及全部 Material `AlertDialog` 不再注入应用自绘菜单，统一使用弹窗 Android 窗口自己的系统长按文本菜单，由系统处理键盘上浮、选区锚点和窗口避让。主页面与悬浮窗仍保留原有菜单设置和自绘样式。
- 系统文本菜单：在“设置 → 系统 → 显示与主题”新增“使用系统长按文本菜单”，默认关闭以保持主页面现有行为；弹窗始终使用系统菜单，开启该选项后主页面、悬浮字幕输入栏和悬浮窗应用搜索框也不再拦截系统 ActionMode。设置通过 DataStore 持久化，并在切换时刷新悬浮窗输入控件。
- 便捷字幕自适应：测量与实际 `Text` 统一使用同一份主题文字样式，完整保留自定义字体族、字重、字距、`PlatformTextStyle`、字体内边距和行高裁剪参数，修复部分三星设备使用自定义字体时字号与换行估算偏大、底部吞字的根因。
- 悬浮窗大字幕：自适应测量直接复制实际 `TextView.paint`，并统一 `includeFontPadding`、简单断行和禁用断词参数，减少 OEM 字体度量差异；自定义字体和稳定字高继续由同一 `OverlayFontApplier` 应用。
- 悬浮快捷名片：备注改为按二维码实际高度计算多行上限，文字区域与二维码区域使用同一尺寸来源；短备注完整换行，超过可用高度后才省略，无链接时使用卡片剩余高度。
- 测试与设备：`:app:testDebugUnitTest` 和 `:app:assembleDebug` 通过，19 个测试套件共 60 项，0 失败、0 错误、0 跳过；新增菜单瞬时隐藏、宿主位移、输入框独立滚动后保持选区锚点、普通页面间距、系统菜单默认值和主题文字度量参数测试。Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`，此前冷启动 `TotalTime=2066 ms`，应用进程日志无 `FATAL EXCEPTION`、`VerifyError` 或 ANR。当前设备不是三星，三星字体渲染结果仍需在原复现设备确认。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，104166607 字节。
- SHA-256：`dcdad335be51ab3ddc2287b0e596f2e8821e5fdb90fe617c6499bee19c1baed6`。

## 2026-07-22 Android 文件管理器全部文件访问

- 权限：Android 11 及以上声明 `MANAGE_EXTERNAL_STORAGE`，内置文件管理器新增“全部文件”入口；用户主动点击后先展示用途与隐私说明，再跳转应用专属的系统“所有文件访问权限”页面，不在启动或后台主动申请。
- 降级：Android 11+ 允许先进入内置文件管理器，再自行选择全部文件访问、媒体读取权限、持久化 SAF 目录授权或系统文件选择器；权限未开启或被撤销时其余路径继续可用。Android 8–10 仍使用原有 `READ_EXTERNAL_STORAGE` 门禁，不受新权限判断影响。
- 刷新：从系统设置返回以及应用恢复前台时重新读取 `Environment.isExternalStorageManager()`；授权成功后跳过重复媒体读取申请，并重新加载文件列表。
- 存储卷：获得权限后通过 `StorageManager.storageVolumes` 补充内部存储、SD 卡和外接存储根目录；仍遵循 Android 对其他应用私有目录和部分 `Android/data` 路径的系统限制。
- 验证：合并后的 Debug Manifest 已确认包含 `android.permission.MANAGE_EXTERNAL_STORAGE`；`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，19 个测试套件共 60 项，0 失败、0 错误、0 跳过。Debug APK 已覆盖安装到设备 `cfc8ef16`，冷启动 `TotalTime=1973 ms`；同一应用专属 Intent 可正常打开 MIUI“所有文件访问权限”页面。使用 `appops` 验证授权状态可切换后已恢复为 `default`，未替用户保留强制授权。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，104171322 字节。
- SHA-256：`5bf5453a5bcccbc5d87e5a8a4f31565dea15c0d1a0e3bb07bfdd84fbc2686601`。

## 2026-07-22 Android 快捷名片包与软件配置备份

- 范围：仅修改 Android 原生端，Flutter 端未改动；长按名片进入的“管理名片”页顶栏新增导入与分享导出按钮，快捷名片主页仍只保留新建与扫码入口。
- 快捷名片包：新增自包含 `.kigcard` ZIP 格式，清单保存名片类型、标题、备注、主题色、链接及横竖图片；导入和导出都会先弹出名片勾选列表，导入同名名片时作为“导入”副本追加，不合并或覆盖现有名片。
- 类型持久化：修复快捷名片原持久化层强制写入 `text` 的问题，图片、二维码和文本类型现在按实际 wire value 读写，导入后重启不会丢失类型。
- 专有文件图标：内置文件管理器为 `.kigvpk`、`.kigspk`、`.kigcard`、`.kigtpk` 和 `.kigconfig` 分别显示语音、音效、名片、字幕和设置还原图标，不再全部显示为普通压缩包。
- 配置备份：在“设置 -> 系统”最底部新增“备份与还原”卡片；基础设置和快捷名片信息始终写入 `.kigconfig`，用户可按需额外包含快捷文本预设、快捷名片图片、当前字体文件、音效板预设与音频、已安装的本地语音包。
- 还原语义：备份中的设置和同名资源覆盖当前内容，备份未包含的本机数据保留；偏好按原始 Boolean/Int/Long/Float/Double/String/StringSet 类型合并写回，并在还原后刷新快捷文本、音效板、名片、字体与语音包列表。
- 包安全：两个格式都校验类型和版本、拒绝目录越界路径、限制清单/图片/备份解压大小；配置还原先解压到缓存暂存区，只允许资源写入应用的名片、音效、语音包和字体目录。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，19 个测试套件共 61 项，0 失败、0 错误、0 跳过；真机 Instrumentation 另有 2 项通过，覆盖 `.kigcard` 类型/同名/图片往返和最小 `.kigconfig` 偏好往返。Debug APK 已安装到设备 `cfc8ef16`，最终冷启动 `TotalTime=1787 ms`，日志无 `FATAL EXCEPTION`、`VerifyError` 或 ANR。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，104223999 字节。
- SHA-256：`0cd52b23f5b6cb5ab1c741545e6565fb1a487d28276e6230e118c77282d4a805`。

## 2026-07-22 Android 高清名片图片、画板自定义颜色与 LED 间距

- 范围：仅修改 Android 原生端及其 LAN 投屏网页资源，Flutter 端未改动。
- 快捷名片图片：裁剪输出由固定 `1080x1920` 提升为最高 `2160x3840`，使用 `RESIZE_INSIDE` 只缩小超限图片、不强制放大低分辨率原图；横竖名片分别保持 16:9 和 9:16。
- 高清渲染：名片图片缓存长边由 1600 提升到 2880 像素，覆盖手机和平板常见 QHD 显示；采样先保留目标分辨率以上的位图再高质量缩放，避免 4K 图片因二次幂采样直接退回 1080p。缓存键加入文件修改时间和目标尺寸，防止不同质量请求复用旧低清缓存。
- 画板取色：原有 7 个主题色后新增“自定义颜色”色轮，点击复用现有 HSL/HEX 取色器；色轮由红、橙、黄、绿、青、蓝、紫 7 个 MD2 实色连续组成细空心环，中间显示当前最终取色。
- 工具栏尺寸：自定义色轮保持与原色点相同的 22dp 大小，工具栏宽高仍按原有 7 色计算，第八项通过原有横向或纵向滚动访问，不扩大工具栏。
- LED 循环间距：Android LED、Android 投屏设置、网页投屏端、网页遥控端、设置归一化和 LAN 服务消息解析统一使用 `24..1600 dp`，不再被服务端旧的 240dp 上限截断。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，21 个测试套件共 66 项，0 失败、0 错误、0 跳过；新增 4K 名片采样、QHD 质量阈值、超大图采样、七色 MD2 色轮和 LED 最大间距测试。`git diff --check` 与 LAN 网页 JavaScript 语法检查通过。
- 真机：Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`，冷启动 `TotalTime=1815 ms`；启动后日志无 `FATAL EXCEPTION`、`VerifyError`、`OutOfMemoryError` 或 `AndroidRuntime` 致命异常。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，104227093 字节。
- SHA-256：`315d7882ca574668f63f08970a4bc737c9e4275edef6012693b129e0451662c2`。

## 2026-07-22 Android 画板可编辑亮暗调色板

- 默认顺序：画板工具栏将自定义七色环移到首位，后接 6 组默认候选色；移除原末尾紫色/浅紫色组，总项数仍为 7，工具栏宽高保持不变。
- 亮暗配色：默认色板和用户色板的每个条目都同时保存亮色主题颜色与暗色主题颜色，画板按当前主题自动取对应颜色。旧版本没有色板配置时自动使用新的 6 组默认色板。
- 编辑入口：画板自定义颜色取色弹窗底部新增“编辑调色板”入口，进入独立二级页面；页面顶栏显示返回和确认按钮，列表样式与资源管理页面一致。
- 编辑能力：每个条目提供亮色和暗色两个圆形颜色按钮，分别打开 HSL/HEX 取色器；支持新增、删除以及通过右侧拖动手柄上下排序，拖动时提供海拔与底色反馈。
- 编辑页样式：条目直接沿用音效板编辑条目的 72dp 卡片规格、圆角、默认海拔、拖动抬升和右侧操作区；两个 40dp 色块使用亮色/暗色图标，并显示对应色名与两组 HEX。新增入口从悬浮按钮移至标题栏，与确认按钮并列；列表按字体管理页限宽，避免平板上过度拉伸。
- 画板默认色：用户尚未手动选色时，亮色主题默认使用色板第一项的亮色，暗色主题默认使用第一项的暗色；色板为空时回退到当前实际主题主色。主题或色板刷新会同步尚未手动修改的默认值，但不会覆盖用户本次运行中已经选择的画笔颜色。
- 中文颜色名称：使用本机 zh-CN Windows 的 `ColorHelper.ToDisplayName` 对 65×65×65、共 274625 个 RGB 点采样，得到 39 种系统中文色名；安卓端内置游程压缩表并以八邻点权重匹配任意颜色，不依赖网络或 Windows。10000 个固定随机样本与 Windows API 的名称一致率为 96.87%，12 个默认候选色另保留直接采样结果。
- 色名显示：调色板条目的“配色 X”替换为“亮色色名 · 暗色色名”，例如“深青色 · 浅绿色”；Windows 返回的“深青”“浅绿”“冰蓝”等名称会统一补齐“色”后缀，下一行继续显示两组 HEX。
- 保存防护：编辑期间使用独立草稿，不会实时破坏当前色板；点击顶栏确认会保存并返回，顶栏或系统返回遇到未保存修改时提供“保存 / 不保存 / 取消”选择。旋转屏幕后草稿仍保留。
- 持久化：新增版本化 `drawing_palette` JSON，并通过 DataStore 写入；颜色统一转为不透明 ARGB、异常或重复 ID 会归一化，最多允许 24 个条目。该偏好会随现有 `.kigconfig` 配置备份与还原。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，23 个测试套件共 74 项，0 失败、0 错误、0 跳过；新增默认迁移、亮暗颜色对、顺序、重复 ID、透明色、新增 ID、亮暗首项默认色、空色板回退、Windows 默认色名、透明度忽略和全域色名后缀测试。真实 Android instrumentation 另有 1 项通过，确认色板 JSON 顺序和亮暗颜色往返一致；测试包已单独卸载，未清除主应用数据。
- 真机：完成编辑页样式、默认颜色和中文色名重构后，Debug APK 已保留数据覆盖安装到设备 `cfc8ef16`，冷启动 `TotalTime=1440 ms`；应用进程日志无 `FATAL EXCEPTION`、`AndroidRuntime`、`VerifyError`、`OutOfMemoryError` 或 `IllegalStateException`。
- APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，104303998 字节。
- SHA-256：`09598c282b1987a606244a7e01ec73b9aba6b563cd8b39b45ced486a9e29bb52`。

## 2026-07-22 Android 主题色备注、官网入口与 0.1.2 版本

- 主题色备注：设置“显示与主题”中的应用主题色，以及快捷名片编辑页的“当前主题色”，统一显示为“`#RRGGBB · 中文颜色名`”，例如“`#038387 · 深青色`”。
- 格式复用：色号统一使用不含透明度的大写六位 HEX，颜色名复用内置 Windows 中文颜色名称采样表，两处界面不会再分别维护文案。
- 应用版本：Debug 与 Release 共用版本名 `0.1.2`，`versionCode` 由 2 递增到 3。
- 二维码外链页：顶部信息图标改为 64dp 居中容器内的 52dp Material Symbol，修复原先只扩大布局框、字形仍为 24dp 导致的图标偏小和基线偏位；同类网页错误页同步统一。
- 关于页官网：KIGTTS Logo 增加可点击波纹，点击后优先以 Custom Tab 打开 `https://kigtts.lhtstudio.com`，不可用或启动失败时回退系统外部浏览器。
- 验证：`:app:testDebugUnitTest`、`:app:assembleDebug` 与 `:app:assembleRelease` 通过，23 个测试套件共 75 项，0 失败、0 错误、0 跳过；Debug 与 Release 包元数据均确认是 `0.1.2 (3)`，Release APK 通过 v2 签名校验。
- Debug APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，102531778 字节，SHA-256 `c48111fc75b029d7e9db315459cc72470c4c9236fd7248ff065bb2285c2b4182`。
- Release APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\release\app-release.apk`，95714243 字节，SHA-256 `83436ad6898af1fc9943fff05d2ebd7eda70b865e52149af95f1c6a5ae2d431d`。
- 关于页版本文案：界面只显示版本名“版本 0.1.2”，不再显示括号内的 `versionCode`；内部版本代码仍保留用于 Android 升级判断。

## 2026-07-22 Android 设置文案用户化

- 表达原则：设置标题改为描述用户目的，说明文字直接描述实际效果；`FAB`、`TTS`、`VAD`、`Tab`、`sp`、音频焦点和模型拆分等实现术语不再直接面向普通用户展示。
- 识别设置：将自动上屏、按住说话、快捷文本联动、播放时暂停识别、语音降噪、智能断句、触发前声音、数字读法和指定说话人等条目改为行为导向文案；技术模型名称仅在具体降噪方案括号中保留。
- 音频设置：使用“当前语音合成引擎”“打开系统语音合成设置”“关闭语音朗读”“朗读与音效音量”等一致术语；回声、降噪和音频设备选择改为用户可判断的标题与结果说明，移除设置页中的原始 AEC3 诊断文本。
- 系统设置：字体缩放、主题色、触感、横屏侧栏、分组名称、字幕字号、应用内文件选择和快捷操作补全均改为面向结果的文案；已经始终启用的内置 WebView 设置卡从界面移除，网页打开及白名单逻辑保持不变。
- 悬浮窗与音效板：移除 `FAB` 和 `TTS` 等缩写，统一使用“悬浮按钮”“语音朗读”“两次按键最长间隔”等名称；锁屏显示、自动贴边和无障碍热键说明压缩为必要的行为与权限信息。
- 验证：版本保持 `0.1.2 (3)` 不变；`:app:testDebugUnitTest`、`:app:assembleDebug` 与 `:app:assembleRelease` 通过，23 个测试套件共 75 项，0 失败、0 错误、0 跳过。
- 真机：Release APK 已保留数据覆盖安装到设备 `cfc8ef16`，冷启动 `TotalTime=766 ms`；应用启动日志无 `AndroidRuntime` 致命异常，设备包仍为非调试版 `0.1.2 (3)`。
- Debug APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\debug\app-debug.apk`，102530450 字节，SHA-256 `085e47b372cf7fdc3bc6bc80e9516afa2ee116996d968b4a01dde8405494e36b`。
- Release APK：`D:\KGTTS_GradleOut\codex-build\app\outputs\apk\release\app-release.apk`，95714243 字节，SHA-256 `47f63cf620da69da3aaa83d54ea7c2c66c788a60024111d214e4f46c84eb2962`。

## 2026-07-22 Android 页面文案用户化与 APP 0.1.2 发布

- 页面与编辑器：快捷文本编辑页统一使用“紧凑快捷文本布局”和明确的目标分组提示；音效板将“关键词 / 唤醒词 / 音效条目”统一为“触发词 / 音效”，并改写新增、编辑、删除、移动和音频剪辑文案。
- 名片与网页：扫码相机、图片裁剪、自动保存、链接打开和分享失败提示补充可执行的处理建议；外部链接页改为可信网址提醒，网页错误页只显示易懂原因，错误代码与 HTTP 状态折叠到“技术详情”。
- 资源与显示：模型管理页不再展示 ASR 和语音包内部路径；系统 TTS / ASR 的用户侧状态统一改为“系统语音合成 / 语音识别”；LED 与投屏设置移除 `dp`、`dp/s` 等实现单位，并优化自动换行、字幕间距、手势和低对比度提示。
- 文件与备份：文件选择器使用“允许访问文件 / 添加可访问目录 / 系统文件选择器”等行为文案；“备份和还原软件配置”统一改为“备份和恢复应用数据”，精简恢复覆盖规则说明。
- 错误信息：导入、导出、下载、分享、字体、画板和语音资源失败时不再把异常原文直接显示给用户；完整异常继续写入 `AppLogger`，界面只给出检查文件、网络、权限、存储空间或重试等下一步操作。
- 下载入口：根目录 README 的 GitHub 发行页和 APK 直链更新到 `APP0.1.2`，移除下载卡片中仍指向 `0.1.1` 的旧 Gitee APK 直链。
- 版本：Debug 与 Release 继续使用 `0.1.2 (3)`，本次未修改 `versionName` 或 `versionCode`。
- 验证：`:app:testDebugUnitTest` 通过，23 个测试套件共 75 项，0 失败、0 错误、0 跳过；`:app:assembleRelease` 完整通过，包含 Release Kotlin/DEX、arm64-v8a 原生库、签名和 `lintVitalRelease`。
- 真机：Release APK 已使用 `adb install -r` 保留数据覆盖安装到设备 `cfc8ef16`；冷启动 `TotalTime=386 ms`，应用进程存活，当前进程日志无 `FATAL EXCEPTION`、`AndroidRuntime`、`VerifyError`、`OutOfMemoryError` 或 ANR。
- Release APK：`D:\KGTTS_GradleOut\build\app\outputs\apk\release\app-release.apk`，95714243 字节，SHA-256 `6c25efa4fd9e86e1e14fd6b684350fc3f6fc2df826201e6c89de7a662b27e7d0`；APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。

## 2026-07-25 Android 顶栏、悬浮窗定位与自定义锁屏

- 主题色顶栏：修正“使用主题色顶栏”开关的显示条件；开启时使用主题主色和对应前景色，关闭时使用普通卡片色，不再反向显示。
- 首次定位：悬浮窗服务改为先异步读取设置、字体、快捷入口和横竖屏位置，再创建窗口并恢复坐标；启动指令在初始化完成前排队，避免 FAB 先显示默认位置再瞬移。
- 坐标防护：新增位置恢复完成标记，窗口字体重建或刷新刚读取持久化布局时不再用尚未恢复的默认坐标覆盖存储；锁屏临时布局和横竖屏切换也不会写回普通 FAB 锚点。
- 自定义锁屏：透明 `LockScreenOverlayHostActivity` 只负责时间、中文日期、滑动解锁和窗口宿主；锁屏内容改为绑定第二个完整的 `LockScreenFloatingOverlayService` 实例，并通过宿主 `windowToken` 创建 `TYPE_APPLICATION_PANEL` 子窗口，启动器、快捷字幕、快捷名片、音效板、输入框和操作说明直接复用普通悬浮窗代码。
- 实例隔离：普通 `FloatingOverlayService` 始终保持正常悬浮窗实例，不再被提升到锁屏；锁屏宿主显示时仅使用 `setHideOverlayWindows(true)` 隐藏普通系统悬浮层。两套实例分别维护界面状态，锁屏操作不修改普通 FAB 的横竖屏锚点和展开状态。
- 锁屏状态差分：基础悬浮窗新增“是否支持折叠 FAB”能力钩子，锁屏实例固定关闭。锁屏启动器不显示折叠返回键，背景点按不会折叠，迷你面板关闭时返回完整启动器；窗口重建、显隐同步和看门狗修复都会强制隐藏锁屏 FAB，并在启动器和迷你面板都不可见时恢复完整启动器。
- 锁屏定位差分：手机竖屏的完整面板居中并下移到时间日期下方，同时为底部滑动解锁提示预留空间；手机横屏和平板固定靠右，左侧留给时间日期与解锁手势。锁屏子窗口使用实际内容尺寸，面板外的透明区域继续由宿主接收滑动解锁。
- 稳定性：移除旧 `LockScreenOverlayPanelView` 近似实现，消除其初始化顺序导致对空列表调用 `take()` 的崩溃路径；执行 `clean` 全量构建后检查 APK，旧类匹配数为 0，新共享实例服务存在。文本输入窗与操作说明窗共用宿主窗口类型解析，`OverlayTextInputWindow.kt` 保持在 498 行。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，24 个测试套件共 79 项，0 失败、0 错误、0 跳过；布局测试覆盖手机竖屏居中、手机横屏靠右、平板靠右和近满宽边距钳制。此前实机已验证共享实例可在锁屏内切换快捷字幕、点击快捷文本并打开键盘输入，焦点保持在锁屏宿主且无 `BadToken`；本轮连接设备未配置可保持的安全锁屏，最终差分版本仍需在安全锁设备上复测熄屏/亮屏展示。
- 设备：最终 Debug APK 已通过 `adb install -r` 保留数据覆盖安装到设备 `cfc8ef16`。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，100271262 字节，SHA-256 `c71573aedd7c4a4668a808023101ce4476e8a88880d774a08018109df450a3f2`。

## 2026-07-26 Android 自定义锁屏布局、交互与后台权限引导

- 横屏避让：锁屏宿主与锁屏悬浮窗共用同一套宽度和横向定位策略，手机横屏和平板按悬浮窗实际左边界计算时间栏宽度；时间、日期和滑动解锁提示不会再伸入右侧悬浮窗区域。
- 竖屏定位：锁屏悬浮窗完成布局后使用真实可变高度再次计算位置。完整启动器优先从 `220dp` 开始并为底部解锁提示预留 `64dp`；迷你快捷字幕、迷你快捷名片和迷你音效板在时钟淡出后按实际高度居中，不再额外避让时钟区域。
- 尺寸同步：透明宿主按根视图真实宽高判断横竖屏，并在宿主尺寸、系统栏边距或悬浮窗内容高度变化后重新定位；锁屏 Activity 使用用户当前方向，修复部分厂商强制横屏时仍按竖屏任务尺寸创建的问题。
- 时钟动效：仅手机竖屏进入快捷字幕、迷你快捷名片、音效板等悬浮窗子页面时，时间和日期以 `180ms` 淡出；返回完整悬浮窗启动器时以 `220ms` 淡入。手机横屏和平板始终保留左侧时间信息。
- 解锁路由：锁屏悬浮窗中所有需要打开主页面、设置、扫码、外部应用、分享页或应用快捷操作的入口，统一先请求系统解锁，只有解锁成功后才执行原操作；普通悬浮窗行为不变。
- 刘海区域：透明锁屏宿主允许内容延伸到屏幕切口短边，状态栏、导航栏和刘海/挖孔区域继续显示原锁屏壁纸，不再出现独立黑条。
- 后台触发：亮屏广播立即尝试显示锁屏宿主，并在 `500ms` 后按当前锁屏状态重试，覆盖应用退到后台后系统锁屏建立较晚的时序；真机退到桌面后熄屏再亮屏可正常生成宿主。
- 窗口回收：锁屏宿主解绑时先同步移除所有附着的子窗口，不再等待绑定服务异步销毁；修复反复熄屏/亮屏后的 `WindowLeaked` 日志和潜在窗口资源残留。普通悬浮窗仍使用原有移除路径。
- 小米权限：小米设备首次开启锁屏显示时弹出“后台弹出界面”说明；旧用户已开启但未看过说明时补弹一次。设置卡保留“后台弹出权限”入口，优先打开小米应用权限页，无法匹配组件时回退系统应用详情页。
- 权限边界：Android 没有名为“后台弹出界面”的通用运行时权限。vivo/iQOO 也提供同名的厂商权限；OPPO/ColorOS 的相近控制通常显示为“允许完全后台行为”或“允许后台活动”，不是 Android 通用权限，且没有稳定公开的直达设置 API。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，24 个测试套件共 83 项，0 失败、0 错误、0 跳过；新增迷你页面不预留时钟高度的布局测试。实机已验证小米权限引导和应用权限页、退到桌面后熄屏/亮屏后台启动、横屏时间区避让、竖屏迷你快捷字幕时钟淡出与居中布局，刘海区域均正常显示锁屏壁纸。最终包连续完成两轮熄屏/亮屏，日志无 `WindowLeaked`、`BadTokenException`、致命异常或 ANR。
- 设备：最终 Debug APK 已通过 `adb install -r` 保留数据覆盖安装到设备 `cfc8ef16`，版本保持 `0.1.2 (3)`。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104581200 字节，SHA-256 `eca10ecc4dd6dd2b09ba76b14e73aeeaa02cfcda5a5d2ed06f9e09aae34d32ba`。

## 2026-07-26 Android vivo 后台显示授权与顶栏默认值

- vivo 识别：后台显示授权引导新增 vivo/iQOO 厂商识别；首次开启锁屏悬浮窗时会像小米设备一样显示对应说明，设置卡固定保留“锁屏显示权限”入口。
- vivo 跳转：OriginOS 优先打开 `com.vivo.permissionmanager/.activity.SoftPermissionDetailActivity` 并传入当前包名，旧 iQOO 系统保留 `com.iqoo.secure` 兼容入口，均不可用时回退系统应用详情页。
- 引导文案：vivo/iQOO 明确提示在“设备管理”中同时开启“锁屏显示”和“后台弹出界面”；小米继续只提示“后台弹出界面”，不会混用厂商术语。
- 厂商策略：设备识别与引导文案拆为无 Android 依赖的策略文件，单元测试覆盖 Xiaomi/Redmi/POCO、vivo/iQOO、其他厂商以及 vivo 双权限文案。
- 顶栏默认值：“使用主题色顶栏”的新用户默认值改为关闭，首次启动使用普通卡片色顶栏；已有用户已经保存的开关值不受影响。
- 构建验证：`:app:testDebugUnitTest`、`:app:assembleDebug` 与 `:app:assembleRelease` 通过，26 个测试套件共 88 项，0 失败、0 错误、0 跳过；新增顶栏默认关闭回归测试，Release 的 `lintVitalRelease` 同步通过。
- vivo 真机：Debug APK 已覆盖安装到远程设备 `V2072A`（Android 13、OriginOS 属性 13.5）。应用内“锁屏显示权限”可直达 KIGTTS 的 vivo 权限详情页，首屏包含“悬浮窗”“锁屏显示”和“后台弹出界面”。
- 后台锁屏：悬浮窗与锁屏显示开启后，应用退到桌面再熄屏/亮屏，窗口焦点正常进入 `LockScreenOverlayHostActivity`，`LockScreenFloatingOverlayService` 已绑定并显示完整锁屏悬浮窗；日志无 `FATAL EXCEPTION`、`WindowLeaked` 或 `BadTokenException`。
- 无密码退出：滑动解锁现在处理 `requestDismissKeyguard()` 的成功回调并立即关闭宿主；宿主显示期间每 400ms 检查一次 Keyguard 状态，覆盖无密码设备自动解锁但未发送 `USER_PRESENT` 的厂商时序，防止锁屏层异常驻留。vivo 实机分别通过自绘横向滑动和外部解除 Keyguard 复测，两种路径都在 1 秒内回到桌面且不再保留宿主 Activity。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104584580 字节，SHA-256 `c73e16cac6cd10f4cea5da7f3dd9939fcb54dc50c0744d66ce06b6ca52eba1fc`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95732483 字节，SHA-256 `0246c32810cb5994f3090bd4b136af00579ef41049ecbca85b52321d704fd419`；包元数据为 `0.1.2 (3)`，APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。

## 2026-07-26 Android 锁屏外观与时钟字体资源

- 设置结构：“悬浮窗与热键”将锁屏显示拆为独立卡片和“锁屏样式设置”二级页面；锁屏主开关、厂商后台显示权限入口、壁纸、日期和字体设置集中管理，普通悬浮窗状态保持独立。
- 自定义壁纸：支持应用内图库或系统文件选择、自由裁剪和清除；选中图片复制到应用私有目录，显示时按屏幕尺寸采样，避免直接解码高分辨率原图。
- 背景效果：自定义壁纸可调 `0..30` 模糊、遮罩颜色与 `0..100%` 透明度；遮罩支持“渐变”和“全填充”。渐变模式在竖屏覆盖顶部与底部，在横屏仅从左侧向右淡出，优先保证左侧时钟和解锁提示可读。
- 自动对比度：横屏只采样壁纸左侧内容区，竖屏采样顶部和底部；遮罩合成后的背景过亮时，时间、日期和解锁提示自动切换为深色并移除文字阴影，较暗背景继续使用浅色和阴影。
- 时间与日期：支持时间日期居左、日期后显示农历、锁屏文字使用系统字体；“使用单独的时钟字体”只影响数字时间，不改变日期、农历、解锁提示或锁屏内嵌悬浮窗。
- 时钟字体选择：列表包含系统字体、所有已导入或下载的中文字体以及专用时钟字体；预览内容使用当前时间，备注只显示字体名称，可变字重和多档字重继续使用现有字重设置控件。
- 字体下载：时钟字体下载弹窗新增“时钟字体 / 中文字体”图标 Tab。时钟字体从独立资源库下载；中文字体继续读取通用字体库并安装到同一字体目录，因此仍可在“设置 -> 系统 -> 显示与主题 -> 字体”中选择。
- 资源仓库：新增 [ModelScope 时钟字体库](https://www.modelscope.cn/models/LHTSTUDIO/KIGTTS_CLOCK_FONTS_Resource) 和 [Hugging Face 时钟字体库](https://huggingface.co/LHT02/KIGTTS_CLOCK_FONTS_Resource)，首批包含 Orbitron、Oxanium、Doto、Share Tech Mono、Bebas Neue、Audiowide 和 Rajdhani，并为每个字体保存 OFL 许可证及 SHA-256 清单。
- 调色板：共享 HSL/HEX 取色弹窗的候选颜色补充黑色与白色，锁屏遮罩默认使用黑色。
- 代码结构：时钟字体页、锁屏壁纸设置卡和锁屏外观控制器均拆为独立文件；本次相关 Kotlin 文件保持在 500 行以内，版本继续为 `0.1.2 (3)`。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 95 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。构建仅保留现有图片裁剪库 `CropImageContract` 的弃用警告。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104651307 字节，SHA-256 `bd9e62392c7a092a3ffd8010f62f278609ad5456355741d16a9c88ff2e98385c`。

## 2026-07-26 Android 字体扩充与自定义锁屏首帧修复

- 字体入口：时钟字体选择页右上角补齐与普通字体页一致的“导入字体”和“下载字体”图标入口，页面内原有“下载字体”按钮保留；通用字体页默认隐藏专用时钟字体。
- 下载筛选：通用字体下载弹窗新增“显示已安装的时钟字体”开关，默认关闭，并明确提示时钟字体不包含中文、中文仍使用系统字体；下载说明改为“下载完成后即可在字体列表中使用，相关授权信息会一并保留”。
- 中文字体：通用资源库新增缝合像素字体 12px 简体、得意黑、霞鹜文楷 GB 屏幕阅读版、马善政毛笔楷体、站酷小薇 LOGO 体、站酷庆科黄油体、龙藏体、志莽行书和刘建毛草；站酷快乐体沿用已有条目。
- 时钟字体：专用资源库新增 Google Sans Flex、Google Sans Code、Roboto Flex、Manrope 和 Teko。Google Sans Flex 使用 Google Fonts 官方六轴可变文件，现有字重设置支持 `1..1000` 连续调整。
- 资源发布：通用字体清单升级为 `2026.07.26.3`，共 35 项；时钟字体清单升级为 `2026.07.26.3`，共 12 项。两份资源均同步到 ModelScope 与 Hugging Face，远端清单和抽样字体 SHA-256 已复核一致。
- 自定义锁屏：用户侧“锁屏时显示悬浮窗”统一改为“启用自定义锁屏”，内嵌控件称为“快捷操作面板”，保留 vivo 系统权限中原生的“锁屏显示”名称。宿主首帧在当前设置、字体、壁纸和模糊效果准备完成后才显示，快捷操作面板同步延后绑定，避免默认样式先出现再跳到自定义样式。
- 壁纸模糊：移除“缩小再放大”的低分辨率近似，改为保持显示尺寸的三次方框卷积高斯近似；处理继续运行在 IO 调度器，不阻塞锁屏主线程。
- 内建图库：相册选择下拉菜单增加 `360dp` 最大高度和独立滚动状态，相册较多时支持触摸拖动与鼠标滚轮，不再溢出后无法选择。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 96 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。版本保持 `0.1.2 (3)`，本轮未覆盖安装到设备。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104655830 字节，SHA-256 `1d8e11c3f5977d49ef77be2f4c9cc77655a97c8e571949646c2e1c6996108b02`。

## 2026-07-26 Android 内建文件选择默认值与字体导入

- 默认选择器：新用户默认关闭“使用应用内文件管理器”和“使用应用内图库”，文件与图片选择优先使用系统组件；已有用户已经保存的开关值保持不变。
- 字体导入：普通字体页和时钟字体页的“导入字体”都会遵循“使用应用内文件管理器”设置；启用时打开内建文件管理器并筛选 `.ttf`、`.otf`，同时保留切换到系统文件选择器的入口。
- 文件图标：内建文件管理器为 `.ttf`、`.otf` 字体文件使用独立的 `font_download` 图标，便于与普通文件和资源包区分。
- 操作布局：“添加可访问目录”从顶部第一行移到目录导航和排序控件所在的第二行，减少手机窄屏下长按钮挤压。
- 权限状态：开启“全部文件访问”后不再显示重复的已授权备注；权限尚未开启时仍显示用途说明和授权入口。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 97 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。版本保持 `0.1.2 (3)`。
- 真机：Debug APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=1646 ms`，应用进程保持运行，启动日志无 `FATAL EXCEPTION`、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104658514 字节，SHA-256 `826d8a2df4c8a54f89e18b5dc0a352bd4dfbdba8fd9321031c105451a7fe0e49`。

## 2026-07-26 Android 使用引导逐项动效与重播入口

- 高亮动效：每一步的高亮框按锚点顺序逐个缩放淡入，初始等待 `40ms`、相邻高亮间隔 `55ms`、单项过渡 `180ms`；遮罩挖孔和主题色边框同步变化，不再整组瞬间出现。
- 气泡动效：提示气泡在高亮序列后按 `45ms` 间隔逐个出现，并组合淡入、轻微缩放和上浮效果；原有箭头位置、气泡海拔和页面说明卡切换保持不变。
- 设置入口：“设置 -> 系统 -> 便捷字幕显示”新增“使用说明”，点击后直接返回便捷字幕并从叠加说明第一步开始，不再进入常规/紧凑模式选择，也不会修改当前布局模式。
- 重播状态：手动查看说明使用仅本次运行有效的请求编号，不会把已经完成首次引导的用户重新标记为新用户；中途退出后再次点击入口仍可从第一步重新开始。
- 引导文案：第三步单独增加“可以从设置中再次进入使用引导。”，明确后续入口位置。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 98 项，0 失败、0 错误、0 跳过；新增已完成用户手动重播的状态测试，`git diff --check` 通过。版本保持 `0.1.2 (3)`，Debug 包已覆盖安装到小米设备并由用户确认“使用说明”入口正常，本轮未提交。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104666561 字节，SHA-256 `7ebcba366a6ca4be29d1342c20da9ab2ec588cae8a012100d06496b0f7cec746`。

## 2026-07-26 Android 锁屏壁纸预览刷新与方向切换

- 即时刷新：锁屏壁纸覆盖写入固定私有路径后，设置页会递增独立的预览修订号并重新解码图片，解决路径未变化导致换图后仍显示旧预览的问题；清除壁纸时也会立即刷新。
- 方向预览：锁屏预览默认使用 `9:16` 竖屏比例，不再跟随当前设备方向固定为横向；右下角新增 `screen_rotation` 单图标按钮，可在 `9:16` 竖屏和 `16:9` 横屏预览之间切换。
- 显示一致性：切换方向时同步按目标尺寸重新采样壁纸，并更新遮罩渐变方向、亮暗文字判断、时间日期与解锁提示布局；该方向只作用于设置页预览，不修改设备或锁屏方向。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 98 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。版本继续使用 `0.1.2 (3)`，本轮未提交。
- 真机：Debug APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=895 ms`，应用进程保持运行，启动日志无致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104669317 字节，SHA-256 `019d5978893f8598aa90a4abbb7f84e00b9ed2f069bf671b3fffb44554c56489`。

## 2026-07-26 Android 锁屏预览还原与独立运行

- 预览内容：移除预览中“滑动解锁”旁的箭头；时间改为设备当前时间并遵循系统 12/24 小时制，日期复用实际锁屏格式器并实时响应农历开关。
- 字体还原：日期和解锁提示遵循“锁屏使用系统字体”；数字时间同时遵循该设置与“使用单独的时钟字体”，包括所选字体及字重。字体仍使用渐进加载，加载失败时与实际锁屏一致地回退系统字体。
- 布局还原：“时间和日期居左显示”会实时改变预览中的时间与日期对齐，解锁提示继续保持居中；横屏预览将时间、日期和解锁提示整列固定在左侧内容区，不再把窄列居中到画面中央。
- 预览尺寸：横竖屏预览共用最大 `420dp` 长边，并受卡片实际宽度约束；竖屏高度等于同一容器中横屏预览的宽度，手机上不再出现过高的纵向预览框。
- 独立锁屏：新增轻量 `LockScreenMonitorService`，专门监听熄屏、亮屏和解锁状态并拉起锁屏宿主；普通 `FloatingOverlayService` 不再管理锁屏宿主，关闭“启用悬浮窗”只移除普通悬浮窗，不会停止自定义锁屏。
- 控件实例：锁屏内嵌的 `LockScreenFloatingOverlayService` 取消对普通悬浮窗总开关的依赖，但继续复用完整悬浮窗控件代码；普通实例和锁屏实例仍保持窗口状态、位置与生命周期隔离。
- 权限边界：自定义锁屏由 `showWhenLocked` Activity 和附着到宿主 `windowToken` 的 `TYPE_APPLICATION_PANEL` 构成，不需要“显示在其他应用上层”的悬浮窗权限；该权限只控制普通悬浮窗。小米与 vivo/iQOO 仍需在厂商权限页确认“锁屏显示”和“后台弹出界面”。
- 系统应用入口：点击锁屏时间会先请求系统解锁，再打开系统时钟；点击日期会按相同流程打开系统默认日历。时钟先从系统闹钟处理器动态识别真实应用包，再兼容小米/AOSP、Google、三星、OPPO、一加、vivo、华为和荣耀的常见时钟包，最后回退通用闹钟 Intent；日历以系统日历分类为首选、当前时间 URI 为回退。无可用应用时显示提示且不影响锁屏稳定性。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，28 个测试套件共 99 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。小米实机从自定义锁屏分别点击时间与日期，系统可识别并启动 `com.android.deskclock/.DeskClockTabActivity` 与 `com.android.calendar/.homepage.AllInOneActivity`；补充动态包识别后，时钟入口不再依赖厂商闹钟中转页，日志无应用致命异常。
- 真机：最终 Debug APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=1750 ms`，版本保持 `0.1.2 (3)`。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104672941 字节，SHA-256 `798a7de3b7e305b50ac1398525808a9cf58064151ca08e0c485aaeee755da539`。
- 版本：继续使用 `0.1.2 (3)`，本轮未提交。

## 2026-07-26 Android 快捷文本条目颜色

- 数据兼容：快捷文本分组新增可选 `itemColors` 并与原有字符串列表按索引对应；旧配置和旧 `.kigtpk` 预设没有颜色字段时按未上色处理，文本协议与分组 ID 保持不变。
- 编辑操作：新增、删除、批量删除、跨分组移动和拖动排序均同步维护条目颜色，避免颜色与文本错位；预设导入导出会保留已设置颜色。
- 编辑界面：快捷文本条目操作区新增普通主题色的 `palette` 图标按钮，颜色选择弹窗复用现有 HSL/HEX 调色板，并提供“清除条目颜色”；已上色条目在左侧绘制色条，色条上下各保留 `8dp` 空隙且不参与布局测量。其它页面的颜色标记尺寸保持不变。
- 编辑条目布局：承载色条的条目内容层与卡片共用 `72dp` 最小高度，并将文本、选择框和操作按钮整行垂直居中；多行文本仍可自然撑高条目。
- 便捷字幕：竖屏快捷文本在底部显示色条，横屏快捷文本在左侧显示色条；候选列表宫格模式同时使用彩色边框与底部色条，列表模式使用左侧色条。
- 主界面候选列表：列表模式左侧色条上下各缩进 `6dp`，避开条目圆角裁剪区域；宫格模式的边框和底部色条保持原尺寸。
- 迷你便捷字幕：主快捷文本区域和完整候选列表遵循相同的方向及宫格/列表规则，颜色标记作为覆盖层绘制，不改变原有控件尺寸、padding 或滚动布局。
- 悬浮窗边缘修正：横屏快捷文本与候选列表列表模式的左侧色条直接放在条目容器真实左边缘，文本间距改由内部文本控件承担；色条不再依赖负边距或越界绘制，因此不会被父级裁剪，也不会覆盖文字。宫格模式继续按按钮圆角裁剪底部色条与波纹，列表模式不启用该圆角裁剪。竖屏快捷文本条目高度与扣除 RecyclerView 上下内边距后的 `88dp` 可视高度一致，底部色条不再落入被裁掉的 `16dp` 区域。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，29 个测试套件共 102 项，0 失败、0 错误、0 跳过；`git diff --check` 通过，ADB 日志未发现应用致命异常。
- 真机：Debug APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；横屏悬浮窗快捷文本的左侧纵向色条显示正常，长按打开候选列表后，列表模式色条同样可见且未遮挡文字；启动与操作日志未发现应用致命异常。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，104687977 字节，SHA-256 `d8bd3fc35b8a41ee6b2221658b9c73b2f9f673308f696a6aa011154010ad3fc5`。
- 版本：继续使用 `0.1.2 (3)`，本轮未提交。

## 2026-07-26 Android 自定义锁屏同步淡入与刘海修正

- 同步入场：锁屏宿主在壁纸、遮罩、字体与颜色准备完成前保持全透明；内嵌悬浮窗完整面板开始执行原有 `220ms` 入场动画时，宿主根层同步淡入，因此时间、日期、解锁提示、壁纸和遮罩同时出现。
- 容错：内嵌悬浮窗初始化失败或连接中断时，宿主会在 `2s` 后独立淡入，避免锁屏内容长期不可见；正常路径只执行一次同步动画。
- 刘海区域：锁屏主题从 Activity 首帧开始声明透明系统栏和切口布局，但将隐藏状态栏延后到壁纸准备完成；此前继续透出完整系统锁屏，避免透明宿主首帧留下黑额头。切换时根层仍为全透明，并结合 `FLAG_LAYOUT_NO_LIMITS`、Android 11 及以上 `always` 和旧版 `shortEdges` 切口模式，让自定义壁纸覆盖状态栏及刘海区域。
- 导航栏区域：时间、日期和解锁提示继续遵循导航栏安全区；壁纸及其遮罩单独抵消根容器的左、右、底部导航栏 Insets，并关闭根容器的 padding 裁剪，因此可延伸覆盖手势导航条或三键导航所在区域。导航栏保持透明且关闭系统对比度遮罩。
- 真机锁屏：小米设备熄屏后重新唤醒，自定义壁纸和遮罩连续覆盖到三键导航区域底边，时间、日期、解锁提示和内嵌悬浮窗保持正常布局；锁屏宿主日志无致命异常。
- 版本：继续使用 `0.1.2 (3)`，本轮未提交。

## 2026-07-26 Android LED 颜色标记、刘海避让与方向锁定

- 快捷文本颜色：LED 右侧快捷文本面板读取条目 `itemColors`，在每个纵向列表条目的左边缘绘制颜色条；文本、分组切换和朗读行为保持不变。
- 叠加层避让：快捷文本面板、LED 设置面板、输入底栏、浮动输入预览、操作提示和全屏预览统一避让 `displayCutout`；输入栏另行跟随 IME。LED 页面保持彻底全屏，不为已隐藏的手势区或三键导航额外预留底部空间。
- 方向锁定：LED 锁定键开启时使用 Android 当前方向锁定，不触发横竖屏跳转；解锁或离开 LED 页面时恢复进入前的 Activity 方向策略。
- 取色器预览：共享 HSL/HEX 取色器的预览色块内新增左对齐中文颜色名，名称复用 Windows 中文颜色采样表；前景文字按预览色亮度自动切换黑白。主题色、名片、LED 和快捷文本等复用入口同步生效。
- 版本：继续使用 `0.1.2 (3)`，本轮未提交。

## 2026-07-26 Android APP 0.1.3 发布

- 版本：Android Debug 与 Release 的 `versionName` 更新为 `0.1.3`，`versionCode` 从 `3` 递增到 `4`，保证可覆盖升级 `0.1.2`。
- 发布范围：整合完整自定义锁屏、独立锁屏监控与权限引导、锁屏壁纸和时钟字体设置、快捷文本条目颜色、使用说明重播与逐项动画、LED 刘海避让和方向锁定，以及字体导入和文件选择体验优化。
- 文档：根目录 README 的 GitHub 发行页、版本徽章和 APK 直链更新到 `APP0.1.3`；应用宝与华为应用市场入口保持不变。
- Release 兼容：锁屏基础主题改用不触发 Android 点号隐式继承的独立名称，消除 `lintVitalRelease` 检出的 `ResourceCycle`；API 28 的 `shortEdges` 和 API 30 的 `always` 刘海策略保持不变。
- 构建验证：`:app:testDebugUnitTest` 与 `:app:assembleRelease` 通过，29 个测试套件共 102 项，0 失败、0 错误；`lintVitalRelease` 和 `git diff --check` 通过。构建仅保留现有图片裁剪库 `CropImageContract` 的弃用警告。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95820331 字节，SHA-256 `528591d04e08b5b5d9294079be92485c87bfeaf64ad0eddf9805c907d01212b4`；包元数据为 `0.1.3 (4)`，APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；系统包信息确认版本为 `0.1.3 (4)`，`pkgFlags` 不包含 `DEBUGGABLE`，`run-as` 同步确认该包不可调试。
- GitHub：发行标签使用 `APP0.1.3`，发行标题使用“APP 0.1.3 发行版”，资产文件名保持 `app-release.apk`。

## 2026-07-26 中文字体资源第二批扩充

- 范围：更新独立字体资源仓库 `D:\KIGTTS_FONTS_Resource` 与本项目记录；Android、Flutter 和应用版本号均未修改。
- 新增字体：更纱黑体 UI SC 1.0.40、Maple Mono CN 7.9、霞鹜漫黑 1.003、铁蒺藜体 1.100、霞鹜臻楷 GB 0.825 和悠哉字体 0.868，共 6 个字体条目。
- 文件选择：更纱使用简体中文 UI 独立 TTF 包；Maple 使用不含 Nerd Font 图标的 CN TTF 包；两者均排除斜体。所有字体取自上游正式 Release，不使用应用不支持的 TTC 文件。
- 字重支持：更纱保留 200/300/400/600/700 五个真实静态字重；Maple 按字体内部 OS/2 元数据保留 250/275/300/400/500/600/700/800 八档；悠哉保留 300/400/500 三档。其余三个字体为单一 400 字重。
- 授权处理：6 个条目均随资源保存各自上游的 SIL Open Font License 1.1；许可证统一为 UTF-8 无 BOM 和 LF，并写入清单 SHA-256。
- 资源清单：`font_manifest.json` 升级为 `2026.07.26.4`，当前共 41 个字体条目、96 个被引用字体/许可证文件，引用资源总计 807417273 字节；清单大小 37797 字节。
- 本地验证：全部引用文件的大小和 SHA-256 匹配；所有字重文件可由 FontTools 解析且清单权重与 `OS/2.usWeightClass` 一致；字体和许可证目录均无未被清单引用的遗留文件。
- 客户端验证：`:app:testDebugUnitTest --tests com.lhtstudio.kigtts.app.data.AppFontCatalogParserTest` 通过，生产清单解析与静态字重吸附逻辑保持兼容。
- Hugging Face：27 个新增/更新文件通过单次提交发布，提交为 `b4f4548064ec3f8fb7e62095302315e9c78c0acc`。
- ModelScope：上传缓存跳过 71 个既有文件，仅提交本次 27 个文件，0 失败。
- 反向验证：使用应用 User-Agent 从 Hugging Face 和 ModelScope 的实际 `resolve` 地址分别下载本次 27 个文件；两个源均为 27/27 文件、341181325 字节，大小与 SHA-256 全部一致。

## 2026-07-26 Android 锁屏遮罩、平板竖屏与网页投屏修正

- 无壁纸遮罩：锁屏背景遮罩不再依赖自定义壁纸位图；未选择壁纸时仍在透明锁屏宿主上覆盖系统锁屏壁纸，颜色、透明度、全填充及边缘渐变均保持生效。设置页始终显示遮罩选项，只有高斯模糊继续要求先选择自定义壁纸；预览同步显示无自定义壁纸时的遮罩效果。
- 平板竖屏：锁屏布局策略将平板拆分为横屏和竖屏两类。平板竖屏改用手机式上下布局，时间和日期位于顶部并遵循居左设置，解锁提示固定在底部，内嵌完整悬浮窗实例按真实可变高度在屏幕中间定位；平板横屏继续使用左侧时钟、右侧悬浮窗布局。
- 投屏模式：网页投屏页的设置面板新增“自适应 / LED”模式选择，安卓“投屏显示设置”同步补充显示模式入口；选择结果写入独立偏好，不修改原生 LED 页面设置，服务或应用重启后仍会恢复。恢复投屏默认设置时同时回到自适应大字幕。
- 设置生效：自适应大字幕的显示高度现已参与最大字号计算；投屏页模式切换会立即重绘并同步安卓端。遥控器延迟发送设置时改为保存当前设置快照，避免状态回推或多端操作覆盖尚未发出的调整。
- 网页音频：投屏页开启音频时会等待浏览器音频上下文真正进入运行状态，再向安卓端登记音频接收；浏览器拒绝播放或初始化失败时自动撤销开关并提示。重连继续携带已确认的音频接收状态，PCM 与文件音频在上下文被临时挂起时会尝试恢复播放。
- 验证：新增平板横竖屏模式、定位策略和投屏显示模式默认值测试；全部局域网投屏脚本通过 Node 语法检查，`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，30 个测试套件共 104 项，0 失败、0 错误、0 跳过，`git diff --check` 通过。版本保持 `0.1.3 (4)`。
- 真机：最终 Debug APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=2170 ms`，应用进程保持运行，启动日志无致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。设备未开启允许 ADB 注入触摸的安全选项，投屏服务默认关闭，因此浏览器实际发声仍需在设备手动开启服务后确认。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，102804728 字节，SHA-256 `01894992af818d2af6b67ea4c5d1978fed702da2868ad5541167d2d94d053b63`。

## 2026-07-26 网页投屏连接、延迟与显示设置修正

- 断联根因：网页命令后的完整状态广播原先依赖 Android 主线程 `Handler`；Compose 重组、锁屏宿主和其它主线程工作繁忙时，广播会长期延后甚至发送失败，表现为操作没有反馈、WebSocket `1006` 断开并自动重连。状态合并广播现改用服务端独立单线程调度器，不再经过 UI 主线程；所有网页控制命令由服务端全局串行执行器处理，既不阻塞 NanoWSD 读帧循环，也避免多个遥控端同时写入时互相覆盖。
- 重连与音频：网页重连初始等待缩短到 `250ms`，退避上限降至 `2s`，发送异常会立即进入恢复流程。已登记的投屏音频客户端瞬断后保留 `2s` 重连宽限，防止短暂掉线立刻把“仅投屏端”音频回退到手机扬声器；服务停止时会清除该宽限状态。
- 显示逻辑：撤销上一轮新增且与“使用正常字形 / 自适应多行”重复的“自适应 / LED”显示模式，安卓设置、投屏页、遥控器及传输协议均不再暴露 `displayMode`。投屏画面继续使用原有 LED 渲染器，编辑预览保持大字幕样式；新的投屏默认值为“使用正常字形”和“自适应多行”同时开启。
- 持久化：投屏服务启动时直接从 `lan_cast_display_settings` 加载显示设置，网页修改后也由服务自身写入 DataStore，不再依赖主界面 Compose 收集器是否存活；前台界面仍会即时同步，但不会重复写入。快速连续调整使用修订号丢弃过期保存任务，避免旧值晚于新值落盘。
- 自动测试：新增投屏默认值、音频重连宽限和真实 WebSocket 命令回归测试；局域网投屏全部 JavaScript 通过 `node --check`，`:app:testDebugUnitTest`、`:app:assembleDebug` 与 `:app:assembleDebugAndroidTest` 通过。单元测试共 30 个测试套件、106 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。
- 实机链路：小米设备同时连接投屏端和遥控端，连续发送 25 条显示设置、字幕样式、清屏和快捷文本相关命令，全部 ACK 成功且连接保持；最大 ACK 延迟 `84ms`，平均 `63.4ms`，两端后续 Ping/Pong 正常。停止并重启投屏服务后，字幕色、背景色、正常字形和自适应多行均正确恢复；测试颜色随后已恢复为新的白字黑底默认值。
- 真机安装：不含临时调试入口的最终 Debug APK 已通过 `adb install -r` 覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=1735ms`，进程正常运行，日志无致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。版本保持 `0.1.3 (4)`。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，100544114 字节，SHA-256 `3f98c5f5658e5dfaa2f67e7db7b27c763cc840ed7b57c11902e86e591b751fa6`。

## 2026-07-26 Android 锁屏投屏保活与 LED 真辉光

- 投屏保活：局域网投屏服务启动成功后持有非引用计数的 `PARTIAL_WAKE_LOCK` 与高性能 `WifiLock`，停止服务时按相反顺序可靠释放；服务被系统回收后的启动策略改为 `START_STICKY`，用户手动停止仍使用 `START_NOT_STICKY`。
- 前台服务类型：投屏服务从 `specialUse` 调整为语义更准确的 `connectedDevice`，补齐 Android 14 及以上对应权限和网络状态前置权限；锁屏宿主与服务器仍保持独立生命周期，自定义锁屏不会主动关闭投屏服务。
- 厂商后台限制：小米实机熄屏约 `8.8s` 后 WebSocket 被断开，系统日志明确记录 HyperOS Greezer 将应用唤醒锁标记为 `disabled`；标准 Doze 白名单与额外网络请求均不能绕过该厂商冻结。首次开启投屏时现在会请求忽略电池优化，运行卡片也新增“后台运行设置”入口；小米设备实测可正确解析到安全中心的应用省电详情页。
- LED 辉光：废弃通过放大半透明几何图形模拟辉光的实现，改为离屏软件位图上的双层 `BlurMaskFilter` 加清晰字芯；外层负责宽范围低透明度光晕，内层补充近距离亮度，边缘连续衰减。
- 字形覆盖：点阵圆点、点阵方块、正常滚动字形和自适应多行正常字形均使用同一套辉光强度与半径计算；关闭辉光时只绘制清晰字芯。正常字形加入渲染缓存、`90ms` 参数防抖和分块重叠区，避免拖动设置时频繁重建大位图或在长字幕块边界切断光晕。
- 视觉验证：小米实机横屏同时渲染正常字形与点阵字形，正常字形保留清晰实心轮廓，点阵保持独立灯珠，两者外缘均呈柔和渐隐光晕，未发现原有硬边半透明块或明显分块接缝。
- 自动验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，31 个测试套件共 108 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。构建仅保留原有图片裁剪库的弃用警告。
- 真机安装：不含临时验证 Activity 和 Receiver 的最终 Debug APK 已通过 `adb install -r` 覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=1894ms`，进程保持运行，启动日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。测试用 Doze 白名单已移除。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，102805947 字节，SHA-256 `097e354a3ed2b28ab189e5b8f5056e98b1a59ed862819703332beb75c32da325`。
- 版本：继续使用 `0.1.3 (4)`，本轮未提交。

## 2026-07-26 Android 可选后台提醒与权限弹窗统一

- 可选后台权限：启用局域网投屏时不再强制先取得忽略电池优化权限；服务会立即启动。尚未授权时只显示可选说明，选择“暂不设置”不会关闭或撤销投屏功能。
- 提醒控制：后台运行说明增加“下次不再提示”复选项，整行均可点按；勾选后写入 DataStore，后续开启投屏不再自动提示。用户仍可通过运行卡片中的“后台运行设置”主动进入系统页面。
- 系统跳转保护：所有自动后台优化请求均移到应用内说明弹窗之后，只有用户确认“前往设置”才打开系统权限页面；已经取得豁免时不重复显示说明。
- 统一样式：麦克风、相机、通知、悬浮窗、文件和图片读取、全部文件访问、锁屏与后台显示、投屏后台运行、无障碍稳定监听均统一复用 `PermissionPurposeDialog`，标题右侧显示对应 Material Symbol，并使用一致的确认、取消和不可误触关闭行为。
- 信息层级：权限弹窗首屏只显示简短用途摘要；权限名称、使用场景、用途范围和隐私说明默认折叠，通过“查看详细说明”展开，并带淡入和纵向展开动画。内建文件管理器与图库不再把完整权限说明直接嵌入选择器。
- 锁屏与无障碍：原“允许锁屏与后台显示”弹窗改为统一权限组件并补充 `lock` 图标；音量热键的无障碍建议与正式授权入口补充 `accessibility_new` 图标，详细用途与隐私说明改为可折叠展示。从未授权状态点击无障碍设置按钮时也会先显示说明。
- 自动验证：新增投屏后台提醒默认开启测试；`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，31 个测试套件共 109 项，0 失败、0 错误、0 跳过，`git diff --check` 通过。构建仅保留原有图片裁剪库的弃用警告。
- 真机安装：最终 Debug APK 已通过 `adb install -r` 覆盖安装到小米设备 `cfc8ef16`；定向打开“投屏与遥控”页面成功，冷启动 `TotalTime=2195ms`，应用进程保持运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，102806845 字节，SHA-256 `383357ed2e8feb4c6c87422f5b636309d3abe43babfe705ea491de314f081dad`。
- 版本：继续使用 `0.1.3 (4)`，本轮未提交。

## 2026-07-26 Android 权限弹窗尺寸与无障碍引导修正

- 尺寸稳定：权限详细说明不再使用 `AnimatedVisibility`、纵向展开或淡入淡出动画，改为稳定的条件布局；展开和收起只触发一次内容重新测量，避免弹窗窗口与内部滚动区在动画期间反复争用高度。
- 高度恢复：详细说明状态改用随弹窗内容生命周期销毁的普通 `remember`，不再跨弹窗保留展开状态；收起时 Material 弹窗可重新按摘要内容计算包裹高度。
- 无障碍定位：音量热键的无障碍说明属于操作引导而非普通权限用途声明，因此撤销结构化“权限名称 / 使用场景 / 用途范围 / 隐私说明”模板。
- 引导文案：恢复原有设备兼容提示、音量键监听说明、QQ 扫一扫直达范围、隐私边界和进入系统无障碍页面后的具体操作步骤；两层引导继续保留右上角 `accessibility_new` 图标。
- 验证：`:app:testDebugUnitTest`、`:app:assembleDebug`、`:app:assembleRelease` 和 `lintVitalRelease` 均通过，31 个测试套件共 109 项，0 失败、0 错误、0 跳过。构建仅保留原有图片裁剪库的弃用警告。
- 真机安装：修复后的 Release APK 已通过 `adb install -r` 覆盖安装到小米设备 `cfc8ef16`；最终构建冷启动 `TotalTime=444ms`，包信息确认版本 `0.1.3 (4)` 且不包含 `DEBUGGABLE`，应用进程保持运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95836899 字节，SHA-256 `f01df3bdb531995e1f6982daba597c8d58649918bbfcfafcd99298d1bf8fc1ab`。
- 版本：继续使用 `0.1.3 (4)`，本轮未提交。

## 2026-07-26 Android 权限说明文案定稿

- 信息结构：普通权限弹窗继续保留现有标题、权限名称和隐私说明；原“使用场景”和“用途与范围”合并为单个“用途与范围”区块，功能范围与具体用途在同一区块中分层显示。
- 摘要文案：全部文件访问改为说明可直接浏览共享存储且无需逐个授权目录；投屏后台运行改为建议持续运行，避免熄屏后投屏和网页遥控无法连接设备。
- 锁屏权限：小米、vivo 和 iQOO 的摘要明确说明需允许“锁屏显示”和“后台弹出界面”，否则无法正常显示自定义锁屏；其它设备使用对应的通用提示。
- 保持范围：麦克风、相机、通知、悬浮窗及文件和图片选择等以“用于”开头的摘要保持原样；无障碍说明继续使用独立操作引导，不套用普通权限摘要结构。
- 验证：`:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，31 个测试套件共 109 项，0 失败、0 错误、0 跳过。构建仅保留原有图片裁剪库的弃用警告；版本继续使用 `0.1.3 (4)`。

## 2026-07-26 Android 调色板卡片海拔与阴影修正

- 卡片海拔：调色板编辑页对齐语音包卡片，常态使用统一的 `UiTokens.CardElevation`，拖拽态提升到 `10dp`，进入和退出拖拽分别使用与语音包一致的 `120ms / 160ms` 海拔动画。
- 阴影承载：卡片外层改用与语音包相同的水平 `2dp`、垂直 `6dp` 留白；调色板 `RecyclerView` 关闭子项裁剪，拖拽时同时将宿主条目提升到 `12dp` 层级，避免阴影被相邻条目或列表边界截断。
- 状态清理：拖拽结束和条目回收时取消残留动画并恢复宿主层级，防止复用后的卡片继续保持抬升状态。
- 验证：`:app:compileDebugKotlin`、`:app:testDebugUnitTest` 与 `git diff --check` 通过，31 个测试套件共 109 项，0 失败、0 错误、0 跳过。版本保持 `0.1.3 (4)`。
- Release 构建：`:app:assembleRelease` 与 `lintVitalRelease` 通过，仅保留原有图片裁剪库的弃用警告；APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95836899 字节，SHA-256 `cba4513cc3104e1599c0217fc273686b2f9d046900897988a7a1c9e1f2d7d07c`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.3 (4)` 且不包含 `DEBUGGABLE`。冷启动 `TotalTime=504ms`，应用进程正常运行，启动日志无致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。

## 2026-07-26 Android 快捷文本颜色编辑入口调整

- 入口位置：编辑快捷文本列表不再为每个条目单独显示调色盘按钮，条目操作区恢复为编辑、拖动排序和删除；颜色入口移动到“编辑快捷文本”弹窗内。
- 编辑样式：文本框下方新增“条目颜色”操作行，显示调色盘图标、当前色号与中文颜色名、圆形颜色样本和进入箭头；未设置颜色时显示“未设置”。
- 保存行为：取色器和“清除条目颜色”继续复用现有实现，但选择结果先保存在弹窗草稿中；点击“保存”后与文本一起写入，取消或关闭编辑弹窗不会修改条目颜色。
- 代码清理：移除快捷文本列表、RecyclerView 适配器、ViewHolder 和条目 Composable 中不再需要的颜色按钮回调链，颜色标记及持久化格式保持不变。
- 验证：`:app:compileDebugKotlin`、`:app:testDebugUnitTest`、`:app:assembleRelease`、`lintVitalRelease` 与 `git diff --check` 通过，31 个测试套件共 109 项，0 失败、0 错误、0 跳过。构建仅保留原有图片裁剪库的弃用警告，版本保持 `0.1.3 (4)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95836899 字节，SHA-256 `5460e778d1650dbe9cbd3ab5688ea34aa0dde0325445f47bb677947569e55d42`；APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.3 (4)` 且不包含 `DEBUGGABLE`。冷启动 `TotalTime=1608ms`，应用进程正常运行，启动日志无致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。本轮未提交。

## 2026-07-26 Android LED 辉光与投屏端控件修正

- 原生 LED 设置：发光效果开关和发光强度滑条不再受点阵渲染开关限制，正常字形、滚动字形和自适应多行字形均可独立启用并调整真实模糊辉光；点阵形状与密度设置仍只在点阵模式中显示。
- 辉光可见性：双层 `BlurMaskFilter` 继续保留清晰字芯，使用感知强度曲线提高中低档位的内外光晕透明度，并适当扩大文字与灯珠的模糊半径；默认 `42%` 强度不再接近不可见，最大强度仍受明确上限约束。
- 网页取色器：遥控器和投屏端的字幕色、背景色输入统一为 `40px` 圆形色样，移除 Chromium 原生色块的内边距与双重边框，补充悬停和键盘焦点反馈。
- 遥控焦点：投屏端快捷文本条目和分组 Tab 使用内描边焦点框，完整高亮保持在滚动容器内部，不再被列表边缘裁剪。
- 遥控导航：上下方向键改为先锁定最近的纵向控件行，再在该行选择横向距离最近的控件；投屏设置中的滑条不会再被更远但横向更接近的开关或按钮跳过，左右导航维持原有空间评分逻辑。
- 浏览器验证：使用真实 Chromium 检查两类取色输入均为 `40 x 40` 圆形、无原生色样边框；快捷文本和分组 Tab 的四边焦点框完整可见；模拟设置页方向键顺序为“开关 -> 滑条 -> 下一开关”。
- 自动验证：局域网投屏目录下 8 个 JavaScript 文件全部通过 `node --check`，`:app:testDebugUnitTest`、`:app:assembleDebug` 与 `git diff --check` 通过，31 个测试套件共 110 项，0 失败、0 错误。构建仅保留原有图片裁剪库的弃用警告。
- Debug APK：`D:\KGTTS\android-app\app\build\outputs\apk\debug\app-debug.apk`，100425538 字节，SHA-256 `1fb24ec32bca548cae6312c50be3c0273977d2ef8d3d0858daa1ad1b552b7db8`。
- 版本：继续使用 `0.1.3 (4)`；本轮未安装、未提交。

## 2026-07-26 Android APP 0.1.4 发布

- 版本：Android Debug 与 Release 的 `versionName` 更新为 `0.1.4`，`versionCode` 从 `4` 递增到 `5`，可从 `0.1.3` 保留数据覆盖升级。
- 发布内容：集中交付投屏连接与音频稳定性、自定义锁屏和平板布局、LED 真实辉光、权限说明、调色板卡片、快捷文本颜色编辑以及网页遥控器焦点与取色器修正。
- 文档：根目录 README 的 GitHub 发行页、版本徽章和 APK 直链统一更新到 `APP0.1.4`；应用宝、华为应用市场和训练器下载入口保持不变。
- 自动验证：局域网投屏目录下 8 个 JavaScript 文件全部通过 `node --check`，`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过；31 个测试套件共 110 项，0 失败、0 错误。构建仅保留原有图片裁剪库的弃用警告。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95837403 字节，SHA-256 `87b788694c0b4d98f12652cf5f027e0a8cbfa8bfc7fbfe0a1c8806e792bd3c0a`；包元数据为 `0.1.4 (5)`，APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)` 且不包含 `DEBUGGABLE`。冷启动 `TotalTime=471ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：发行标签使用 `APP0.1.4`，发行标题使用“APP 0.1.4 发行版”，资产文件名保持 `app-release.apk`；本轮只更新 `origin`，不向 `kigscope` 远端合并或推送。

## 2026-07-27 Android LED、投屏交互与锁屏布局修正

- 调色板：拖动排序时只提升卡片海拔，不再叠加主题色或降低透明度；标题栏确认键始终可用，即使调色板没有改动也能正常保存并退出。颜色按钮、拖动手柄、预设色、自定义色轮、遮罩颜色和编辑调色板入口补齐统一震动反馈。
- 设置恢复：语音合成与麦克风设置分别增加“恢复默认值”入口和二次确认。语音合成会恢复朗读开关、播放增益、音频焦点及 Piper 参数；麦克风会恢复首选输入、系统回声抑制、通话模式、AEC3 和降噪方式，并在实时识别运行时只重启一次录音器。
- LED 参数：原“点阵密度”统一拆分为“每行 LED 行数”和“灯珠尺寸”。安卓与网页均按实际文字行高计算点阵宫格；圆点最大直径使用宫格对角线，方点最大边长使用宫格高度。旧版 `dotDensity` 会自动迁移为近似 LED 行数，设置存储格式升级到版本 5。
- 网页循环：滚动字幕改为按“文字宽度 + 循环间距”连续铺排多个副本，不再把整个投屏视口算进空白间距；最小间距下两遍文字之间只保留用户设置的间隔。
- 投屏音频：投屏显示页默认尝试启用音频。浏览器允许自动播放时直接注册为音频端；被自动播放策略拦截时保持开启选择并显示“启用音频”确认条，由用户点击后恢复，不会静默切回关闭。
- 遥控器动画：快捷文本主控件和长按候选弹窗增加内容键缓存；普通按钮只引起运行状态或设置状态变化时不再重建滚动列表、重播入场动画或重置滚动位置。波纹节点改为独立装饰元素，避免命中按钮文字布局规则。
- LED 震动：LED 全屏透明按钮、输入栏按钮、快捷文本条目、分组选择、设置开关、分段按钮和取色入口补齐应用统一震动反馈；锁屏样式、时钟字体、投屏地址与二维码 Tab 等近期二级入口同步补齐。
- 锁屏首次显示：锁屏专用悬浮窗实例在宿主首次揭示完成前固定显示横版 Logo，不再先闪现最近字幕再切回 Logo；普通悬浮窗的顶部状态逻辑保持不变。
- 平板横屏：锁屏悬浮面板改为居中放置在右半区，时间和滑动解锁区域居中放置在左半区；启用时间居左时增加独立内边距，避免内容贴近屏幕左缘。
- 验证：局域网投屏 JavaScript 全部通过 `node --check`，`:app:testDebugUnitTest` 通过，32 个测试套件共 114 项，0 失败、0 错误、0 跳过；新增旧点阵设置迁移、灯珠几何和右半区定位测试。真实 Chromium 验证了新设置控件、24dp 连续循环间距、音频自动启用，以及非快捷文本状态更新不会重建滚动列表。`git diff --check` 通过，版本保持 `0.1.4 (5)`。
- Release 构建：`:app:assembleRelease` 与 `:app:lintVitalRelease` 通过，仅保留现有图片裁剪库弃用警告和 Android SDK XML 工具版本提示。APK 位于 `D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95838239 字节，SHA-256 `2af7aa7afe187ceca103d2d8381a83282dc6eaf96ae04993248b43b77259035e`；本轮未安装、未提交。

## 2026-07-27 Android 锁屏电量与 LED 密度设置补充

- 分组图标：编辑便捷字幕分组时，当前图标预览和图标选择宫格强制使用普通前景色，不再跟随主题色染色；选中项背景提示保持不变。
- 解锁反馈：自定义锁屏的滑动手势达到解锁阈值并松手时触发一次系统虚拟按键震动，未达到阈值的回弹不震动。
- LED 密度：Android LED 设置、投屏设置、投屏显示页和网页遥控器统一将“每行 LED 行数”改名为“灯珠密度”，可调上限从 48 提高到 256；原设置值和默认 24 保持兼容。
- 锁屏电量：时间和日期下方可选显示当前电量及充电状态，默认关闭；提供“简洁”和“完整”两种文案样式，并支持始终显示或仅在充电、低于阈值时显示。
- 电量阈值：低电量阈值默认 30%，可在 1% 至 100% 间调整；条件判断按“低于阈值”执行，等于阈值时不显示。锁屏存续期间通过系统电量广播实时更新，字体、明暗色、自定义壁纸可读性和入场淡入均沿用时间日期区域。
- 设置预览：锁屏设置页新增独立“电量状态”卡片，壁纸预览同步读取设备当前电量并按真实显示策略呈现；锁屏字体说明同步覆盖电量状态文字。
- 验证：局域网投屏目录下 8 个 JavaScript 文件全部通过 `node --check`；`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，32 个测试套件共 116 项，0 失败、0 错误、0 跳过。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95854619 字节，SHA-256 `032e96a16486a7c5ed5afaa92aa944227adea037eaeee7aedc590bc1e900ccf1`；已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`，设备确认版本为 `0.1.4 (5)`。

## 2026-07-27 Android 快捷文本词频排序与区域手势

- 词频排序：编辑快捷文本页面新增“根据使用频率排序”开关；主界面和悬浮窗点击快捷文本或候选列表条目时记录分组内使用次数，次数相同时继续保持原手动顺序。
- 稳定刷新：点击条目后当前列表不立即重排；重新进入页面、切换分组、重新展开悬浮窗或重新打开候选列表时才应用新的频率顺序，并从列表起点展示，避免滚动位置突然跳动。
- 固化排序：编辑页新增“应用当前排序”，二次确认后将所有分组的当前频率顺序写入原有手动列表，同时关闭自动排序；条目颜色跟随文本一起重排，预设导入导出格式不变。
- 区域手势：便捷字幕快捷文本区域支持竖屏上滑打开候选列表、下滑聚焦输入框并拉起键盘；横屏对应左滑和右滑。悬浮窗展开态使用相同逻辑，手势只覆盖条目容器，不覆盖紧凑模式分组切换器。
- 手势设置：设置的“便捷字幕显示”卡片新增快捷文本区域手势总开关与方向翻转开关；主界面和悬浮窗共用持久化设置。
- 使用引导：首步引导按当前横竖屏、手势开关和翻转状态显示真实操作方向；紧凑模式分组切换器增加独立高亮框和箭头气泡，竖屏提示上下滑动、横屏提示左右滑动。
- 自动验证：`:app:compileDebugKotlin`、`:app:testDebugUnitTest`、`:app:assembleDebug`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 121 项，0 失败、0 错误、0 跳过。新增词频排序稳定性、分组隔离、手势方向翻转和引导状态测试；构建仅保留现有图片裁剪库弃用警告和 Android SDK XML 工具版本提示。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `c90ae3c4f954e361c56970c7cac782272dc71a5cfa7ee4119432fb90c03512c1`；APK Signature Scheme v2 验证通过，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)` 且不包含 `DEBUGGABLE`。冷启动 `TotalTime=533ms`，应用进程与前台 Activity 正常，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：本轮提交至 `github-main-clean`，随后以非快进合并同步到 `main`；仅推送 `origin`，不向 `kigscope` 远端推送。

## 2026-07-27 Android 悬浮窗迷你快捷文本裁剪修正

- 问题定位：悬浮窗迷你便捷字幕在竖屏下出现第三行文字和底部条目颜色标记同时被裁剪，确认是新增快捷文本区域手势后，手势宿主、快捷文本容器和列表形成的多层裁剪边界导致。
- 修正方式：关闭手势宿主、快捷文本容器和 `RecyclerView` 的子项及内边距裁剪，让条目已有的文字与颜色标记可在原有绘制边界内完整显示。
- 布局保持：未修改快捷文本字号、行距、内边距、条目高度、滚动位置或颜色条位置，避免引入新的排版和触控区域变化。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 121 项，0 失败、0 错误、0 跳过。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `c2f2bd6aec2c3bc3adf15e8adf3e7af5e0be37de00ceae4c6ba6b611633383a2`；签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `TotalTime=473ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：本轮提交至 `github-main-clean`，随后以非快进合并同步到 `main`；仅推送 `origin`，不向 `kigscope` 远端推送。

## 2026-07-27 Android 悬浮窗自定义字体裁剪补充修正

- 问题定位：初次解除手势宿主和列表裁剪后，系统字体可完整显示，但自定义字体第三行仍可能被截断；原因是自定义字体的字形外延会超出条目绘制边界，而快捷文本条目自身的根容器仍在裁剪子视图。
- 修正方式：补充关闭条目根容器的子项及内边距裁剪，保留原有字号、行距、内边距、条目高度、文本位置和颜色条位置。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 121 项，0 失败、0 错误、0 跳过。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `71572c3a52be011a589496f36dc232156b223557e597ea88348c23a93eb0d21c`；签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)`，冷启动 `TotalTime=823ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。

## 2026-07-27 Android 快捷文本颜色、布局引导与悬浮窗行高修正

- 布局选择：首次进入便捷字幕的“选择快捷文本布局”弹窗中，选中项不再增加海拔和阴影，仅通过主题色底色、描边和单选状态区分。
- 颜色保存：编辑快捷文本改为一次性提交文本和条目颜色，避免连续调用保存时颜色请求被正在执行的文本保存忽略；快捷文本配置保存器改为串行处理首个和最新快照，连续编辑不再丢失最后一次修改。
- 状态同步：每个持久化快照完成后同步发布给实时宿主，主界面状态即时更新，悬浮窗按顺序接收最终颜色配置，解决颜色更新延迟和小概率未保存。
- 悬浮窗行高：竖屏迷你快捷文本的系统字体和第三方字体统一强制使用 `1.15×` 紧凑行高；系统字体直接按实际字体度量设置，第三方字体通过字体应用器修正到相同目标，三行文本在不同设备上保持一致。
- 引导布局：第二步说明卡向左上偏移，减少与大字幕操作气泡互相遮挡；第三步在横屏下把语音识别和发送气泡移动到对应按钮上方，竖屏位置保持不变。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 123 项，0 失败、0 错误、0 跳过；新增紧凑行高和横屏气泡位置测试。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `23eef9b5820ab008c07170ec00fd63641c9a540dc4a68a883345c109d33c171d`；签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)`，冷启动 `TotalTime=1331ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：本轮提交至 `github-main-clean`，随后以非快进合并同步到 `main`；仅推送 `origin`，不向 `kigscope` 远端推送。

## 2026-07-27 Android 第二步使用引导横竖屏避让修正

- 横屏定位：第二步说明卡使用独立的紧凑宽度，定位到顶栏菜单按钮右侧并与菜单顶部对齐，避开大字幕右侧操作按钮及其气泡。
- 竖屏定位：第二步说明卡固定贴近屏幕底部，不再落在大字幕底部操作按钮气泡所在高度；说明卡会在实测高度更新后保持底部边距。
- 其它步骤：第一步、第三步及第二步所有高亮框和气泡锚点保持不变；横屏第三步语音识别和发送气泡仍位于按钮上方。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 123 项，0 失败、0 错误、0 跳过。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `b6b30f6b8be240fb140916b16bd6d39942af78f47214e34030d5686f73b3a1dd`；签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)`，冷启动 `TotalTime=808ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：本轮提交至 `github-main-clean`，随后以非快进合并同步到 `main`；仅推送 `origin`，不向 `kigscope` 远端推送。

## 2026-07-27 Android 悬浮窗竖屏快捷文本垂直居中修正

- 问题定位：迷你便捷字幕竖屏快捷文本虽然使用垂直居中重力，但旧实现为文字设置了顶部 `8dp`、底部 `20dp` 的非对称内边距，导致文字视觉中心相对 `88dp` 卡片中心向上偏移约 `6dp`。
- 修正方式：创建态和绑定态统一改为上下 `8dp` 对称内边距，让系统字体与第三方字体均以卡片实际中心排布；卡片高度、三行上限、`1.15×` 行高、左右内边距和底部颜色条位置保持不变。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 123 项，0 失败、0 错误、0 跳过。版本保持 `0.1.4 (5)`。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871003 字节，SHA-256 `236d296133d8a75ca5842a7a83981291882483217063aebf3a56d61df6a0e518`；签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.4 (5)`，冷启动 `TotalTime=426ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：本轮提交至 `github-main-clean`，随后以非快进合并同步到 `main`；仅推送 `origin`，不向 `kigscope` 远端推送。

## 2026-07-27 Android APP 0.1.5 发布

- 版本：Android Debug 与 Release 的 `versionName` 更新为 `0.1.5`，`versionCode` 从 `5` 递增到 `6`，支持从 `0.1.4` 保留数据覆盖升级。
- 新增功能：增加锁屏电量与充电状态显示、最高 256 档 LED 灯珠密度、快捷文本使用频率排序及快捷文本区域手势；支持将当前频率顺序固化为手动排序。
- 交互优化：完善便捷字幕首次使用与设置重放引导，横竖屏分别避让说明卡和按钮气泡；布局选择选中项移除额外阴影，横屏语音识别与发送气泡移动到按钮上方。
- 稳定性修复：修复快捷文本颜色连续保存丢失、主界面与悬浮窗颜色同步延迟，以及手势容器引入的文字和颜色条裁剪；统一系统字体与第三方字体行高，并修正竖屏悬浮窗快捷文本垂直居中。
- 文档：根目录 README 的 GitHub 发行页、版本徽章和 APK 直链统一更新到 `APP0.1.5`；应用宝、华为应用市场和训练器下载入口保持不变。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，34 个测试套件共 123 项，0 失败、0 错误、0 跳过。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95871007 字节，SHA-256 `f72cdb08e65939aee6204656b45550628b4d5b35d5b334b9ef3db8e4c0d43cd5`；包元数据为 `0.1.5 (6)`，不包含 `DEBUGGABLE`，签名证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机安装：Release APK 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；设备确认版本为 `0.1.5 (6)`，冷启动 `TotalTime=1087ms`，应用进程正常运行，日志无应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。
- GitHub：发行标签使用 `APP0.1.5`，发行标题使用“APP 0.1.5 发行版”，资产文件名保持 `app-release.apk`；`github-main-clean` 同步到 `origin` 与 `kigscope`，只在 `origin` 合并并推送 `main`。

## 2026-08-07 Android 实验性目标说话人识别前端

- 模块并存：语音识别设置新增“兼容模式（原版）”与“实验识别”，默认继续使用原版；切换模式会重置说话人门控状态，但识别底模仍使用现有 SenseVoice。
- 双用途接口：实验识别关闭“仅识别我的声音”时保留全部环境语音，作为现场字幕和辅助收音入口；开启后使用现有声纹样本持续筛选目标说话人。
- 目标筛选：新增可替换的 `TargetSpeakerFrontend` 接口和 VoiceFilter-Lite 架构启发的轻量时间掩码实现，通过重叠窗口声纹评分、进入/退出阈值滞回、上下文扩展和淡入淡出压低未命中区域；多人同时说话的重叠语音仍是当前局限。
- 轻声与头壳场景：实验模式提供独立“轻声收音灵敏度”，默认 `50%`，通过较低的低能量门限和受峰值约束的单次增益改善轻声、头壳内语音接收；提高灵敏度可能同时带入更多环境声。
- 声纹样本：目标匹配同时比较组合声纹与各条独立样本；录入引导建议经常戴头壳的用户用其中一句采集真实头壳内语音。
- 测试：新增目标区域保留、非目标丢弃、阈值滞回、短音频等待、未知模式回退、灵敏度门限和有限增益单元测试。针对性测试及完整 `:app:testDebugUnitTest :app:assembleDebug` 均通过，35 个测试套件共 130 项，0 失败、0 错误、0 跳过；构建仅保留现有图片裁剪库、SDK XML 工具版本和 AEC3 依赖的弃用警告。
- 构建环境：完整验证前确认百度网盘同步会在 `app/build` 中持续生成 `.baiduyun.uploading.cfg` 并占用 Kotlin 产物，临时停止同步并清理生成目录后构建成功；该临时格式已在仓库排除列表中，不涉及源文件。
- 设计记录：`android-app/EXPERIMENTAL_RECOGNITION.md` 记录模式语义、当前边界以及后续接入量化 VoiceFilter-Lite、WeSep 等流式前端的替换点。

## 2026-08-07 Android 实验性神经目标说话人分离

- 方案调研：对照 Google VoiceFilter-Lite、WeSep、Personalized PercepNet 及可部署公开权重，选用 `penta2himajin/tse-conv-tasnet-48k` 作为可选流式分离器，并使用与其匹配的 SpeechBrain ECAPA-TDNN ONNX 生成 192 维条件特征；SenseVoice 识别底模保持不变。
- 资源管理：新增约 90 MB 的可选神经分离资源下载、断点续传、固定版本、文件大小与 SHA-256 校验、原子替换、删除和许可证说明。模型不打进 APK，失败或未安装不会影响原识别链路。
- 实时性能：分离器按 48 kHz / 10 ms 因果分块运行，复用 89 组流式状态双缓冲和输入输出张量，优先启用 ONNX Runtime XNNPACK；桌面四线程基准约 `RTF 0.217`，自动模式在设备实测 `RTF > 0.85` 后回退轻量筛选。
- 效果保护：先由 CAMPPlus 判断目标说话人，再执行神经分离；分离结果重新计算 CAMPPlus 相似度并检查能量比例，身份偏移、音频塌缩或模型异常时使用原轻量筛选结果。公开模型跨语料表现不稳定，因此神经输出不做无条件替换。
- 双声纹兼容：新采集样本同时保存 CAMPPlus 与 ECAPA 特征；旧样本继续可用但只走轻量筛选。神经资源删除不会清除样本，重新安装后可继续使用已有 ECAPA 特征。
- 双用途保留：关闭“仅响应指定说话人”时完全绕过目标分离，继续保留多人环境字幕和辅助收音接口；轻声收音灵敏度与原实验设置继续生效。
- 设置界面：实验识别新增“自动选择（推荐）/优先神经分离/轻量筛选”，指定说话人卡片显示资源、样本就绪状态以及下载或删除入口。
- 测试：新增 SpeechBrain 滤波器组官方夹具逐值对照、神经后端启用条件、双特征序列化兼容及质量回退策略测试；版本保持 `0.1.5 (6)`。

## 2026-08-08 Android 识别 ITN 与标点独立设置

- 设置调整：移除旧“数字读法”三档后处理，改为默认关闭的“ITN（数字与格式规范化）”开关；关闭后 SenseVoice 使用 `use_itn=false`，中文数字尽量保持原始语言，不再统一转换为阿拉伯数字。
- 标点拆分：SenseVoice 本身只提供组合式 `use_itn`，无法拆出独立子模块，因此新增 sherpa-onnx 中英 CT-Transformer INT8 标点恢复后端；自动添加标点使用单独开关，只处理完整识别结果，流式预览保持低延迟。
- 资源管理：标点模型不打入 APK，提供官方 GitHub Release 的约 62 MB 可选下载、断点续传、压缩包及模型双 SHA-256 校验、原子安装和删除；安装后约占 72 MB。
- 输出规则：SenseVoice 输出的中英文句读会先统一移除，再仅由独立标点开关决定是否恢复，确保 ITN 与标点在所有组合下互不干扰；`3.14`、`12:30` 等数字内部的分隔符会保留。ITN 切换会安全停止收音、释放并重载 SenseVoice，再恢复原运行状态。
- 并发保护：识别控制器统一跟踪最终分段与流式预览的转写任务；切换 ITN 前先停止录音并等待正在使用旧 SenseVoice 的任务退出，避免模型释放与转写并发。神经分离器一旦发生执行期异常，本次实例不再重复尝试并继续走轻量回退。
- 运行验证：项目现有 sherpa-onnx 桌面运行时成功加载同一 INT8 标点模型；中文示例恢复为“我们都是木头人，不会说话，不会动。”，加载约 `679 ms`，两条推理分别约 `5.1 ms` 与 `4.1 ms`。Android 端首次启用时在后台预加载模型，避免把加载耗时集中到首条识别结果。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug` 通过，37 个测试套件共 138 项，0 失败、0 错误、0 跳过；Debug APK 已确认不包含可选神经或标点模型。全量 `lintDebug` 仍被项目既有 28 项错误阻断，本轮新增标点文件无 Lint 错误，仅有一条存储空间 API 建议。版本保持 `0.1.5 (6)`。

## 2026-08-08 魔搭实验识别资源包

- 兼容策略：保留旧文件 `kigtts-recognition-resources-20260505.7z` 及其下载链接不变；新增文件使用 `kigtts-recognition-resources-experimental-20260808.7z`，避免新旧资源被覆盖或混淆。
- 包内容：版本为 `2026.08.08-experimental.1`，完整保留 SenseVoice、Silero VAD、GTCRN、DPDFNet 和中英标点资源，并新增 48 kHz 流式目标说话人提取模型及 SpeechBrain ECAPA-TDNN 条件编码模型；包内包含来源、许可证和 33 个文件的 SHA-256 清单。
- 构建验证：资源包使用 32 MB LZMA2 字典，解包后 33 个文件全部通过包内 SHA-256 校验；未压缩内容共 440865190 字节，7z 文件为 320227794 字节，SHA-256 为 `fa6e5a93e986ed5a3e61e826cfe9f34cf8f5ffcddbebd008b39b6a7c2485c32e`。
- 魔搭发布：文件已上传到 `LHTSTUDIO/KIGTTS_ASR_Resource`，提交为 `08e7be64f8dba17d56b98d27101702fbd892226f`；通过公开下载地址反向下载后，文件大小和 SHA-256 均与本地包一致。
- Hugging Face：同名文件已上传到 `LHT02/KIGTTS_ASR_Resource`，提交为 `03e3f4d0fdaed002dda382aba82037de9b1e3ba7`；HF LFS 元数据与公开地址反向下载均确认文件为 320227794 字节，SHA-256 与魔搭、本地包一致。
- 客户端地址：魔搭与 Hugging Face 默认下载地址均切换为 `kigtts-recognition-resources-experimental-20260808.7z`；读取设置时仅将仍等于 `20260505` 旧内置默认值的地址迁移到新版，自定义下载源保持不变。
- 资源复用：安装新版完整识别包后，客户端会直接发现包内标点、TSE 和 ECAPA 模型，不再要求用户重复下载；单独资源下载继续作为旧识别包兼容回退，随完整包安装的资源在设置中标明来源且不提供单独删除入口。若同时存在单独副本，删除后界面会立即回落到识别包内资源，不会短暂显示为未安装。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleRelease` 与 `:app:lintVitalRelease` 均通过，38 个测试套件共 141 项，0 失败、0 错误、0 跳过；新增默认地址、旧内置地址迁移和自定义地址保留测试。构建仅保留现有图片裁剪库、SDK XML 工具版本和 AEC3 依赖弃用警告。
- Release 安装：最终 `app-release.apk` 为 95922882 字节，SHA-256 `c18a977b5f1669e8df635569bd8de52a59e10b9e15e9bf650ed77b62bfcad0eb`；版本保持 `0.1.5 (6)`，APK Signature Scheme v2 校验通过，证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`，包内不含可选大型模型。
- 真机验证：最终 Release 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `LaunchState=COLD`、`TotalTime=721 ms`，应用进程正常驻留，日志未发现应用致命异常、`VerifyError`、`OutOfMemoryError` 或应用 ANR。构建前发现百度网盘重新占用 Gradle 缓存，已停止且未恢复。

## 2026-08-08 标点资源双源与 SenseVoice raw-text 本地原型

- 独立标点资源：将 sherpa-onnx 中英 CT-Transformer INT8 标点包以 `kigtts-punctuation-resources-20260808.tar.bz2` 发布到 `LHTSTUDIO/KIGTTS_ASR_Resource`（ModelScope）和 `LHT02/KIGTTS_ASR_Resource`（Hugging Face）。文件大小 `64717756` 字节，SHA-256 为 `c0d5aa5f8eeb686032345e180bedf39319dc2e0556781c6264bcadba8328a6e1`；两个公网直链均已反向下载并逐字节校验。
- 下载源设置：标点模型新增独立的 ModelScope / Hugging Face 地址和首选源持久化，不再直连 sherpa-onnx GitHub Release；设置页可进入“标点模型下载源”弹窗手动编辑和切换，空地址或旧上游内置地址会恢复到对应 KIGTTS 默认镜像，自定义地址保持不变。
- ModelScope 直链：`https://modelscope.cn/models/LHTSTUDIO/KIGTTS_ASR_Resource/resolve/master/kigtts-punctuation-resources-20260808.tar.bz2`。
- Hugging Face 直链：`https://huggingface.co/LHT02/KIGTTS_ASR_Resource/resolve/main/kigtts-punctuation-resources-20260808.tar.bz2`；上传提交为 `0781238743d52d4e613285830f0a6d77ef2f7484`。
- SenseVoice 调研：官方模型通过 14 号 `withitn` 与 15 号 `woitn` 条件 token 同时控制标点和书写规范化，并不存在可直接删除的独立 ITN 文件。对两行 560 维条件嵌入执行 `0.05-0.95` 插值扫描后，模型在约 `0.40-0.50` 之间按语句突变，无法稳定得到“有标点但数字不规范化”的单条件模型。
- 本地备用原型：在 `D:\KIGTTS_ASR_Resource\SenseVoiceRawPunct-20260808-local` 保存单模型双流方案。它依赖 sherpa-onnx 1.13.1+ 的 per-stream `use_itn`，同一模型实例批量解码 raw / ITN 两条流，最终文字取 raw 分支，仅按 token 时间戳转移同模型 ITN 分支的中文句读标点，不加载外部标点模型。
- 原型验证：用中文日期、时间、金额、电话、百分比和数量语音完成 5 组离线验证，均保留中文数字读法并恢复句读；样例包括“今天是二零二六年八月八日下午三点十五分。”和“温度是三十七点五度，电量还有百分之二十八。”。该结果证明推理路径可行，后续仍需真人、多口音、噪声和长句回归。
- 兼容边界：安卓端当前使用 `sherpa-onnx-1.12.38-noort.aar`，尚不支持 per-stream `use_itn`；本次只保留本地原型，不切换生产识别路径。接入前需升级并验证 1.13.1+ no-ORT AAR 的 Android ABI、内存和批推理延迟。
- 自动验证：`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 通过，38 个测试套件共 144 项，0 失败、0 错误、0 跳过；新增标点默认双源、旧 GitHub 地址迁移和自定义地址保留测试。构建仅出现项目既有图片裁剪库弃用警告。
- 新 Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，95939266 字节，SHA-256 `6898aef758d044076fce0e6587390738471871645c932c6727df6a99017432a6`；版本保持 `0.1.5 (6)`，APK Signature Scheme v2 校验通过，证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。按要求本次只打包，未安装到设备。

## 2026-08-08 SenseVoice 单模型 ITN 魔改备用包

- 方案收敛：按要求停止沿用双流转移标点思路，改为单个 SenseVoice、单次推理；标点继续由 SenseVoice 自身输出，不加载独立标点模型，也不使用外部标点/ITN 后处理拼接。
- 模型结构：在官方 SenseVoice 编码器输出后增加仅由 `withitn` 条件启用的 `512 -> 128 -> 512` 残差适配器，共 `132736` 个可训练参数；官方底模全部冻结，`woitn` 路径完全绕过适配器。
- 本地留存：完整实验包位于 `D:\KIGTTS_ASR_Resource\SenseVoicePunctOnlyITN-20260808-local`；最终完整权重为 `model.punct-only-itn.pt`，SHA-256 `ec87b88e0c2925c3b32ba47f874816a6a8bcadba2f9dae63c51f8b2cd0b67277`；独立适配器为 `punct_only_itn_adapter.pt`，SHA-256 `abc8f85d48252c748e938346dd67ce878a4ebf76da21735b39ffece63443017d`。
- 隔离验证：逐张量比较确认官方底模既有参数变化为 `0`；27 条验证音频在关闭 ITN 时与官方底模逐条一致。开启后，22 条合成验证中 21 条完全一致，唯一差异为句末感叹号变句号；5 条真人证明音频中 3 条完全一致。
- 已知限制：真人日期样本缺少一个中间逗号；口语金额“四毛五”当前识别为“四五”。本轮保留真实结果，不通过外挂标点模型掩盖。当前仅为 PyTorch/FunASR 本地备用原型，尚未完成 Android 所需 ONNX 图内实现、量化与真机性能验证，因此未切换生产识别路径。

## 2026-08-08 SenseVoice 单模型 ITN 第四版训练

- 第四版结构：在冻结的第三版 `512 -> 128 -> 512` 编码器残差适配器后增加 `LayerNorm -> 512 -> 64 -> 25055` 低秩 CTC 修正层；官方底模与第三版适配器均冻结，本轮仅训练 `1637376` 个 CTC 修正参数，总附加参数为 `1770112`。
- 语言隔离：第四版只在显式 `language=zh` 且开启 `withitn` 时启用两级修正；`woitn`、自动语言模式、粤语、英语、日语和韩语直接走官方 SenseVoice 路径，避免中文训练影响底模其它语言。
- 训练选模：使用 139 条聚焦训练记录训练 30 轮并保留全部检查点；第 17 轮起真实证明集达到 5/5，第 27 轮首次在同一进程达到合成 22/22。因第 27 轮全新进程仍处于感叹号/句号边界，最终选择第 30 轮作为稳定权重。
- 最终验证：第 30 轮在全新进程中真实证明集 5/5、未参与训练的合成集 22/22；日期中间逗号和口语金额“四毛五”均正确。关闭 ITN 后 27/27 与官方底模逐条一致，官方底模原始张量变化为 0；粤/英/日/韩各一条旁路样本与底模 4/4 一致。
- 本地权重：完整模型 `D:\KIGTTS_ASR_Resource\SenseVoicePunctOnlyITN-20260808-local\model.punct-only-itn-v4.pt`，SHA-256 `f2ba2de868962b30b83630563c9b601056e1fe3d6a8d584ee3223a12729f3506`；适配器 `punct_only_itn_v4_adapter.pt`，SHA-256 `91840492a9e35a816f893d53b51ecc409aed460380680c009f91a6e5883e9584`。
- 交付边界：仍为 PyTorch/FunASR 本地备用模型，不加载独立标点模型，尚未导出 Android/sherpa-onnx 可用的 ONNX 图，也未切换生产识别路径。

## 2026-08-08 Android 实验识别完整资源包 V4 与语音烟测

- V4 移动端模型：第四版已导出为 sherpa-onnx 可直接加载的 INT8 ONNX，文件为 `D:\KIGTTS_ASR_Resource\SenseVoicePunctOnlyITN-20260808-local\onnx-v4\model.int8.onnx`，大小 `241025533` 字节，SHA-256 `6ad1aaceb8a98a6f3b93b23874acb7d2038caa568dc6024bf7a29542df6b7ccb`。桌面 sherpa-onnx 验证覆盖真人 5/5、合成 22/22、关闭 ITN 与旧模型一致 27/27，以及粤/英/日/韩 4/4；125.088 秒音频总推理 4.338 秒，RTF 约 `0.0347`。
- 单包交付：新增 `kigtts-recognition-resources-experimental-v4-20260808.7z`，将 SenseVoice V4、Silero VAD、中英文标点、GTCRN、DPDFNet2、DPDFNet4、TSE 神经分离器、TSE 外部权重、ECAPA-TDNN、许可证和说明统一放入一个安装包，不再要求用户分别下载语音识别组件。
- 包校验：归档使用 Android 安全的非固实 7z、16 MiB LZMA2 字典；压缩包大小 `322796863` 字节，SHA-256 `4e7e47000bd19c9c0591407f65f69dca424c680ff1abf25f05033038d2b0571a`。`7z t` 与解包后的 `SHA256SUMS.txt` 均通过；manifest 使用 `experimental-full-v4` 配置并列出 14 个核心文件的大小与 SHA-256。
- 双源发布：ModelScope `LHTSTUDIO/KIGTTS_ASR_Resource` 与 Hugging Face `LHT02/KIGTTS_ASR_Resource` 已上传同名完整包。HF 提交为 `d888eca721bcd8ce8b2c25b7bfc832aa4a4c56d0`；两个公开直链均返回 `322796863` 字节，远端关联哈希均为本地 SHA-256。
- 客户端入口：设置页只保留“语音识别资源包”一个下载/导入入口，说明明确一个资源包包含识别、分段、标点、降噪和实验性说话人分离；标点与神经分离区域仅显示随识别包安装的状态，缺失时引导重新安装完整包，不再显示独立下载或下载源入口。
- 地址迁移：ModelScope 与 Hugging Face 默认地址统一切换到 V4 完整包；仍使用旧内置默认地址或早期实验地址的用户会自动迁移，自定义地址保持不变。ITN 说明同步注明实验 V4 会保留中文数字原始读法并输出标点。
- 安装防护：识别资源校验必须找到真实 SenseVoice ONNX 与 `tokens.txt`，不再把标点、VAD 等辅助 ONNX 误判为识别模型；完整 V4 还必须包含非空 `requiredFiles` 清单并逐项核对路径、大小和 SHA-256，缺组件或损坏时不会替换当前可用资源。
- 并发修复：`RealtimeHostService.ensureController()` 改为同步创建，修复冷启动时多个调用同时建立控制器、重复加载 ASR 和内存陡增的问题。完整包冷启动日志只出现一次 `ASR init` 和一次 `ASR loaded`。
- 性能保护：Redmi K70 实测 TSE 神经分离 RTF 为 `4.208-4.604`，不满足实时处理。自动后端现在先用 40 ms 静音片段做短性能探针，RTF 超过 `0.85` 时立即回退轻量筛选，避免第一句话被完整神经推理阻塞；显式选择神经分离仍允许强制使用。
- 实机烟测：小米设备 `cfc8ef16` 上依次通过 V4 ASR、中文标点、GTCRN 离线/流式、DPDFNet2/4 流式、Silero VAD、192 维 ECAPA 和 TSE 前向推理。日期样本输出为“今天是二零二六年八月八日，下午三点十五分。”，数字未被转换为阿拉伯数字；烟测期间无 `FATAL EXCEPTION`。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleRelease :app:lintVitalRelease` 通过，38 个测试套件共 148 项，0 失败、0 错误、0 跳过；Debug APK 已保留数据覆盖安装。完整资源冷启动 `TotalTime=2073 ms`，PSS 约 `608 MiB`、RSS 约 `738 MiB`，未发现应用崩溃、ANR 或内存溢出。Release APK 为 `95922882` 字节，SHA-256 `0030920ec90d5de7295bd369d25b1ad2d7c51779fe67b3955abaaafbf49b5367`；版本保持 `0.1.5 (6)`。

## 2026-08-09 Android 实验识别实时链路回归修复

- 基线核对：`origin/main` 与本地 `HEAD` 均为 `592cef236`，兼容模式基线未发生远端漂移；本轮问题来自未提交的实验识别实时接入代码。
- 根因修复：实验灵敏度不再同比例压低结束静音门限，避免环境底噪让窗口持续不结句；经典 VAD 超过 12 秒时改为提交当前分段而非直接清空，持续讲话不再整段丢失。
- 尾段收尾：正常停止、开关模式关闭和 PTT 松手会冲刷 Silero/经典 VAD 的待处理音频并等待最终识别任务完成；切换 ITN、替换模型或销毁控制器时才强制取消任务，兼顾结果完整性与模型释放安全。
- 并发保护：流式预览与最终识别统一经同一 ASR 解码互斥锁串行执行，避免同一 SenseVoice 实例被并发解码；已排队任务区分“等待完成”和“取消并等待”两种收尾语义。
- PTT 提交：确认模式先保留当前流式预览，再请求录音窗口冲刷并等待最终识别，完成后才执行上屏、转输入框或取消；松手后到最终结果返回期间仍接收识别回调。预览和最终句仅句读不同时优先采用带标点结果，重叠文本只拼接一次。
- 标点默认：ITN 继续默认关闭，保留中文数字原始读法；“自动添加标点”对新配置恢复为默认开启，完整 V4 资源包继续提供独立标点模型。外置标点恢复器缺失或加载失败时保留识别器原始句读，不再先删除后返回无标点文本。
- 自动验证：`:app:testDebugUnitTest`、`:app:assembleDebug`、`:app:assembleRelease`、`:app:lintVitalRelease` 与 `git diff --check` 通过，39 个测试套件共 154 项，0 失败、0 错误、0 跳过；新增实验静音门限、尾段提交判定、标点默认值、标点恢复器降级以及 PTT 句读/重叠合并测试。
- 构建产物：Debug APK 为 `100751618` 字节，SHA-256 `8405f34e0bb37722818ccf27378503360abb866dd0c5fc929ef5901e2c7d171c`；Release APK 为 `95922882` 字节，SHA-256 `67d8604ac860b002b0cf49f9811cf19108e1c66fa65cfffa8332f3dec0e6561e`，版本保持 `0.1.5 (6)`。
- 真机边界：构建前小米设备 `cfc8ef16` 可见，覆盖安装时设备从 ADB 断开，随后 `adb devices` 为空；本轮新包尚未完成真实麦克风、PTT 和 V4 全组件复测，不能把此前资源前向烟测视为本轮实时链路验证。

## 2026-08-09 SenseVoice V5 数字识别与实时前端优化

- 数字底模：在冻结官方 SenseVoice 主体的前提下继续训练中文 `withitn` 图内修正层，选用 V5 第 4 轮检查点；Android 使用的 INT8 ONNX 为 `240893880` 字节，SHA-256 `2b2061eaf2f5163ec680474f97474240cb447a30b2a3e7193d4d9557dbc6e114`。关闭 ITN 时仍完全旁路修正层，粤语、英语、日语和韩语继续走官方模型路径。
- 离线回归：sherpa-onnx 原生解码通过真人证明集 `5/5`、旧合成集 `22/22`、大数字内容正确率 `28/28`、关闭 ITN 与旧 INT8 模型一致 `55/55`、粤/英/日/韩旁路 `4/4`；大数字标点完全一致 `27/28`，唯一差异是最长金额在“亿”后多一个逗号。桌面双线程总 RTF 约 `0.0361`。
- 完整资源包：发布 `kigtts-recognition-resources-experimental-v5-20260809.7z`，大小 `322746858` 字节，SHA-256 `ed2352c62c5e7c5cabf4ec6f8f829c970a305e4290314db957775e0a18708f9e`；14 个 manifest 必需文件的大小和 SHA-256、`7z t` 及完整解包校验均通过。ModelScope 上传提交为 `4db62281bab8ee8c53817d9983b0e12de8779ff5`，Hugging Face 提交为 `e278288898cf6a240f85363f8d4031b8cdc807bb`；HF LFS 元数据与本地一致，ModelScope 公网文件已完整反向下载并再次得到同一 SHA-256。
- 下载迁移：实验识别默认 ModelScope / Hugging Face 地址切换到 V5 完整包；仍使用 V4 内置默认地址的用户会自动迁移，自定义下载地址保持不变。
- 首字保护：Silero 预录由 `100 ms` 调整为 `240 ms`，旧默认值自动迁移，降低声母在 VAD 起点前被截断的概率；每次接收音频后先更新预录再排空分段，避免快速结句时前缀尚未进入缓存。
- 首字去重：新增线性时间的精确采样重叠检测，只移除预录尾部与 VAD 分段头部实际重复的音频，既保留起声前上下文，也不把同一首音节送入模型两次；PTT 流式预览与最终句拼接同时改为忽略空白和句读计算重叠，并保留英文单词边界。
- 嘈杂环境默认值：实验模式默认使用流式 GTCRN，旧 RNNoise / Speex 与神经语音增强改为设置、持久化和运行时三层互斥，避免重复降噪破坏轻声与头壳内语音；AUTO 神经说话人分离只在轻量声纹分数处于模糊区间时启用，显式“优先神经分离”仍可强制运行。
- 目标说话人稳定性：多窗口判断要求持续命中，或单窗口达到更高置信度，拒绝孤立的中等分数尖峰；关闭“仅响应指定说话人”时仍完整保留环境字幕接口。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，40 个测试套件共 162 项，0 失败、0 错误、0 跳过；新增音频边界、首字重叠、句读边界拼接、AUTO 神经启用策略、持续声纹证据和 V4 地址迁移测试。
- Release APK：`D:\KGTTS\android-app\app\build\outputs\apk\release\app-release.apk`，`95939266` 字节，SHA-256 `53e6c3478ec89f85a01fdf67d746fba6285f4f1accd2070db21197bfb49e45a1`；版本保持 `0.1.5 (6)`，APK Signature Scheme v2 校验通过，证书 SHA-256 为 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197`。
- 真机验证：Release 已通过 `adb install -r` 保留数据覆盖安装到小米设备 `cfc8ef16`；冷启动 `LaunchState=COLD`、`TotalTime=682 ms`，前台 Activity 与进程正常，日志未发现应用致命异常。当前未提供真实嘈杂环境或头壳内录音，因此本轮只确认管线、模型离线集与启动稳定性，现场识别效果仍需单独采样验收。

## 2026-08-09 本人声纹零调参管线与 V6 完整资源包

- 用户流程：语音识别设置只保留“本人声纹”卡片、“仅响应我的声音”开关和录制/重录/删除操作，不再向用户展示实验模式、灵敏度、后端或验证阈值。关闭开关时完全绕过声纹筛选，继续识别周围所有人的声音，作为现场字幕与辅助收音接口。
- 自动判定：CAMPPlus 作为低延迟初筛，明确结果直接通过或拒绝；灰区才调用中文 ERes2NetV2 确认模型，重噪声候选再按设备性能决定是否尝试原 Conv-TasNet 目标说话人分离。确认模型缺失、推理失败或分离结果不合格时自动回退，不阻断普通识别。
- 自适应阈值：三段本人声纹的两两相似度用于估计当前用户阈值，并限制在保守区间；确认模型不能救回 CAMPPlus 明确拒绝的语音。本人验证通过后，从该次真实确认开始的 3 秒内，不超过 1.1 秒的连续短句可复用会话结果；复用不会续期，兼顾轻声短答与防止旁人长期搭便车。
- 录制质量：每段声纹检查有效语音时长、活动比例和削波，自动裁掉首尾静音；正式收音和声纹录制优先尝试 `UNPROCESSED` / `VOICE_RECOGNITION`，不可用时回退 `MIC`，并在 Android 10+ 请求朝向用户的麦克风方向。采样结束到多套特征生成完成之间，录制弹窗会切换为“正在处理声纹…”，避免停在“剩余 0 秒”造成卡死误解。
- 数据兼容：声纹记录新增可选 ERes2NetV2 确认向量，旧 CAMPPlus/ECAPA 样本继续可读；新录制同时保存 CAMPPlus、ERes2NetV2，以及资源可用时的 ECAPA 条件特征。大模型保持在语音识别资源包中，不增加 APK 体积。
- V6 资源：发布 `kigtts-recognition-resources-experimental-v6-20260809.7z`，大小 `388644391` 字节，SHA-256 `1e386604c7150e0cf9b497eabb96d5fbfc0051138e8360349e824456863c15cf`。包为非固实 7z、32 MiB LZMA2 字典，包含 15 个带大小与 SHA-256 的运行时必需文件；`7z t`、完整重解压和逐项哈希均通过。
- 双源发布：V6 已上传 ModelScope `LHTSTUDIO/KIGTTS_ASR_Resource` 与 Hugging Face `LHT02/KIGTTS_ASR_Resource`，HF 提交为 `3e091bd45bcd2a339ea7f47920da30ea526f503e`；两个公开地址均返回 `388644391` 字节和本地 SHA-256。客户端内置地址切换到 V6，旧 V5 内置地址自动迁移，自定义下载源保持不变。
- 模型验证：桌面 sherpa-onnx 1.13.4 上 ERes2NetV2 输出 192 维，5.592 秒音频推理约 `0.367 s`、RTF `0.066`；同音频降到 25% 音量相似度 `0.9974`，加入约 20 dB 噪声后 `0.9281`，与测试集中其他说话人的相似度为 `0.0466-0.2501`。
- 真机前向：Redmi K70 上 Debug 语音烟测通过，ERes2NetV2 输出 192 维并耗时约 `2670 ms`；同一轮 ASR、中文标点、四套降噪、Silero VAD、ECAPA 和 TSE 均通过，TSE RTF 为 `4.314`，会被自动性能保护停用。真机结果进一步确认 ERes2NetV2 必须只用于灰区确认。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，41 个测试套件共 171 项，0 失败、0 错误、0 跳过；新增自动声纹决策、会话复用、录制质量、确认向量序列化、V5 地址迁移和 Android ERes2NetV2 前向测试。
- Release 安装：最终 `app-release.apk` 为 `95939446` 字节，SHA-256 `dbd3dd69adad003d40038e93c9ee93409eedd00e5c5c453fef61b57041d56308`；版本保持 `0.1.5 (6)`，APK Signature Scheme v2 与证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 验证通过，包内未包含可选大型模型。Release 已保留数据覆盖安装到 Redmi K70 `cfc8ef16`，冷启动 `TotalTime=1478 ms`，主 Activity 和进程正常，未发现应用致命异常、ANR、`VerifyError` 或内存溢出。

## 2026-08-09 本人声纹固定文案过拟合修复

- 根因：自适应门限仅根据三段本人正样本两两相似度计算，却允许把 CAMPPlus 主门限从默认 `0.5` 上调到 `0.68`、ERes2NetV2 确认门限从 `0.60` 上调到 `0.74`。缺少其他说话人负样本时，这种上调没有统计依据，并会偏向录制时的固定朗读内容。
- 门限修复：本人正样本现在只用于在跨文本、跨姿态差异较大时下调门限，不再上调用户默认门限；现有声纹无需重新录制，控制器重建声纹状态时会直接按新策略重新计算。
- 句段修复：Silero/经典 VAD 输出的 `RecognitionSegment` 已是完成句段，声纹门不再要求累计到 `2.6 s` 才进行最终判定。普通短句会在当前句内完成 CAMPPlus/ERes2NetV2 判断，不再等待无关的下一句话后超时丢弃。
- 兼容模式：移除已验证会话对非复用长句的错误提前返回；不超过 `1.1 s` 的连续短句仍按既有 3 秒策略复用，较长句则正常重新验证。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug` 与 `git diff --check` 通过；新增“正样本不得抬高门限”和“样本自然漂移允许下调门限”回归测试。构建仅出现项目既有图片裁剪库弃用警告。
- 真机边界：验证时 `adb devices` 无可用设备，尚未取得修复后的真实声纹相似度日志，也未完成不同文本、轻声、头壳内说话和旁人干扰测试。

## 2026-08-09 Android 实时识别静默模拟音频测试

- 管线复用：将 `AudioRecord` 分块读取与后续实时识别处理拆开，真实麦克风和模拟 PCM 现在共用降噪、语音增强、VAD、流式预览、声纹门控、SenseVoice、标点和结果回调逻辑，避免烟测只验证孤立模型前向。
- 安全边界：新增 `RealtimeController.runSimulatedAudio` 内部接口，仅允许在 ASR 已加载且麦克风未运行时调用；输入自动重采样到 16 kHz，测试结束等待全部异步识别任务完成并释放测试控制器资源。
- 静默入口：Debug 清单新增 `RUN_REALTIME_PIPELINE_SMOKE` 广播接收器，既不创建 `AudioRecord`，也不加载或调用播放链路；未提供 WAV 时直接使用当前音色包在内存中合成测试语句，手机不会发声。
- 模拟场景：固定生成 clean、24% 轻声、双级低通闷声、7 dB SNR 噪声、多抽头混响和 5 dB SNR 闷声噪声六类输入，可选择兼容/实验/当前模式并输出逐场景文本、声纹事件、电平、分块数和耗时报告。
- 外部样本：支持应用缓存目录中的单/双声道 PCM16 WAV，双声道自动下混；可选按当前声纹配置启用验证。合成语音可验证“非本人应拒绝”，本人跨文本通过率仍需未参与录制的本人样本。
- 文档：`android-app/EXPERIMENTAL_RECOGNITION.md` 增加 ADB 命令、参数、报告路径、覆盖范围及实体麦克风和真实声场无法由模拟输入验证的边界说明。
- 自动验证：新增模拟场景确定性、边界静音、轻声能量和立体声 WAV 下混测试；`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，42 个测试套件共 175 项，0 失败、0 错误、0 跳过。版本保持 `0.1.5 (6)`。
- 真机静默测试：Redmi K70 `cfc8ef16` 上首先移除缓存 WAV，确认当前音色“铃”可在内存中生成 22050 Hz 测试语音且手机不发声；自动重采样后实验模式识别为“今天是二零二六年八月九日下午三点十五分。”并返回 `PASS`。随后使用固定测试 WAV 运行兼容/实验双模式与六类场景共 12 个组合，全部输出正确文本并返回 `PASS`，整轮约 29 秒。测试结束后已保留数据覆盖恢复安装 `0.1.5 (6)` Release。

## 2026-08-10 合成音色声纹注册与冒认矩阵

- 外部音源：新增 Debug 外部声纹清单读取器和桌面生成脚本。实测生成 Microsoft Edge 神经语音六组音色，每组包含三句注册语音和一条未见测试句；另使用官方 `openbmb/VoxCPM-0.5B` 在 CPU 上生成 4.4 秒测试语音。全部转换并校验为 16 kHz、单声道、PCM16 WAV，生成音频与模型缓存不进入仓库。
- 注册扩展：`SyntheticVoiceprintMatrixReceiver` 可使用应用内音色或任一微软音色作为临时注册声纹；外部音色同样走 CAMPPlus、可用时的 ECAPA、正式 VAD/降噪/语音增强、声纹门控与 SenseVoice。每个独立案例重置短时已验证会话，模拟音频期间同步收集声纹和最终文本回调，避免不同案例串案；用户声纹、设置和模型目录不会被写入。
- 场景设计：注册音色使用 clean、quiet、muffled、noisy、reverberant、headshell_noisy 六种场景；其他微软/VoxCPM 音色使用 clean、noisy、headshell_noisy，其他应用内音色使用 clean。报告新增音源标记及全部 `相似度:pass/reject` 决策。
- 实验模式真机结果（Redmi K70 `cfc8ef16`）：

| 注册音色 | 本人通过 | 其他音色拒绝 | 冒认 |
|---|---:|---:|---:|
| 铃（应用内 Piper） | 6/6 | 16/29（55.17%） | 13 |
| 微软晓晓 | 6/6 | 12/27（44.44%） | 15 |
| 微软晓伊 | 6/6 | 9/27（33.33%） | 18 |
| 微软云希 | 6/6 | 22/27（81.48%） | 5 |
| 微软云健 | 6/6 | 23/27（85.19%） | 4 |
| 微软晓北（东北口音） | 5/6 | 19/27（70.37%） | 8 |
| 微软晓妮（陕西口音） | 6/6 | 13/27（48.15%） | 14 |

- 结果判断：严格标准要求本人全部通过且其他音色零冒认，因此所有轮次仍为 `FAIL`。男声注册的区分度明显高于合成女声；晓北仅在 `headshell_noisy` 场景漏掉本人。合成音色之间可能共享模型声学特征，不能把数值直接外推到真人，但结果确认当前实验前端仍需加强近似女声区分。
- 兼容基线：以铃、微软晓晓和微软云健复核时，旧 Sherpa 管理器对合成冒认样本的拒绝均为 0；它按三条注册样本中的最佳单条匹配，保持旧版兼容语义。实验模式通过多窗口持续证据和自适应门控取得显著改善，本轮不修改兼容模式生产行为。
- 可复现入口：Debug 清单注册 `RUN_SYNTHETIC_VOICEPRINT_MATRIX`；`android-app/EXPERIMENTAL_RECOGNITION.md` 已记录生成、ADB 推送、注册音色选择和报告读取命令。Microsoft/VoxCPM 仅作为本地测试夹具来源，不新增生产网络依赖。
- 自动验证：`python -m py_compile tools/generate_voiceprint_matrix_audio.py`、`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过；42 个单元测试套件共 175 项，0 失败、0 错误、0 跳过。构建仅保留既有图片裁剪库弃用警告和 Android SDK XML 工具版本提示。
- Release 恢复：最终 `app-release.apk` 为 `95939446` 字节，SHA-256 `2b7e93bca6c5da8f5b1cd911c242299dd01b4dc4f34c533bf7fe74574936ac6e`；APK Signature Scheme v2 与证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 验证通过。已保留数据覆盖安装回 Redmi K70 `cfc8ef16`，设备为不可调试的 `0.1.5 (6)` Release，进程和前台启动正常，日志未发现应用致命异常、ANR、`VerifyError` 或内存溢出。

## 2026-08-10 声纹容错档位与固定自动标点

- 声纹容错：在“本人声纹”中新增六档吸附滑条，依次为“智能、宽松、较宽、中等、较严、严格”，默认使用“智能”。每档提供面向实际收音环境的说明，滑动跨档时保留震动反馈。
- 自适应边界：“智能”可根据三段声纹在较大范围内向宽松方向适配；手动档位只允许围绕用户选择做小幅下调，不会把“较严/严格”自动放宽成宽松档。旧浮点门限接口继续映射到最接近的手动档位，新配置单独持久化档位索引。
- 标点策略：移除设置页“自动添加标点”开关，最终识别结果始终补充标点；流式输入预览仍不运行外置标点模型。旧配置键和调用接口仅用于备份兼容，读取或写入关闭值时仍固定为开启。
- ITN 语义：实验 V6 开启 ITN 时进入 V5 延续的中文数字增强分支，用于改善大数字、日期和金额识别，并保留中文数字读法；关闭时完全旁路该分支，使用 SenseVoice 原始听写。开启且模型声明内建句读时直接保留模型标点，关闭时使用资源包内独立标点模型恢复最终句读。
- V4 边界：本轮实机保留的识别资源仍是 V4。V4 对“千/亿”和念错单位后的非标准数字顺序更脆弱，不能作为 V6 数字增强效果的回归结论；V5 已针对含“千”的大数字补训，V6 沿用同一数字模型，需换装 V6 资源后再做真人对照。
- 实机界面：Redmi K70 Debug 上确认标点开关已消失，ITN 说明与固定标点行为一致；六个容错标签完整显示，默认“智能”及说明没有重叠或裁剪。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过；42 个单元测试套件共 177 项，0 失败、0 错误、0 跳过。构建仅保留既有图片裁剪库弃用警告。
- Release 恢复：最终 `app-release.apk` 为 `95939446` 字节，SHA-256 `eb4993cc518dd5479d7896753586539d325deda39c4ff48b12cc75c297739d44`；APK Signature Scheme v2 与证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 验证通过。已保留数据覆盖安装回 Redmi K70，设备为不可调试的 `0.1.5 (6)` Release，主 Activity 与进程正常，启动日志未发现应用致命异常。

## 2026-08-10 中文 Piper 单模型外语兼容朗读

- 单模型路由：中文 eSpeak 语音包现在按文字脚本拆分输入，拉丁字母、日文假名和韩文谚文分别使用 `en-us`、`ja`、`ko` 规则生成音素，仍只调用当前中文 Piper 声学模型，不加载第二套 TTS 模型。
- 日文读法：从 MeCab IPADIC 2.7.0-20070801 提取含汉字词形的最低成本读音，生成约 1.34 MB 的 XZ 精简表；首次遇到日文汉字时延迟解压为字节索引并使用最长匹配转换为假名，不加载完整分词器和 13.3 MB Kuromoji JAR。纯假名路径不初始化词典。
- 音色兼容：优先原样保留当前 Piper 音色表已经支持的外语 IPA，包括重音、长音、浊辅音和组合附标；仅对音色表真正缺失的音素使用中文近似音回退，避免把完整音节压短或静默跳过。目标是在同一音色下保持内容可辨认，不追求标准母语口音。
- 识别边界：仅包含汉字而没有假名的短语无法可靠自动判断是中文还是日文，默认继续按中文朗读；含假名的连续汉字会一并按日文处理。非中文 Piper 语音包保持原有音素路径不变。
- 真机静默合成：Redmi K70 使用现有“流萤(3)”中文 Piper 音色分别生成英语、日语和韩语 WAV，三项均为有限非空波形。保留完整 IPA 后，同一英语样例从 `22528` 样本 / `1.02 秒` 恢复到 `54272` 样本 / `2.46 秒`，确认第一版音节过短问题来自错误清理模型已支持的 IPA。

## 2026-08-10 语音识别按钮操作模式整合

- 单一模式：将原“按住说话”和“按住说话确认模式”两个开关合并为“语音识别按钮操作模式”，三档为“点击开关”“按住说话”“按住并确认”，设置页会随当前档位显示对应操作说明。
- 入口统一：设置页、主界面顶部展开状态面板和悬浮窗顶部展开状态面板使用同一模式选择器；切换为任一按住模式时会停止正在运行的持续识别，避免录音状态跨模式残留。
- 交互反馈：主界面顶部面板的输入设备、输出设备和语音按钮模式在展开与选择时提供震动；悬浮窗状态面板的展开/收起、设备下拉、语音按钮模式下拉及菜单项选择使用同一轻触反馈。
- 旧配置迁移：新增单一整型配置键，同时继续写入旧布尔键以兼容备份和旧逻辑；未保存新键的用户会按原两个开关组合无损映射到三档模式。
- 最终验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleRelease :app:lintVitalRelease` 全部通过；Release APK 为 `97,298,870` 字节，SHA-256 为 `E70BAA48B188B9C89A71A718BA367C839341734D50D22E87A24BBB27B2900708`，APK Signature Scheme v2 与正式证书校验通过。已覆盖安装到 Redmi K70，确认版本 `0.1.5 (6)`、未包含 `DEBUGGABLE` 标记、主进程正常启动且未发现应用崩溃或 ANR。

## 2026-08-10 中文 Piper 路径回归隔离

- 根因定位：真机音素日志确认 eSpeak `cmn` 对部分未命中汉字会退回按 Unicode/英文字母拼读；“花儿”和“鸟儿”位置实际混入了 `you/one/five` 等英文音素，因此出现中文口胡。这不是外语分段造成的停顿问题。
- 中文前端：基于 pypinyin `0.55.0` 的单字与词组数据生成压缩带声调拼音索引，中文先转换为声调数字拼音，再交给 eSpeak `cmn-Latn-pinyin`；词组优先、单字回退，并修正“背着”的当前语境读音。词典加载失败时保留原 eSpeak 汉字路径。
- 资源体积：中文索引包含 `89,035` 条读音，XZ 压缩后为 `532,756` 字节，SHA-256 为 `204603A4A381A7BE3F67AC2CCDCB5C95F46CB46467F3BB120053C1C58476F827`；源码数据与许可证哈希均由生成脚本校验。
- 回归防护：新增中文日期、大数字、标点、阿拉伯数字及“太阳当空照”复现句的路由、拼音转换、音素回退和真机静默合成样例；英语、日语、韩语仍使用各自独立前端。
- 外语清晰度：中文 Piper 的英语与日语分段新增中文谐音前端。英语先从 eSpeak IPA 按音节量化为最接近的普通话声韵母，日语先从假名映射到普通话拼音；二者最终通过 `cmn-Latn-pinyin` 生成中文模型熟悉的音素，转换失败时自动回退原 IPA 路径。韩语暂时保持原路径。
- 谐音细化：补齐 IPA 小写脚本 `ɡ` 的辅音回退，避免英语尾辅音被静默丢失；对“日本語”等常见日文词组使用整词中文近似，避免拨音被拆成额外音节。Debug 音频烟测会同时记录外语原始音素、谐音拼音及最终波形指标，便于继续按试听结果迭代。
- 真机结果：Redmi K70 使用“流萤(3)”生成英语 `he lou / wo er de / di si / yi zi / ki ge te si` 与日语 `ni hong guo / kong ni qi wa / a li ga tuo...` 谐音样本，英语为 `46,592` 个采样、日语为 `78,080` 个采样，中文复现句仍完整生成并且音素中未再出现 Unicode 英文回退，整轮返回 `PASS`。
- 最终验证：完整 `:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 通过，Android 真机仪器测试 3/3 通过；Release APK 为 `97,849,270` 字节，SHA-256 `54C8713E71B251EEA3AAAEF2C3E55B04BD30C308EC82660B5B8F42F364557A0C`，APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。Release 已覆盖安装到 Redmi K70，确认 `0.1.5 (6)`、非 debuggable、主 Activity 与进程正常，启动日志未发现应用致命异常或 ANR。

## 2026-08-10 麦克风与按住确认手势震动反馈

- 主界面：便捷字幕麦克风键在点击开关、PTT 按下与松手时提供震动；按住确认模式拖入“上屏”“输入文本框”“取消”区域，以及从其他区域返回默认上屏区域时，仅在目标实际变化时震动一次。
- 悬浮窗：折叠、展开和迷你状态共用的麦克风键补齐松手提交与拖拽跨区反馈；确认模式不再把“输入文本框”目标锁死，手指可在输入、取消和上屏区域之间往返选择。
- 设置一致性：所有新增反馈继续遵循“震动反馈”总开关，关闭后主界面与悬浮窗均不触发按键震动。
- 真机反馈：Redmi K70 切换到“按住并确认”后，以麦克风键拖入输入区并松手，系统振动记录捕获到按下、跨区和提交三次独立的 KIGTTS 触觉事件；测试结束后已将用户操作模式恢复为原来的“点击开关”。

## 2026-08-10 中文 Piper 韩语谐音朗读

- 韩语前端：中文 Piper 音色下的韩文分段改为中文谐音拼音，再交给 `cmn-Latn-pinyin` 生成中文模型熟悉的音素；非中文音色不受影响，转换为空或失败时继续回退原韩语 eSpeak 路径。
- 读法策略：常见问候、致谢、道歉和句尾使用整词谐音；未收录韩文按 Hangul 声母、元音和收音实时拆解，保证任意韩文不会因为词表未覆盖而退化成静音或极短音节。
- 真机结果：“안녕하세요 한국어입니다”转换为 `a1 ni1 ha1 sai1 you1 han2 gu1 ge1 yi1 mi1 da1`；“流萤(3)”生成长度由原路径的 `20,480` 采样、约 `0.93 秒` 增加到 `39,680` 采样、约 `1.80 秒`，四语音频烟测返回 `PASS`。试听文件 `korean-chinese-homophone.wav` 为 `79,404` 字节，SHA-256 `2A85FED160DF777F12FADE4C1C80FFBF718D41003D84F1BCEC9E3B5DBD60F570`。
- 最终验证：`:app:testDebugUnitTest :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 通过，46 个单元测试套件共 193 项全部通过，中文 Piper 多语真机仪器测试 3/3 通过。Release APK 为 `97,849,270` 字节，SHA-256 `416F03F213300DCB525E8334F6F2B03DBCFFC252E9C462C2AFB628E73AC8E719`，APK Signature Scheme v2 与正式证书校验通过；已覆盖安装到 Redmi K70，确认 `0.1.5 (6)`、非 debuggable、主进程正常且启动日志无应用致命异常或 ANR。

## 2026-08-10 语音识别语言选择与预设占位符调整

- 语言选择：在“设置 - 语音识别 - 识别行为”新增“识别语言”下拉选项，提供中文（普通话）、自动判断、中文（粤语）、英语、日语和韩语；默认使用中文（普通话），非法或旧配置值同样回退到普通话。
- 运行时切换：识别语言通过 DataStore 持久化并显式传入 sherpa-onnx SenseVoice。识别正在运行时会暂停收音、重建 ASR 后恢复原状态，不重载 TTS；兼容与实验管线共用同一语言设置。
- 交互一致性：下拉选中项使用现有设置控件样式和触感反馈，并根据所选语言提示单语言限制或自动判断用途。
- 预设文案：主界面与悬浮窗回退数据中的内置快捷文本将连续 `XX` 统一写为 `X X`，同时在中文边界保留空格，避免英文前端把占位符按罗马数字读取；未新增或修改任何 `XX` 专项处理逻辑。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，48 个测试套件共 198 项，0 失败、0 错误、0 跳过；主源码确认无连续 `XX`。版本保持 `0.1.5 (6)`，Release APK 为 `97,849,270` 字节，SHA-256 `C065F38E28154A037192E070BDA3598675C88E8C27730BA9B8FC527D6A4DB241`。

## 2026-08-10 便捷字幕清空占位文本与启动显示策略

- 设置入口：在“设置 - 系统 - 便捷字幕显示”新增“清空状态占位文本”，点击后通过多行弹窗编辑；空文本不可保存，内容统一换行格式并限制为 200 个字符。
- 启动策略：新增“启动时保持上次触发的文本”开关并默认关闭。关闭时主界面、实时后台服务和悬浮窗首次启动统一显示清空占位文本；开启后恢复旧版行为，读取快捷字幕配置中上次显示的文本。
- 运行时同步：修改占位文本时，如果当前正显示旧占位文本，会立即替换为新内容并同步快捷字幕配置、实时通知、蓝牙媒体标题和悬浮窗；正在显示的正常字幕不会被设置操作打断。
- 显示样式：主界面按当前自定义占位文本判断弱化显示，不再依赖写死常量；清空按钮与后台服务同样使用当前设置值。
- 自动验证：新增启动文本选择、默认值、换行规范化、空值回退和长度边界单元测试；`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，49 个测试套件共 203 项，0 失败、0 错误、0 跳过。构建仅保留既有图片裁剪库弃用警告和 Android SDK XML 工具版本提示。
- 底栏朗读状态：全局“关闭语音朗读”开启时，便捷字幕底部朗读开关改为 `Graphic Eq Off` 图标；点击后会同时解除全局关闭状态并开启“发送时播放语音”。紧凑操作菜单保持相同行为和状态文案。

## 2026-08-10 字幕预览交互、音效触发与双指缩放

- 输入预览交互：主界面输入预览、输入预览弹窗和悬浮窗输入预览支持左右滑动移动光标；输入预览启用时会屏蔽大字幕一笔画手势。点按预览显示当前正在编辑的文本，长按复制当前编辑内容；悬浮窗预览补齐复制、粘贴、清空和关闭操作，发送后继续清空输入框。
- 音效触发抑制：修复快捷文本命中音效板后仍继续朗读的问题。“触发音效时不朗读文字”现在对语音识别、手动输入和允许触发音效的快捷文本使用同一判定；仅在实际命中触发词时跳过本句 TTS。
- 识别管线收敛：生产运行时移除兼容/实验模式切换和旧声纹门控分支，统一使用当前 V6 实时识别与本人声纹管线；旧识别模式配置键和归一化接口仅保留给历史设置、备份还原与迁移读取，不再影响运行时行为。
- V6 标点边界：V6 仍需保留完整资源包内的独立标点模型。开启 ITN 且模型声明内建标点时直接保留 SenseVoice 输出并跳过外置恢复；关闭 ITN 时使用外置标点模型补回最终句读，因此不能从资源包中删除该组件。
- 双指缩放：主界面大字幕、主界面输入预览、输入预览弹窗、全屏大字幕预览、LED 字幕、LED 输入预览、LED 大字幕预览弹窗、悬浮窗展开态大字幕、悬浮窗输入预览和悬浮窗大字幕预览均支持双指缩放字号。单指点击、长按、滚动、左右移光标和 LED 拖动保持原行为；缩放过程中实时更新，手势结束后才持久化字号。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，50 个测试套件共 211 项，0 失败、0 错误、0 跳过。Debug APK 为 `106908364` 字节，Release APK 为 `97882038` 字节；构建仅保留既有图片裁剪库弃用和 Android SDK XML 工具版本提示。

## 2026-08-10 V6 内建标点收敛与稳定资源包

- 识别路径：SenseVoice V6 固定启用内建 ITN 与标点输出，最终文本直接使用模型结果；移除独立标点推理引擎、资源仓库、运行时加载与释放、下载源、下载进度、安装校验、设置状态和用户开关，不再对流式或最终文本执行第二次标点恢复。
- 设置与迁移：设置页不再显示 ITN 或自动标点选项。新安装和旧内置地址迁移统一指向 `kigtts-recognition-resources-v6-20260810.7z`，用户手动填写的自定义下载地址仍保持不变；旧实验地址仅作为识别并迁移到新默认地址的历史常量保留。
- 稳定资源：从 V6 暂存副本生成 `D:\KIGTTS_ASR_Resource\kigtts-recognition-resources-v6-20260810.7z`，移除 `asr/punct` 与 `asr/punct-en`，清单由 15 项缩减为 11 项。归档采用 32MB LZMA2 字典、非固实压缩，大小 `324911962` 字节，SHA-256 `f02aab69b8c131da12e610694a85815a70d190c4a166fb012e1b3c068f40042a`；`7z t`、完整解压、逐文件大小与 SHA-256 复核均通过，未发现 `punct` 路径。
- 双源发布：新包已上传到 ModelScope `LHTSTUDIO/KIGTTS_ASR_Resource` 与 Hugging Face `LHT02/KIGTTS_ASR_Resource`，公开下载对象的大小和哈希与本地一致。ModelScope 上传提交 `ff169b96`、清理提交 `0da74256c1f185ec95cc1f5e483d746924862ec1`；Hugging Face 上传提交 `59ed9b7a85b95345921cca34d9bdc69e7164caea`、清理提交 `fac0e9bd4b9d381c6d98fd66e862c82293d45654`。
- 远端清理：两个资源仓库均移除未发布的 `experimental` 系列包和独立标点归档；已上线 App 使用的 `kigtts-recognition-resources-20260505.7z` 原样保留。最终两个源都只包含旧线上包和新稳定 V6 包及仓库元数据。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，49 个单元测试套件共 203 项，0 失败、0 错误、0 跳过。Release APK 为 `97865654` 字节，SHA-256 `9DE4DCA5C7779A171C1C7794417F937F95B549AD9AC0A922D33AF7A17E630966`；APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过，版本保持 `0.1.5 (6)`。

## 2026-08-10 聆听模式与字幕排版菜单

- 模式入口：便捷字幕顶栏在全屏按钮右侧新增“聆听模式”。从“点击开关”首次进入时会要求选择“按住说话”或“按住并确认”，可选择下次不再提示；选择结果和提示偏好均持久化，已有按住模式保持不变。
- 双区字幕：竖屏按上下、横屏按左右拆分大字幕与聆听字幕，横竖屏分别保存换边状态。聆听字幕按最终断句逐行追加，流式结果单独更新；最新、上一条和更早内容分别使用 100%、75% 和 50% 不透明度。用户查看历史时暂停跟随并恢复全不透明，回到末尾容差区后继续自动跟随。
- 显示控制：聆听字幕有独立字号、双指缩放、上下翻转、区域互换、回到最新、识别语言、输入设备和可滚动预览。聆听期间隐藏主字幕 LED 入口；主字幕的粗体与对齐合并为一个 `Text Fields` 图标菜单，横竖屏共用同一行为，使用引导同步更新。
- 音频管线：聆听与主语音按钮共用一条 `AudioRecord`，避免多录音器抢占设备；聆听识别绕过本人声纹，仅在按住语音按钮时临时开启主识别。相同语言复用 SenseVoice 实例并串行解码，不同语言才加载第二实例。普通按住模式松手会刷新尾段，确认模式提交后明确恢复为仅聆听，避免吞尾字或持续误入主字幕。
- 独立设置：设置的语音识别区域新增聆听模式二级页，单独保存语言、内置麦克风默认路由、最低音量、语音增强、兼容降噪和 VAD。字号、翻转和换边等显示操作不会重建识别器或重启收音。
- 悬浮窗：迷你悬浮窗增加完全独立的聆听卡片；竖屏叠放、横屏并排，换边设置与主界面一致。卡片支持历史滚动、边缘渐变、透明度层级、双指缩放、语言和麦克风菜单及独立可滚动预览，锁屏宿主和普通系统悬浮窗复用同一套控件与定位逻辑。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，50 个单元测试套件共 206 项，0 失败、0 错误、0 跳过。Release APK 为 `97,914,806` 字节，SHA-256 `58305CC2504F7565BB875088C3623AB798B7FC48AC79F39B95397842180B14BD`，版本保持 `0.1.5 (6)`；本轮未安装到实机，聆听双区和悬浮窗交互仍需设备烟测。

## 2026-08-10 APP 0.1.6 构建与实机安装

- 版本更新：Android 原生端版本更新为 `0.1.6 (7)`，本轮不调整 Flutter 版本，也不修改发行版下载链接。
- 构建校验：`:app:assembleRelease` 与 `lintVitalRelease` 通过；Release APK 为 `97,914,802` 字节，SHA-256 `61B2AC9BE09245D2593210B5C909A9651847E2019564B94E30E70E2BC268116F`。APK 内版本确认为 `0.1.6 (7)`，APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。
- 实机验证：通过 `adb install -r` 保留数据覆盖安装到 Xiaomi Redmi `23113RKC6C`（序列号 `cfc8ef16`）；设备 `dumpsys package` 回报 `versionName=0.1.6`、`versionCode=7`。冷启动后应用进程正常存在，本次启动日志未发现应用崩溃或 ANR。

## 2026-08-10 聆听字幕跟随与弹出菜单动画修正

- 菜单退场：便捷字幕文本样式菜单、紧凑输入操作菜单和聆听字幕菜单统一使用现有 `Md2AnimatedOptionMenu`，关闭时保留 180ms 淡出与缩放退场，不再因状态立即销毁而突兀消失；主字幕与聆听字幕工具条末端按可滚动状态显示同色渐变。
- 自动跟随：聆听字幕改为监听 `LazyList` 实际布局和末行高度，合并高频流式更新，并在重新测量后补齐滚动到真实底部；程序滚动不再误判成用户查看历史，用户主动上翻、接近底部吸附和“回到最新”行为保持不变。
- 断句稳定性：识别解码增加代际校验，最终断句、VAD 边界和收音状态变化会使旧流式任务失效；流式回调顺序收敛，避免旧结果在最终句之后回写并覆盖新行。
- 聆听交互：上下翻转增加淡出切换；主界面与悬浮窗顶部折叠栏在“关闭语音朗读”同一行提供聆听模式开关，入口图标保持不染色。悬浮窗菜单样式与既有控件统一，横屏左右换边键按实时方向显示。
- 悬浮窗稳定性：聆听卡片以单实例在启动器和迷你面板宿主之间迁移，支持迷你便捷字幕、迷你启动器和迷你音效板，迷你快捷名片中隐藏；显示目标状态与实时音量刷新解耦，避免动画被高频取消后卡片停留在透明状态。
- 最终验证：`:app:testDebugUnitTest :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，50 个单元测试套件共 206 项，0 失败、0 错误、0 跳过。Release APK 为 `97,931,186` 字节，SHA-256 `3A3FBD4BB00DC3B248EF2D8C3A411C388BF74FC5D1F158292B0FAFE790739EAA`；APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。已覆盖安装到 Redmi K70，确认 `0.1.6 (7)`、非 debuggable、主进程正常且启动日志无应用致命异常或 ANR。

## 2026-08-11 聆听输入态、后台性能与悬浮窗安全区

- 输入态显示：聆听模式二级设置新增“输入模式下隐藏聆听字幕”，默认关闭。关闭时键盘弹出只隐藏聆听字幕底部工具条，开启时主界面和悬浮窗输入面板会暂时收起整个聆听区域，退出输入后恢复。
- 渐变一致性：主字幕与聆听字幕工具条、聆听内容滚动区的渐变统一从卡片实际海拔色过渡到同色透明，不再经过透明黑而显得比卡片更深；横竖屏工具条随键盘状态使用淡入淡出和尺寸过渡。
- 流式字幕：为 SenseVoice 滚动窗口结果增加短句级重复折叠，再执行重叠拼接，避免“很高兴。高兴。很高兴。高兴。”在最终结果到达前反复显示；聆听专用滚动解码间隔调整为 520ms，按住确认预览仍保持 260ms。
- 后台负载：主 Activity 退到后台时解除 Compose 对实时宿主的状态订阅，回到前台时复用既有宿主且不重复扫描语音包；悬浮窗不再随 60ms 音量刷新重复重建聆听内容或计算窗口位置，已绑定宿主时也不再走重复运行时桥接刷新。
- 安全区：普通悬浮窗、迷你面板与聆听卡片统一按导航栏安全边界计算宽高、居中和夹取范围，兼容底部导航条以及横屏侧边导航键。
- 历史与预览：语音识别历史在实时宿主和备用 ViewModel 路径统一最多保留 50 条，新增记录时淘汰最旧内容；聆听字幕预览弹窗支持长按单条字幕复制并显示完成提示，单击关闭行为保持不变。
- 验证结果：完整 `:app:testDebugUnitTest :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 通过；最终补充历史上限与预览复制后再次运行全部 50 个单元测试套件，共 208 项，0 失败、0 错误、0 跳过，并重新完成 Debug Kotlin 编译和 Release 构建。实机确认键盘工具条隐藏、设置开启后输入态整个聆听区隐藏；聆听关闭与开启后的后台稳态样本约 `0.7%-1.2% CPU`，日志未发现应用 ANR 或致命异常。Release 已覆盖安装到 Redmi K70，版本 `0.1.6 (7)`，APK 为 `97,931,186` 字节，SHA-256 `CD7070ACE4D79FB82BE02E7FA930F478142A0DC0922CA7604503CD840623F132`，v2 签名与正式证书校验通过。

## 2026-08-11 悬浮窗确认操作对齐与聆听字幕收尾

- 确认操作对齐：悬浮窗“按住并确认”的两个目标按钮改为读取当前麦克风按钮的实时屏幕中心，并统一换算到确认层坐标；移除半个 FAB 尺寸和方向性 dp 微调等手算补偿。竖屏三个按钮共用水平轴，手机横屏共用垂直轴，容器边界夹取也不会让两个目标按钮相互错位。
- 几何防护：新增独立确认按钮布局计算和横屏、竖屏、边界夹取单元测试，悬浮窗尺寸、迷你页面高度或横竖屏布局变化时不再依赖固定预制尺寸。
- 聆听断句：VAD 已经结束句段但最终解码为空时，控制器仍发送句段结束事件；实时宿主会使用当前流式字幕作为兜底最终文本。有正常最终文本时与流式预览去重并补充标点，然后清空流式行并换行，避免后续语句持续堆积在同一条。
- 渐变动效：主字幕和聆听字幕工具条的起止端渐变改为 140ms alpha 动画，并补齐滚动起始端；主界面聆听字幕上下渐变使用 150ms 动画。悬浮窗聆听字幕上下渐变复用可中断的 View alpha 动画，并记忆目标状态，快速滚动时不会反复重启动画。
- 自动验证：完整 `:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 通过，51 个单元测试套件共 213 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。Release APK 为 `97,931,186` 字节，SHA-256 `B3949CD29D395C5D8572AF23F285EE1C2CE63CD1D0059A59665CE2B2027BA185`。
- 实机验证：Redmi K70 `cfc8ef16` 上实际按住悬浮窗确认按钮，竖屏确认两个目标与麦克风中心 Y 坐标一致，横屏中心 X 坐标一致；切换方向后布局继续正确。最终 Release 已保留数据覆盖安装，设备回报 `0.1.6 (7)`，主进程正常，启动日志无应用致命异常或 ANR，系统旋转设置已恢复自动。

## 2026-08-11 回退多尺寸布局检查改动

- 回退范围：撤销本轮异常分辨率与 DPI 检查期间加入的悬浮窗安全宽度、聆听卡片尺寸、锁屏时钟避让和平板横屏分区策略，同时删除对应的临时布局策略与单元测试。
- 工具栏回退：一并撤销检查后追加的主字幕与聆听字幕工具栏 48dp 强制高度和底部对齐调整，恢复到检查前的既有布局测量方式。
- 保留内容：不回退更早完成的聆听模式、悬浮窗确认操作对齐、断句修复、渐变动效及其它当前未提交功能。
- 验证结果：`:app:compileDebugKotlin :app:testDebugUnitTest :app:assembleRelease` 与 `git diff --check` 通过，51 个单元测试套件共 213 项，0 失败、0 错误、0 跳过。Release APK 为 `97,931,186` 字节，SHA-256 恢复为检查前的 `B3949CD29D395C5D8572AF23F285EE1C2CE63CD1D0059A59665CE2B2027BA185`；已覆盖安装到 Redmi K70，设备保持物理分辨率 `1440x3200` 与用户原有 `577dpi`，应用回报 `0.1.6 (7)`，启动日志无应用崩溃或 ANR。

## 2026-08-11 进一步回退便捷字幕工具栏包装

- 根因定位：主字幕与聆听字幕工具栏从 8 月 10 日加入滚动渐变后，共同改为通过 `ScrollableToolbarFade` 的额外 `Box` 容器布局；该容器改变了 `weight`、`fillMaxSize` 与 `Crossfade` 的测量关系，是两个工具栏位置同时异常的最早共同改动。
- 回退内容：恢复工具栏原有的直接 `Row/Column` 布局和主字幕横屏纵向滚动方式，移除滚动渐变包装；同批次的识别结果代际防护以及之后完成的聆听模式功能均保留。
- 验证结果：`:app:compileDebugKotlin :app:testDebugUnitTest :app:assembleRelease` 与 `git diff --check` 通过，51 个单元测试套件共 213 项全部通过。Release APK 为 `97,931,186` 字节，SHA-256 `9980410A58AFDDE0D852492A7D57634EAC41A08CFF3033F4D2FE635EB3301078`；已覆盖安装到 Redmi K70。正常 `1440x3200@577dpi` 实机截图确认聆听提示恢复居中，主字幕与聆听字幕工具栏均回到各自卡片底部，启动日志无应用崩溃或 ANR。
## 2026-08-11 聆听分段、工具条渐变与预览修正

- 聆听流式转写改用滚动窗口专用合并策略：只拼接可靠重叠内容，无重叠窗口直接替换，避免长时间聆听时文本重复累积。
- 最终确认结果不再与旧流式预览强制拼接；最终解码为空时才回退到当前预览。
- 聆听模式下 VAD 最长连续语音分段由 12 秒缩短为 6 秒，经典 VAD 与 Silero VAD 使用同一动态上限，并保留前滚音频余量。
- 大字幕和聆听字幕工具条恢复两端滚动渐变，渐变直接绘制在原有 `Row` / `Column` 上，不增加影响布局测量的包装容器。
- 竖屏聆听字幕工具条固定为 48dp，并复用大字幕的 4dp/44dp 横向留白和 36dp 滑条高度，避免切换时上下跳动及左右位置不一致。
- 主界面与悬浮窗的聆听预览统一取消历史文本透明度；悬浮窗在横竖屏切换后会刷新聆听卡片，恢复横屏左右位置切换键。
- 主界面与悬浮窗的聆听输入设备入口统一使用 `mic_gear` 图标。
- 横屏聆听工具条补齐与大字幕一致的 2dp 按键间隔和滑条区域上下 4dp 内边距。
- 验证：`:app:testDebugUnitTest :app:assembleRelease` 与 `git diff --check` 通过，51 个测试套件共 217 项全部通过。Release APK 为 `97,931,186` 字节，SHA-256 `95211B231FF47A88F0F24E0F620CCD3DDBD5576FA4E7F8C73B457CB3DB9CE40F`；已覆盖安装到 Redmi K70，横屏主界面截图确认大字幕与聆听字幕工具条基线正常，启动日志未发现应用崩溃或 ANR。

### SOSV 字幕生命周期对照调整

- 参考 `LHT02/auto-caption` 的 SOSV 实现（提交 `928c72d683f76dd31a2bf4765b869a9948d8af83`）：流式预览在单次 VAD 语句内反复解码完整缓冲区，并更新同一条字幕，而不是拼接多个最近窗口。
- 聆听模式的 Silero VAD 参数对齐为最短静音 0.1 秒、最短语音 0.25 秒、最长语音 5 秒；未检测到说话时仅保留 10 个 Silero 窗口作为预览前滚。
- KIGTTS 自有的降噪、语音增强、最低音量和经典 VAD 辅助过滤保持不变；没有移植 `auto-caption` 的杂音过滤实现。
- 验证：`:app:testDebugUnitTest :app:assembleRelease` 与 `git diff --check` 通过，51 个测试套件共 218 项全部通过。Release APK 为 `97,931,186` 字节，SHA-256 `C1CCEE8EC9DE24C2A8F07FE2F8826CEF1760A45C3AA170E404D8FC99285029D3`；已覆盖安装到 Redmi K70（版本 `0.1.6`，versionCode `7`），应用进程正常启动，最近 500 行应用日志未发现崩溃或 ANR。设备当时处于锁屏状态，本轮未进行真实麦克风连续聆听复测。

### 聆听模式强制分段与虚拟长对话烟测

- 修复持续环境声下 Silero 长时间保持说话状态时只裁剪流式窗口、不一定产生最终结果的问题：新增独立的 5 秒聆听边界策略，达到连续语音上限后强制刷新 VAD、最终解码当前内容并开始新行。
- 聆听模式的自然静音确认阈值由 0.1 秒调整为 0.3 秒，减少逗号、换气和短暂停顿导致的碎片化；普通识别管线参数保持不变。
- Debug 实时管线回调补齐聆听流式与最终结果，新增 `ListeningPipelineDeviceTest`，长音频由 instrumentation 按真实时间注入正式降噪、语音增强、VAD 和 SenseVoice 管线，规避厂商系统的后台广播时限。
- Redmi K70 使用微软晓晓和云希交替的 34.296 秒中文对话，轮次间隔 1.2 秒并加入固定随机种子 16 dB SNR 环境底噪：调整前产生 16 条过碎字幕，调整后减少为 11 条且完整覆盖四轮对话。
- 另用微软晓晓生成 29.592 秒无标点连续长句并加入 12 dB SNR 环境底噪，最终稳定确认 5 条字幕，验证没有自然静音时也会定期换行；两项测试均在手机上静默执行，不占用麦克风且不播放音频。
- 验证：`:app:testDebugUnitTest :app:assembleRelease`、`lintVitalRelease` 与 `git diff --check` 通过，52 个测试套件共 221 项全部通过；两项 `ListeningPipelineDeviceTest` 实机运行均通过。Release APK 为 `97,931,186` 字节，SHA-256 `E1997AC9D946425F09B3ED55ADE88C38CC889DBDBAF17C433BF6795D327A39C4`，已覆盖安装到 Redmi K70（版本 `0.1.6`，versionCode `7`），启动日志未发现崩溃、ANR 或 ART 校验错误。

### 聆听确认与 FAB 识别所有权修复

- 根因修复：聆听流式结果即使内容不变也会反复重置宿主的稳定确认计时，导致同一行可能永久停留在预览态。宿主现在仅在可见预览实际变化时重新计时，最终结果和兜底确认统一进入逐条字幕列表。
- 终点防护：Silero 未及时输出句段时，聆听管线会在检测到说话后的 450ms 尾部静音主动刷新当前句段；原有 5 秒连续语音强制边界继续保留。最终解码为空时仍使用当前流式结果，避免吞掉已经显示的文字。
- 重复防护：稳定计时与引擎最终结果几乎同时完成时，会按忽略标点后的包含关系判断为同一句，并使用最终结果更新兜底行，不再拆成相邻重复字幕。
- 列表标识：启动烟测捕获到引擎正数结果 ID 与宿主兜底 ID 都从 `1` 开始时会触发 Compose 重复 key 崩溃。兜底字幕现使用递减负数 ID，引擎 ID 与已有条目冲突时也会自动分配负数，并新增分配策略单元测试和宿主字幕 ID 唯一性断言。
- 控制权切换：`RealtimeHostService` 统一管理聆听和 FAB 的识别所有权。按下 FAB 时暂停聆听、启用主识别和确认流式；提交完成时先关闭确认流式，再恢复聆听，避免设置观察器异步覆盖状态或把 FAB 尾音送进聆听列表。
- 真机回归：新增 `RealtimeHostListeningDeviceTest`，在同一个真实宿主服务中注入 14 秒微软多说话人噪声音频，确认连续聆听产生多条最终字幕；随后切到“按住确认”，关闭与控制权无关的声纹门禁后验证 FAB 获得识别文本，提交后聆听恢复。该宿主测试与原有 `ListeningPipelineDeviceTest` 均在 Redmi K70 上通过。
- 自动验证：`:app:testDebugUnitTest :app:assembleDebugAndroidTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，53 个单元测试套件共 226 项，0 失败、0 错误、0 跳过。Release APK 版本保持 `0.1.6 (7)`，大小 `97,947,570` 字节，SHA-256 `B0202C8712A5E9327A5DCCF936DF9CC30414ADD4A39CB63C156AC431E9FC9720`；APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。
- 实机收尾：最终 Release 已保留数据覆盖安装到 Redmi K70，冷启动 `TotalTime=987ms`，前台持续运行 45 秒后主进程仍正常；确认版本 `0.1.6 (7)`、不可调试，日志未再出现字幕重复 key、应用崩溃或 ANR。Android 测试包已从设备移除。

### 悬浮窗聆听卡片与锁屏响应式布局

- 卡片组合：悬浮窗聆听卡片改为按主悬浮窗的实际可见卡片边界定位。竖屏两者等宽，横屏两者等高，间距统一为悬浮窗内部卡片使用的 `12dp`，透明外层内边距不再造成视觉错位。
- 悬浮预览：聆听卡片复用现有悬浮窗预览遮罩与大字幕预览的固定尺寸根容器，启动器展开态和迷你页面都可点按卡片打开完整可滚动预览、点按预览关闭；卡片与预览内部都显式区分轻点和滚动并触发 `performClick()`，不再被 `ScrollView` 吞掉。聆听预览会直接挂载到卡片当前所在的实际根容器，并使用高于普通卡片的固定海拔；实时刷新和窗口重新定位期间均保持预览在最前，避免跨宿主状态误判或原卡片覆盖预览。预览进入与退出动画由目标可见状态驱动，高频聆听刷新只更新现有文本，不再重建整张卡片、反复播放动画或让半透明遮罩滞留。
- 锁屏手机布局：仅在“锁屏、聆听模式、悬浮窗启动器展开态”同时成立时，将时间、日期、电量压缩为悬浮窗组合上方的一行；电量开启时保持时间左、日期中、电量右，电量关闭时日期居中且时间移至最右。电量左侧使用同色 `Battery Android` 系列 Material Symbols 图标，按电量切换 `frame_1` 至 `frame_6`，充电、极低电量和满电分别使用 `bolt`、`alert` 与 `full`；进入其它迷你悬浮窗页面后隐藏顶部信息。
- 大屏适配：新增最小宽度不低于 `600dp` 且宽高比不超过 `1.28` 的展开态近方形布局，小尺寸方形外屏不会命中。近方形屏仅在聆听卡片上方空间放不下正常时钟组时启用单行；横屏平板的时钟与解锁提示作为同一组居中于左半区，左对齐样式下解锁文字与时钟同轴，箭头移动到文字右侧。
- 几何验证：新增悬浮窗聆听卡片可见边界、间距、顶部/底部保留区，以及手机、平板和展开态近方形模式判定的单元测试。`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，55 个测试套件共 234 项，0 失败、0 错误、0 跳过。Release 已保留数据覆盖安装到 Redmi K70；锁屏实机确认卡片等宽与间距、单行日期时间布局、预览遮罩层级及轻点关闭均正常，12 秒录屏按每秒 2 帧抽查的 24 帧预览持续完整，日志无应用崩溃或 ANR。最终 APK 为 `97,947,570` 字节，SHA-256 `D0418594F274BC26269ED6A046E6AE8E011A725DE1B41CC67ED14C41C0BA0BA6`。
- 后续修正：手机聆听模式的单行信息在关闭电量显示时调整为时间居左、日期居右；开启电量时保持时间、日期、电量从左到右排列。锁屏宿主内嵌悬浮窗在聆听卡片可见时把 Window 收束到两张卡片的实际组合边界，并使用公开的非模态窗口标志，让外围区域继续交给锁屏根视图处理滑动解锁；打开聆听预览时才临时恢复全屏窗口。
### 按住确认最终结果收口修正（2026-08-11）

- 按住确认继续保留类似语音输入法的分段拼接：已经确认的识别分段会依次接到前文。
- 松手提交时不再把仍在滚动的流式预览提前当成确认结果；它只作为最终解码为空时的界面兜底，避免纠正后的最终文本被重复拼接。
- `RealtimeController.flushPendingRecognition()` 现在会等待已提交 ASR 任务对应的结果回调完成，再让宿主提交整段文本，修复最后一段回调晚于提交动作导致的“不确认”问题。
- 新增回调等待与多段听写拼接回归测试；实机虚拟音频烟测改用异步结果回调，覆盖与真实麦克风一致的松手提交时序。

### 悬浮窗聆听预览与横屏几何修正（2026-08-11）

- 聆听历史预览与通用字幕预览在圆角卡片边界内裁剪文本和滚动内容，外层仍保留阴影空间，避免长历史文字越过卡片圆角。
- “回到最新字幕”和自动跟随改为按文本实际高度减去视口高度计算最大滚动位置，横屏窄卡片可稳定滚动到最后一行。
- 手机横屏的“聆听卡片 + 主悬浮窗”组合沿用普通悬浮窗已有的 `16dp` 左移补偿，并继续以扣除导航栏后的安全区域为基准，修复组合整体偏右。

### 悬浮窗聆听预览方向与条目交互修正（2026-08-12）

- 聆听历史预览复用迷你大字幕的预览遮罩、圆角卡片、海拔和横竖屏尺寸策略，不再维护一套独立预览窗体。
- 横竖屏切换时按当前窗口尺寸重建预览卡片；普通系统悬浮窗的屏幕和安全区测量改用当前窗口指标，避免旋转后继续读取竖屏尺寸。
- 聆听历史改为可滚动的独立条目列表。识别历史或流式文本变化时原位刷新，贴近底部时继续跟随最新内容，查看旧记录时保留原滚动位置。
- 每条聆听字幕支持长按复制并提供震动与完成提示；点按条目或卡片空白处仍关闭预览。
- 实机验证：Redmi K70 上确认聆听预览在横屏显示后切换竖屏会立即按新方向重建，内容保持在共享预览卡片边界内；长按单条字幕会写入系统剪贴板并显示“已复制”。最终 Release 已保留数据覆盖安装，应用进程正常，启动日志未发现应用崩溃、ANR 或 ART 校验错误，系统自动旋转设置已恢复。
- 自动验证：`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，56 个单元测试套件共 237 项，0 失败、0 错误、0 跳过。Release APK 保持 `0.1.6 (7)`，大小 `97,963,954` 字节，SHA-256 `E5D3FC7EF09CF451033F496C8DDA0F1F47DDC177B5AE457884D5F16643ACC983`；APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。

### 锁屏紧凑时钟与平板候选列表定位（2026-08-12）

- 锁屏聆听模式的紧凑日期时间改为强制单行：时间固定贴齐悬浮窗可见卡片左边缘；关闭电量显示时日期贴齐右边缘，开启电量显示时电量贴齐右边缘且日期使用中间剩余空间。日期空间不足时只在末尾省略，不再自动换行。
- 紧凑状态行与锁屏悬浮窗复用同一套外层宽度、水平位置和 `10dp` 可见卡片内缩计算，手机竖屏与展开态近方形大屏均有纯几何回归测试。
- 平板候选列表会记录快捷文本区域边界和长按/手势实际触发点。横屏时弹窗右边缘对齐快捷文本区右边缘，纵向位置跟随触点；竖屏时弹窗底边对齐快捷文本区底边，横向位置跟随触点，并统一限制在窗口安全边距内。手机继续使用原有居中弹窗。
- 平板或大屏横屏非紧凑模式下，主页面分组栏和候选列表内的分组预览气泡均跟随当前分组条目的实际中心线，显示在对应条目左侧；手机端保持原有相对整个分组控件居中的位置。
- 自动验证：`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，57 个单元测试套件共 242 项，0 失败、0 错误、0 跳过。Release APK 保持 `0.1.6 (7)`，大小 `97,963,954` 字节，SHA-256 `76030BEA097F7C7A65AD1491FD3A738FA3EC16AAA472D90B8549FCC20A1E61AF`；APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。最终 Release 已保留数据覆盖安装到 Redmi K70，启动日志未发现应用崩溃、ANR 或 ART 校验错误，验证过程未修改设备分辨率或 DPI；平板定位目前由纯几何单元测试覆盖，尚未在平板实机上复核。

### 快捷文本候选排序、桌面小工具与音频生命周期修复（2026-08-12）

- 候选列表：主界面便捷字幕候选弹窗改用支持 Pick up and move 的 RecyclerView。长按后移动会浮起条目并拖动排序，未移动直接松手则显示单行纯图标编辑/删除菜单；频率排序开启时会在确认后应用当前顺序并切回手动排序。
- 桌面小工具：新增快捷入口、快捷名片和快捷字幕三个独立 AppWidget。快捷入口默认 4x1；名片与字幕默认 4x5 且可调整尺寸。只有名片支持翻页，空名片会进入新建流程；字幕候选分组、条目及条目色与主应用同步，每个小工具的大字幕分别存储且不读取主界面当前字幕。
- 字幕小工具：候选区域不使用滚动容器，先显示可左右翻页的分组列表；进入分组后，快捷文本列表会占满 Logo 下方的全部区域，并提供上下翻页和返回键。选择条目只替换当前小工具的大字幕并自动返回常规字幕状态，不启动主应用、不朗读也不触发音频模块。分组页和条目页末尾都会补充不可见占位格，避免不足一页时现有条目被拉宽或拉高；小工具固定使用系统字体，避免独立进程加载自定义字体资源。
- 名片小工具：外观复用悬浮窗单张名片并取消外层边框，左下角链接区替换为纯图标的左右循环翻页键。二维码以 640px 位图单独传入 `ImageView`，二维码下方白色圆角卡由 `RemoteViews` 的 shape drawable 独立绘制，不再合成进会被缩放的名片背景位图；标题、备注、打开图标、翻页键与 Logo 同样作为独立矢量或文本前景层，降低高 DPI 启动器上的模糊。
- 视觉与主题：快捷入口使用无底色 Material Symbols 矢量图标；右上角打开键直接使用应用内 `Material Symbols Sharp` 字体中 `open_in_new` 的原始轮廓，翻页及方向图标同样使用矢量 drawable。静态小工具颜色随系统亮暗主题切换，并在系统配置变化时主动刷新。
- 进程边界：三个 Provider 均运行在 `:widgets` 进程；主进程只以 AtomicFile 发布精简 JSON 快照，存在小工具实例时才发出定向刷新，未添加小工具时不会启动 Widget 进程。Widget 进程不打开 UserPrefs/DataStore，也不初始化 ASR、TTS、悬浮窗或实时宿主；名片页码和各字幕小工具的大字幕只保存在 Widget 进程专用状态文件中。
- 深链路由：快捷入口可分别打开便捷字幕、快捷名片、画板和音效板；名片预览按 ID 定位，空状态直接进入新建文本名片。字幕条目点击保持在桌面并更新对应小工具，不经过主应用的朗读与上屏链路。
- 聆听语言崩溃：实机 tombstone 指向旧版 RNNoise 的进程级 FFT 计划。原实现会在销毁任意一个降噪实例时释放所有实例共享的 FFT 资源且保留已初始化标志。JNI 现串行化原生入口并统计实例引用，只在最后一个实例销毁后释放及重置全局 FFT 状态。
- 音频审计：ASR 流改为异常路径也必定释放；Piper 每次合成创建的 ONNX 输入 Tensor 在 `finally` 中关闭；神经说话人分离器的推理、重置和关闭使用同一对象锁且关闭幂等；ASR 资源切换会先停止并等待识别任务，采用先创建后替换的事务式切换并释放旧识别器，失败时恢复原识别管线。
- 回归覆盖：新增 Widget 快照解析单元测试、RNNoise 四实例并发处理/错峰销毁实机测试，以及 RNNoise 持续录音时依次切换中、英、日、韩、自动语言的实机测试。
- Launcher 兼容：Redmi K70 的 MIUI Launcher 实例测试发现 `RemoteViews` 禁止加载 `android.widget.Space` 和基础 `android.view.View`，占位及分割元素均改用白名单内的 `TextView`；三个小工具已以真实桌面实例复测，当前日志未再出现 `Error inflating RemoteViews` 或 Widget 进程崩溃。字幕小工具只保留顶部 Logo 和打开键的应用跳转，动态条目的外层与文本层共用同一选择操作；名片小工具只在标题区、二维码、Logo 和右上角打开键提供跳转，避免 MIUI 同时命中动态分组或翻页键下方的整块背景点击层。名片实机截图确认独立二维码白底边缘清晰，未随背景图降采样。
- 最终验证：`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 与 `git diff --check` 通过，58 个单元测试套件共 245 项，0 失败、0 错误、0 跳过。Release APK 为 `98,163,601` 字节，SHA-256 `39016F75A181BF8F38AB2D9C2AB37F2528DD6098FE3F9384BC3F5A8C7A3AF35A`，版本 `0.1.6 (7)`，APK Signature Scheme v2 与正式证书 SHA-256 `3193af87840d767843e42770c92823b20df10b5edf03a7d618ce9f4da0f5e197` 校验通过。已保留数据覆盖安装到 Redmi K70；快捷入口、名片左右翻页、Sharp 右上打开图标、二维码独立白底、字幕两级分页与系统亮暗主题均完成桌面实例复核，用户确认小工具操作正常。

## 2026-08-12 Android APP 0.1.6 发布

- 版本发布：Android 原生端保持 `versionName 0.1.6`、`versionCode 7`，README 的发行页和 APK 直链更新为 `APP0.1.6`；Flutter 重写目录不纳入本次发行。
- 发布范围：整合 V6 实验识别资源、目标说话人筛选与声纹容错、中文 Piper 多语言兼容、语音识别按钮模式、聆听字幕、锁屏与悬浮窗响应式布局、候选文本拖动排序，以及快捷入口/快捷名片/快捷字幕桌面小工具。
- 自动验证：`:app:testDebugUnitTest :app:assembleRelease :app:lintVitalRelease` 全部通过，58 个单元测试套件共 245 项，0 失败、0 错误、0 跳过；`git diff --check` 通过。
- 安装验证：正式 Release 已保留数据覆盖安装到 Redmi K70，版本确认为 `0.1.6 (7)`；三个桌面小工具完成真实启动器复核，用户确认操作正常。
- APK 校验：`app-release.apk` 大小 `98,163,601` 字节，SHA-256 `39016F75A181BF8F38AB2D9C2AB37F2528DD6098FE3F9384BC3F5A8C7A3AF35A`；APK Signature Scheme v2 和正式签名证书校验通过。
